# Postgres PVC is now managed by the Postgres Helm chart.
# This file has been removed to avoid conflicts with the chart's auto-created PVC.
# The chart will create its own PVC that binds to the resume-postgres-local-pv defined in pv-resume-postgres-local.yaml.
