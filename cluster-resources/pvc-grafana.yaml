# Grafana PVC is now managed by the Grafana Helm chart.
# This file has been removed to avoid conflicts with the chart's auto-created PVC.
# The chart will create its own PVC that binds to the grafana-pv defined in pv-grafana.yaml.
