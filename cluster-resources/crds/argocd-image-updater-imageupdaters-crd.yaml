---
# argocd-image-updater ImageUpdater CRD (copied from upstream argoproj-labs/argocd-image-updater)
# This file enables uninstall/install of the ImageUpdater CRD in the cluster.
# Keep this in sync with upstream CRD definitions when upgrading Argo CD Image Updater versions.
# Source: https://github.com/argoproj-labs/argocd-image-updater/blob/master/config/crd/bases/argocd-image-updater.argoproj.io_imageupdaters.yaml

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: imageupdaters.argocd-image-updater.argoproj.io
spec:
  group: argocd-image-updater.argoproj.io
  names:
    kind: ImageUpdater
    listKind: ImageUpdaterList
    plural: imageupdaters
    singular: imageupdater
  scope: Namespaced
  versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          description: ImageUpdater is the Schema for the imageupdaters API
          type: object
          properties:
            apiVersion:
              type: string
            kind:
              type: string
            metadata:
              type: object
            spec:
              description: ImageUpdaterSpec defines the desired state of ImageUpdater
              type: object
              properties:
                applicationRefs:
                  type: array
                  items:
                    type: object
                commonUpdateSettings:
                  type: object
                namespace:
                  type: string
                writeBackConfig:
                  type: object
          required:
            - spec
      served: true
      storage: true
      subresources:
        status: {}
