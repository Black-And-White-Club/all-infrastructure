# Minimal Postgres Operator values
# CloudNativePG operator for managing Postgres clusters
resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 256Mi

# Enable monitoring integration
monitoring:
  podMonitorEnabled: true

# Webhook for admission control
webhook:
  port: 9443
