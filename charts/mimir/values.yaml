# Mimir values - minimal config
# TODO: Copy detailed config from frolf-bot-infrastructure

mimir:
  structuredConfig:
    multitenancy_enabled: false

minio:
  enabled: false

nginx:
  enabled: true
  replicas: 1

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi
