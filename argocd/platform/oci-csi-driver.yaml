---
# ArgoCD Application to install the OCI Block Storage CSI driver.
# The public repo keeps only a scrubbed instance principals template. Supply the
# real compartment-scoped config from a private source before deploying.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: oci-csi-driver
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-4"
spec:
  project: platform
  source:
    repoURL: https://github.com/Black-And-White-Club/all-infrastructure
    targetRevision: main
    path: cluster-resources/csi/oci-csi-driver
  destination:
    server: https://kubernetes.default.svc
    namespace: kube-system
  ignoreDifferences:
    - group: apps
      kind: Deployment
      name: csi-oci-controller
      namespace: kube-system
      jsonPointers:
        - /metadata/generation
        - /metadata/annotations/deployment.kubernetes.io~1revision
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
