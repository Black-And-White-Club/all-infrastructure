# The Lich King

This directory holds the master ArgoCD `Application` that boots up the rest of the platform. The bootstrap script
applies this manifest after the one-time installers so that The Lich King can sync every downstream ApplicationSet.
