# Helm Chart Updates — 2026-07-15

> Auto-generated by nightly-helm-version-check routine.
>
> **Note:** Helm could not be installed in this environment (get.helm.sh and GitHub releases are blocked by egress policy). Chart versions were determined by querying Helm chart repository `index.yaml` files and `Chart.yaml` files directly from GitHub (`raw.githubusercontent.com`), supplemented by GitHub gh-pages helm index files. Versions are accurate for charts whose indexes were reachable; charts whose registries are outside this policy are noted in the skipped section.

## Summary

17 charts checked. 9 updated. 8 require manual review or are already at latest.

- **Updated:** 9 charts
- **Already at latest:** 3 charts
- **Skipped (manual review required):** 5 charts

---

## Updates Applied

| Chart | File | Old Version | New Version | Bump Type | Notes |
|-------|------|-------------|-------------|-----------|-------|
| sealed-secrets | argocd/platform/sealed-secrets-controller.yaml | 2.18.5 | 2.19.1 | patch | |
| argo-rollouts | argocd/platform/argo-rollouts.yaml | 2.40.9 | 2.41.0 | patch | |
| argo-cd | argocd/platform/argocd.yaml | 9.5.14 | 10.1.3 | ⚠️ MAJOR (chart v9→v10) | ArgoCD app stays v3.4.2→v3.4.5; chart 10.0.0 changes networkPolicy.create default from false→true — see breaking changes below |
| argocd-image-updater | argocd/platform/argocd-image-updater.yaml | 1.1.5 | 1.2.4 | minor | |
| kube-state-metrics | argocd/observability/kube-state-metrics-app.yaml | 7.3.0 | 7.8.1 | minor | |
| alloy | argocd/observability/alloy-app.yaml | 1.8.1 | 1.10.1 | minor | |
| prometheus-node-exporter | argocd/observability/node-exporter-app.yaml | 4.55.0 | 4.56.1 | patch | |
| mimir-distributed | argocd/observability/mimir-app.yaml | 6.0.6 | 6.1.0 | minor | ⚠️ Minimum supported Kubernetes bumped to 1.32 — see notes |
| nats | argocd/apps/frolf-nats.yaml | 2.14.0 | 2.14.2 | patch | |

---

## ⚠️ Breaking Changes / Skipped

### argo-cd 9.5.14 → 10.1.3: NetworkPolicy default changed ⚠️

**Applied** (chart 10.0.0 introduced one notable default change):

> In chart 10.0.0, `global.networkPolicy.create` was changed from **`false`** to **`true`**. This means Kubernetes NetworkPolicy resources will now be deployed by default. In environments using a service mesh or custom NetworkPolicies (e.g. Cilium), this can restrict ArgoCD traffic unexpectedly.
>
> **Action if affected:** Add `global.networkPolicy.create: false` to `charts/argo-cd/values.yaml` before syncing, or verify that the generated NetworkPolicies are compatible with the cluster's CNI and existing policies.

The underlying ArgoCD application version only bumped v3.4.2 → v3.4.5 (patch within v3.x), so no ArgoCD application-level migration is required.

---

### mimir-distributed 6.0.6 → 6.1.0: Kubernetes 1.32 minimum ⚠️

**Applied**, but note that 6.1.0 raises the minimum supported Kubernetes version to **1.32**. If the cluster is on Kubernetes < 1.32, do not merge this change until the cluster is upgraded.

Additional 6.1.0 changes: Mimir upgraded to 3.1.2; `kafka.extraEnv` removed (use `kafka.env`); rollout-operator upgraded to 0.48.0 (requires manual pod deletion workaround if upgrading from 6.0.0 or 6.0.1 — see changelog).

---

### loki 6.55.0 → 7.x: SKIPPED — Breaking repoURL migration required 🚨

| Chart | File | Current | Latest (grafana helm index) | Reason |
|-------|------|---------|----------------------------|--------|
| loki | argocd/observability/loki-app.yaml | 6.55.0 | 7.0.0 | SKIPPED — see below |

The file already contains this warning comment:

```
# MIGRATION REQUIRED before next Loki bump: chart 7.0.0 in grafana/loki is GEL (enterprise) only.
# OSS users must switch repoURL to https://grafana-community.github.io/helm-charts before upgrading.
# 6.55.0 is the last OSS-compatible version in this repo. SSD (SimpleScalable) mode is also
# deprecated as of chart 6.52 and will be removed in a future community chart version.
```

**This is not a simple version bump.** Upgrading requires:
1. Changing `repoURL` from `https://grafana.github.io/helm-charts` to `https://grafana-community.github.io/helm-charts`
2. Reviewing the values schema for breaking changes between 6.x and 7.x in the community chart
3. Understanding the SSD (SimpleScalable Deployment) deprecation — migrating to a supported deployment mode if needed
4. Testing the new chart against the existing Loki PVCs/object store

**Do not auto-merge.** Requires a planned migration effort.

---

### frolf-postgres 16.2.4 → 17.1.0: SKIPPED — 16.x→17.x chart transition 🚨 (with key finding)

| Chart | File | Current | Latest (verified) | Reason |
|-------|------|---------|-------------------|--------|
| postgresql (frolf) | argocd/apps/frolf-postgres.yaml | 16.2.4 | 17.1.0 | SKIPPED — see below |

**Key finding:** Bitnami's chart version numbering does NOT 1:1 track the PostgreSQL major version. Verification shows:
- Chart **16.2.4** → `appVersion: 17.2.0` (PostgreSQL **17**)
- Chart **17.1.0** → `appVersion: 17.6.0` (PostgreSQL **17**)

Both chart versions ship **PostgreSQL 17**. There is no PostgreSQL major version change in this upgrade (PG 17.2 → PG 17.6, a patch upgrade).

However, this update is **skipped** because:
- The YAML comment explicitly warns: *"frolf-bot lost its entire DB this way when PR #10 upgraded the chart (16.x→17.x transition)"*
- This is exactly the `16.x → 17.x` chart version transition that the file warns about
- The root cause of the previous data loss should be understood before proceeding (was it a PG major bump at an earlier chart version, or something else in the 16.x→17.x chart transition?)

**Recommended action:**
1. Verify what PostgreSQL version frolf-bot was running _before_ PR #10 caused the data wipe
2. Confirm there are no other `initdb` triggers in the bitnami chart during a chart major version bump (e.g., new default storage configuration, image changes)
3. If the previous data loss was solely due to a PG major version bump (which has since been resolved since both 16.x and 17.x are now PG 17), the update to 17.1.0 may be safe — but back up first
4. Consider whether to step through 16.7.27 (latest 16.x patch) before jumping to 17.1.0

---

### cert-manager v1.16.2 → UNKNOWN: SKIPPED — Cannot verify latest version

| Chart | File | Current | Latest found | Reason |
|-------|------|---------|--------------|--------|
| cert-manager | argocd/platform/cert-manager.yaml | v1.16.2 | Cannot determine | SKIPPED — registry unreachable |

`charts.jetstack.io` (cert-manager's Helm registry) is blocked by egress policy in this environment. The cert-manager GitHub repo's `Chart.yaml` uses a `v0.0.0` placeholder on the main branch (set by the release tooling). No tags beyond `v1.16.2` were found to be accessible. **It is likely that v1.16.2 is still the latest**, but this could not be confirmed. A manual check of https://github.com/cert-manager/cert-manager/releases is recommended.

---

### tailscale-operator 1.96.5 → UNKNOWN: SKIPPED — Cannot verify latest version

| Chart | File | Current | Latest found | Reason |
|-------|------|---------|--------------|--------|
| tailscale-operator | argocd/platform/tailscale-operator.yaml | 1.96.5 | Cannot determine | SKIPPED — registry unreachable |

`pkgs.tailscale.com/helmcharts` is blocked by egress policy in this environment. The `tailscale/helm-charts` GitHub repo (raw content) was not reachable. Check https://github.com/tailscale/helm-charts/releases or the Tailscale changelog for the latest operator helm chart version.

---

### resume-postgres 18.1.13 → UNKNOWN: SKIPPED — Cannot determine latest

| Chart | File | Current | Latest found | Reason |
|-------|------|---------|--------------|--------|
| postgresql (resume) | argocd/apps/resume-postgres.yaml | 18.1.13 | 17.1.0 (GitHub main) | SKIPPED — current appears newer than GitHub main |

The `bitnami/charts` GitHub repository `main` branch currently shows postgresql chart version `17.1.0`. The cluster is running `18.1.13`, which is **higher** than what GitHub main shows. This suggests bitnami's Helm registry has chart versions not yet reflected in the GitHub main branch (likely OCI-based distribution). Since I cannot access `charts.bitnami.com` directly and cannot verify whether a version newer than `18.1.13` exists, this chart is left unchanged.

---

## No Changes (already at latest)

- **ingress-nginx**: 4.15.1 (already latest — `kubernetes/ingress-nginx` main)
- **grafana**: 10.5.15 (already latest — confirmed via `grafana/helm-charts` gh-pages index; app v12.3.1)
- **tempo-distributed**: 1.61.3 (already latest — confirmed via `grafana/helm-charts` gh-pages index; note: chart is marked `deprecated: true` in the index, indicating Grafana may be moving away from the `tempo-distributed` chart)

---

## Changelogs

### sealed-secrets: 2.18.5 → 2.19.1

**Release notes** ([view on GitHub](https://github.com/bitnami-labs/sealed-secrets/releases/tag/helm-v2.19.1)):

App version bumped to `0.38.4` (sealed-secrets controller). Patch release; no breaking changes noted.

---

### argo-rollouts: 2.40.9 → 2.41.0

**Release notes** ([view on GitHub](https://github.com/argoproj/argo-helm/releases/tag/argo-rollouts-2.41.0)):

App version: `v1.9.0`. Patch helm chart release. No breaking changes in helm chart.

---

### argo-cd: 9.5.14 → 10.1.3

**Release notes** ([view on GitHub](https://github.com/argoproj/argo-helm/releases/tag/argo-cd-10.1.3)):

- App version: `v3.4.2` → `v3.4.5` (no ArgoCD major version change)
- **Chart 10.0.0 breaking change:** `global.networkPolicy.create` default changed from `false` to `true`. Review NetworkPolicy compatibility before syncing.
- The ArgoCD application itself is a patch release within the v3 series.

---

### argocd-image-updater: 1.1.5 → 1.2.4

**Release notes** ([view on GitHub](https://github.com/argoproj/argo-helm/releases/tag/argocd-image-updater-1.2.4)):

App version: `v1.2.2`. Minor chart release for argocd-image-updater.

---

### kube-state-metrics: 7.3.0 → 7.8.1

**Release notes** ([view on GitHub](https://github.com/prometheus-community/helm-charts/releases/tag/kube-state-metrics-7.8.1)):

App version: `2.19.1`. Minor release series; no major breaking changes noted.

---

### alloy: 1.8.1 → 1.10.1

**Release notes** ([view on GitHub](https://github.com/grafana/alloy/releases/tag/helm-alloy-1.10.1)):

- 1.10.1 (2026-06-29): Update to Grafana Alloy v1.17.1
- 1.10.0 (2026-06-12): `alloy.command` field added to override the entrypoint; update to Alloy v1.17.0; `controller.autoscaling.horizontal.externalHPA` support added
- 1.9.0 (2026-06-08): `controller.autoscaling.horizontal.externalHPA` for KEDA; Alloy v1.16.3; bug fix for `alloy.configMap.key` being ignored
- 1.8.2 (2026-05-25): config-reloader updated to v0.91.0

No breaking changes noted.

---

### prometheus-node-exporter: 4.55.0 → 4.56.1

**Release notes** ([view on GitHub](https://github.com/prometheus-community/helm-charts/releases/tag/prometheus-node-exporter-4.56.1)):

App version: `1.12.1`. Patch release. No breaking changes.

---

### mimir-distributed: 6.0.6 → 6.1.0

**Release notes** ([view on GitHub](https://github.com/grafana/mimir/releases/tag/mimir-distributed-6.1.0)):

Key changes in 6.1.0:
- Mimir upgraded to 3.1.2
- Minimum Kubernetes version raised to **1.32**
- `kafka.extraEnv` removed; use `kafka.env` instead
- `docker.io` set as default registry for mimir image
- Query-frontend cache size limit increased to 25MB; memory limit to 4GiB
- Rollout-operator upgraded to 0.48.0 (if upgrading from 6.0.0/6.0.1, delete the rollout-operator certificate secret and recreate the pod)
- Various bugfixes for ScaledObject, zone-aware replication, and missing labels

---

### nats: 2.14.0 → 2.14.2

**Release notes** ([view on GitHub](https://github.com/nats-io/k8s/releases/tag/nats-2.14.2)):

App version: `2.14.2`. Patch release tracking NATS server 2.14.2. No breaking changes.

---

*Generated: 2026-07-15 by nightly-helm-version-check routine. Helm registry access limitations noted above; verify unknown chart versions manually.*
