# Helm Chart Updates — 2026-05-25

> Auto-generated by nightly-helm-version-check routine.
>
> **Note:** Helm binary could not be installed in this environment due to network policy restrictions on `get.helm.sh` and chart repository domains (e.g. `*.github.io` chart repos returned 403). Chart versions were sourced from `raw.githubusercontent.com` (Chart.yaml from source repos), the `grafana/helm-charts` `gh-pages` index, the `prometheus-community/helm-charts` `gh-pages` index, and official project documentation. The GitHub REST API was also used but hit the unauthenticated rate limit (60 req/hr) during the run.

## Summary

17 charts checked. 5 updated. 4 skipped or unable to verify (require manual review).

## Updates Applied

| Chart | File | Old Version | New Version | Bump Type | Notes |
|-------|------|-------------|-------------|-----------|-------|
| sealed-secrets | argocd/platform/sealed-secrets-controller.yaml | 2.18.5 | 2.18.6 | patch | appVersion 0.37.0 unchanged |
| argo-cd | argocd/platform/argocd.yaml | 9.5.14 | 9.5.15 | patch | appVersion v3.4.2 unchanged |
| cert-manager | argocd/platform/cert-manager.yaml | v1.16.2 | v1.20.2 | ⚠️ MINOR (4 versions) | See breaking changes section — large version gap, review upgrade notes |
| argocd-image-updater | argocd/platform/argocd-image-updater.yaml | 1.1.5 | 1.2.2 | minor | appVersion v1.2.1; see changelog notes |
| kube-state-metrics | argocd/observability/kube-state-metrics-app.yaml | 7.3.0 | 7.4.0 | minor | appVersion 2.19.0 |

## ⚠️ Breaking Changes / Skipped

| Chart | File | Current | Latest Available | Reason |
|-------|------|---------|------------------|--------|
| postgresql (frolf-bot) | argocd/apps/frolf-postgres.yaml | 16.2.4 | 17.1.0 | **SKIPPED — PostgreSQL major version bump from PG16 to PG17 detected.** Bitnami chart 17.x ships PostgreSQL 17.x (appVersion: 17.6.0). Applying this update WILL WIPE THE FROLF DATABASE (initdb runs on PG major upgrade). frolf-bot already lost all its data once via exactly this mechanism. Requires a migration plan and a scheduled maintenance window. |
| loki | argocd/observability/loki-app.yaml | 6.55.0 | 7.0.0 | **SKIPPED — Chart is now ENTERPRISE ONLY.** As of March 16, 2026, the Grafana Loki OSS chart was forked at version 6.55.0 to [grafana-community/helm-charts](https://github.com/grafana-community/helm-charts). The `grafana/loki` chart version 7.0.0+ is now exclusively for Grafana Enterprise Logs (GEL) — a commercial product. Upgrading would silently switch from OSS Loki to GEL. The cluster almost certainly runs OSS Loki. **Action required:** Migrate the `repoURL` from `https://grafana.github.io/helm-charts` to the community fork (new URL TBD) before any future loki updates. |

## Unable to Verify (keeping current)

| Chart | File | Current | Issue |
|-------|------|---------|-------|
| tailscale-operator | argocd/platform/tailscale-operator.yaml | 1.96.5 | Chart registry at `pkgs.tailscale.com/helmcharts` is blocked by environment network policy. The `tailscale/helm-charts` GitHub repo's Chart.yaml has `version: 0.1.0` as a placeholder (version is stamped at release time). Cannot verify latest without registry access. |
| postgresql (resume) | argocd/apps/resume-postgres.yaml | 18.1.13 | Bitnami chart registry (`charts.bitnami.com`) is blocked by environment network policy. The GitHub `bitnami/charts` main branch CHANGELOG only documents up to 17.1.0 (appVersion: 17.6.0 / PG17), but the current pinned version 18.1.13 is higher — the registry appears ahead of the GitHub main branch. Cannot verify the latest 18.x patch or whether a PG major version change is involved. Keeping current. |

## No Changes (already at latest)

- ingress-nginx: 4.15.1 (already latest, appVersion 1.15.1)
- argo-rollouts: 2.40.9 (already latest, appVersion v1.9.0)
- alloy: 1.8.1 (already latest, appVersion v1.16.1)
- prometheus-node-exporter: 4.55.0 (already latest, appVersion 1.11.1)
- tempo-distributed: 1.61.3 (already latest, appVersion 2.9.0)
- mimir-distributed: 6.0.6 (already latest stable; 6.1.0-weekly.394 is a dev/weekly build — intentionally skipped)
- grafana: 10.5.15 (already latest, appVersion 12.3.1)
- nats: 2.14.0 (already latest, appVersion 2.14.0)

## Changelogs

### sealed-secrets: 2.18.5 → 2.18.6

**Source:** [bitnami-labs/sealed-secrets](https://github.com/bitnami-labs/sealed-secrets/releases/tag/helm-v2.18.6)

Patch release. AppVersion remains 0.37.0. No breaking changes expected.

---

### argo-cd: 9.5.14 → 9.5.15

**Source:** [argoproj/argo-helm](https://github.com/argoproj/argo-helm) — chart `argo-cd`, appVersion v3.4.2.

Patch release. AppVersion v3.4.2 unchanged from 9.5.14. No breaking changes expected.

---

### cert-manager: v1.16.2 → v1.20.2

**Source:** [cert-manager/cert-manager](https://github.com/cert-manager/cert-manager) — [Release 1.20 notes](https://cert-manager.io/docs/releases/release-notes/release-notes-1.20/)

**⚠️ This is a large version gap (4 minor versions: v1.16 → v1.17 → v1.18 → v1.19 → v1.20). Review upgrade notes for each intermediate version before merging.**

**v1.20.2** (latest patch as of 2026-05-25):
- Fixes invalid YAML generated in the Helm chart when both `webhook.config` and `webhook.volumes` are defined.
- Bumps Go to 1.26.2 with dependency security updates.

**v1.20 upgrade from v1.19:** No special upgrade steps required per official docs.

**Recommended:** Review the intermediate upgrade guides at https://cert-manager.io/docs/releases/upgrading/ for v1.16 → v1.17, v1.17 → v1.18, v1.18 → v1.19, and v1.19 → v1.20 before merging. Currently supported versions are v1.19.x and v1.20.x (v1.16 is EOL).

---

### argocd-image-updater: 1.1.5 → 1.2.2

**Source:** [argoproj/argo-helm](https://github.com/argoproj/argo-helm) — chart `argocd-image-updater`, appVersion v1.2.1.

**Notable change in the argocd-image-updater application (v1.2.1):**
- The tool now defaults to using the **Kubernetes API** to retrieve applications instead of the Argo CD API. This is a behavior change.
- It is now recommended to install in the same namespace as Argo CD (`argocd`). If currently installed in a dedicated namespace, review the upgrade notes.
- To retain previous behavior: set `applications_api: argocd` in the `argocd-image-updater-config` ConfigMap before updating.
- ServiceAccount permissions have been adapted to allow read access to `Secret` and `argoproj.io/Application` resources.

**Changelog source:** https://github.com/argoproj-labs/argocd-image-updater/blob/master/CHANGELOG.md

---

### kube-state-metrics: 7.3.0 → 7.4.0

**Source:** [prometheus-community/helm-charts](https://github.com/prometheus-community/helm-charts) — chart `kube-state-metrics`, appVersion 2.19.0.

Minor chart release. AppVersion bumped to 2.19.0 (from 2.18.x).

Notable upstream (kube-state-metrics v2.18.0 release):
- Addresses a regression that caused a panic when new versions were added to existing `CustomResourceDefinition`s.
- `endpointslices` are now part of the default resources exposed as metrics. `endpoints` is deprecated and needs to be manually activated via the `--resources` flag.

---

## ⚠️ Action Required: Loki Migration

The loki chart at `grafana.github.io/helm-charts` is now enterprise-only (GEL). The cluster should migrate to the OSS community chart at `grafana-community/helm-charts` (forked from 6.55.0). Until this migration is completed:

1. **Do not upgrade loki beyond 6.55.0** using the current `repoURL`.
2. Identify the new community chart URL from https://github.com/grafana-community/helm-charts.
3. Update `argocd/observability/loki-app.yaml` with the new `repoURL` before the next loki update.

---

## ⚠️ Action Required: cert-manager EOL

cert-manager v1.16.x is **end of life** (not in the supported releases table as of 2026-05-25). The currently supported versions are v1.19.x and v1.20.x. This update brings cert-manager back into support. Prioritize merging this PR to restore supported status.
