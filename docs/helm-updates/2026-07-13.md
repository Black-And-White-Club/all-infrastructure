# Helm Chart Updates — 2026-07-13

> Auto-generated by nightly-helm-version-check routine.

## Operational Notes

**Network constraints:** This run operated in a restricted-egress environment. Direct Helm repo index fetching was blocked for most chart repositories by the organization's egress policy. Chart versions were discovered via GHCR anonymous OCI tag listing (argoproj, grafana) and the Docker Hub v2 API (bitnami/postgresql). Eight chart repos returned no data and are listed as unknown in the "Version Check Failed" section below.

Helm itself was compiled from source via `go install helm.sh/helm/v3/cmd/helm@latest` (using `proxy.golang.org` which is egress-allowed). Installed version: Helm v3.21.

---

## Summary

17 charts checked. 6 updated. 2 skipped (require manual review). 1 already at latest OSS-compatible version (Loki). 8 chart repos unreachable (version unknown).

---

## Updates Applied

| Chart | File | Old Version | New Version | Bump Type | Notes |
|-------|------|-------------|-------------|-----------|-------|
| argo-cd | argocd/platform/argocd.yaml | 9.5.14 | 10.1.3 | ⚠️ MAJOR (9→10) | ArgoCD major chart bump — review breaking changes in release notes |
| argo-rollouts | argocd/platform/argo-rollouts.yaml | 2.40.9 | 2.41.0 | patch | |
| argocd-image-updater | argocd/platform/argocd-image-updater.yaml | 1.1.5 | 1.2.4 | minor | |
| mimir-distributed | argocd/observability/mimir-app.yaml | 6.0.6 | 6.1.0 | minor | |
| postgresql (frolf) | argocd/apps/frolf-postgres.yaml | 16.2.4 | 16.7.27 | minor | **Stays on PG16 series.** Verified: latest in the `16.x` chart series is `16.7.27`. The absolute latest chart (`18.x`) ships a different PG major version and is separately blocked — see Breaking Changes. |
| postgresql (resume) | argocd/apps/resume-postgres.yaml | 18.1.13 | 18.7.13 | minor | Both versions are in the `18.x` chart series (same PostgreSQL major version). Safe to apply. |

---

## ⚠️ Breaking Changes / Skipped

### 🔴 frolf-postgres — PostgreSQL major version bump blocked

| Chart | File | Current | Latest Available | Reason |
|-------|------|---------|------------------|--------|
| postgresql (frolf) | argocd/apps/frolf-postgres.yaml | 16.2.4 | 18.7.13 | **SKIPPED — PostgreSQL major version bump detected.** The absolute latest Bitnami postgresql chart (`18.7.13`) is in the `18.x` series, which ships a different PostgreSQL major version than the current `16.x` series (PG16). Applying this update **WILL WIPE THE FROLF DATABASE** — Bitnami's chart triggers `initdb` on PG major version upgrade, destroying all data. frolf-bot already lost all its data this way (per comment in `argocd/apps/frolf-postgres.yaml`). **Requires explicit human review, a pg_dumpall backup, and a planned maintenance window before attempting this migration.** |

The chart was updated within the safe `16.x` series (16.2.4 → 16.7.27) only.

---

### 🔴 Loki 7.0.0 — Enterprise (GEL) only, OSS repo switch required

| Chart | File | Current | Latest | Reason |
|-------|------|---------|--------|--------|
| loki | argocd/observability/loki-app.yaml | 6.55.0 | 7.0.0 | **SKIPPED — Not applicable for OSS users.** Chart `7.0.0` in `grafana/loki` Helm repo is for Grafana Enterprise Logs (GEL) only. OSS users must first switch `repoURL` to `https://grafana-community.github.io/helm-charts` before upgrading. Additionally, SSD (SimpleScalable) mode used by this deployment is deprecated as of chart 6.52 and will be removed in a future community chart version. `6.55.0` is already the last OSS-compatible version in the current repo. **Requires a migration plan (repo switch + SSD mode deprecation handling) before any further upgrades.** |

---

## Already at Latest

| Chart | File | Version |
|-------|------|---------|
| grafana | argocd/observability/grafana-app.yaml | 10.5.15 |
| tempo-distributed | argocd/observability/tempo-app.yaml | 1.61.3 |

---

## Version Check Failed (Chart Repos Unreachable)

The following charts could not be version-checked because their Helm repositories are blocked by the session's egress policy. These charts were left at their current pinned versions.

| Chart | File | Current Version | Blocked Repo |
|-------|------|-----------------|--------------|
| sealed-secrets | argocd/platform/sealed-secrets-controller.yaml | 2.18.5 | `bitnami-labs.github.io` |
| ingress-nginx | argocd/platform/nginx-ingress-app.yaml | 4.15.1 | `kubernetes.github.io` |
| cert-manager | argocd/platform/cert-manager.yaml | v1.16.2 | `charts.jetstack.io` |
| tailscale-operator | argocd/platform/tailscale-operator.yaml | 1.96.5 | `pkgs.tailscale.com` |
| kube-state-metrics | argocd/observability/kube-state-metrics-app.yaml | 7.3.0 | `prometheus-community.github.io` |
| alloy | argocd/observability/alloy-app.yaml | 1.8.1 | `grafana.github.io` (not on GHCR OCI) |
| prometheus-node-exporter | argocd/observability/node-exporter-app.yaml | 4.55.0 | `prometheus-community.github.io` |
| nats | argocd/apps/frolf-nats.yaml | 2.14.0 | `nats-io.github.io` |

**Manual action required:** Run the following on a machine with egress access to check these charts:

```bash
helm repo add bitnami-labs https://bitnami-labs.github.io/sealed-secrets
helm repo add ingress-nginx https://kubernetes.github.io/ingress-nginx
helm repo add jetstack https://charts.jetstack.io
helm repo add tailscale https://pkgs.tailscale.com/helmcharts
helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
helm repo add grafana https://grafana.github.io/helm-charts
helm repo add nats https://nats-io.github.io/k8s/helm/charts/
helm repo update
helm search repo sealed-secrets/sealed-secrets
helm search repo ingress-nginx/ingress-nginx
helm search repo jetstack/cert-manager
helm search repo tailscale/tailscale-operator
helm search repo prometheus-community/kube-state-metrics
helm search repo prometheus-community/prometheus-node-exporter
helm search repo grafana/alloy
helm search repo nats/nats
```

---

## Changelogs

> Note: Direct GitHub release page fetches were blocked by the session's egress policy (`github.com` returns 403). URLs are provided for manual review.

### argo-cd: 9.5.14 → 10.1.3

**Release notes** ([view on GitHub](https://github.com/argoproj/argo-helm/releases/tag/argo-cd-10.1.3)):

This is a **major chart version bump** (9→10). Review the full changelog for breaking changes in values schema, CRDs, and default behaviors. The chart corresponds to an ArgoCD application version update.

Key areas to check before applying:
- CRD changes (may require manual CRD upgrade)
- Values file compatibility (`charts/argo-cd/values.yaml`)
- RBAC and permission changes

---

### argo-rollouts: 2.40.9 → 2.41.0

**Release notes** ([view on GitHub](https://github.com/argoproj/argo-helm/releases/tag/argo-rollouts-2.41.0)):

Patch update. Low risk.

---

### argocd-image-updater: 1.1.5 → 1.2.4

**Release notes** ([view on GitHub](https://github.com/argoproj/argo-helm/releases/tag/argocd-image-updater-1.2.4)):

Minor update. Review for any new configuration options in values schema.

---

### mimir-distributed: 6.0.6 → 6.1.0

**Release notes** ([view on GitHub](https://github.com/grafana/mimir/releases/tag/mimir-distributed-6.1.0)):

Minor update. Review mimir values file for any deprecated configuration.

---

### postgresql (frolf): 16.2.4 → 16.7.27

**Release notes** ([view on GitHub](https://github.com/bitnami/charts/releases/tag/postgresql%2F16.7.27)):

Minor update within the `16.x` (PG16) chart series. PostgreSQL major version unchanged (still PG16). Safe to apply.

**PostgreSQL major version upgrade status:** The latest Bitnami chart overall is `18.7.13` (`18.x` series, ships PG17). Upgrading frolf-postgres from `16.x` to `18.x` would trigger a PostgreSQL major version upgrade (PG16→PG17) which runs `initdb` and **permanently destroys the database**. When ready to migrate:

1. Take a full `pg_dumpall` backup
2. Schedule maintenance window
3. Update `targetRevision` to the desired `18.x` version
4. Monitor init logs closely on first startup

---

### postgresql (resume): 18.1.13 → 18.7.13

**Release notes** ([view on GitHub](https://github.com/bitnami/charts/releases/tag/postgresql%2F18.7.13)):

Minor update within the `18.x` chart series. PostgreSQL major version unchanged (same series). Safe to apply.

---
