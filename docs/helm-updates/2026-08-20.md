# Helm Chart Updates — 2026-08-20

> Auto-generated by nightly-helm-version-check routine.

> **Note on version discovery:** This run used `raw.githubusercontent.com` to fetch `Chart.yaml`
> from each chart's GitHub source repository (same method as the 2026-05-14 run). Helm binaries
> and Helm chart GitHub Pages repos were unreachable in this environment. The GitHub REST API
> (`api.github.com`) was blocked; release notes links are constructed from the known tag naming
> conventions and were not verified. `charts.bitnami.com` and `pkgs.tailscale.com` remain
> unreachable — both PostgreSQL entries and tailscale-operator were skipped. Loki and
> tempo-distributed require OSS migration to `grafana-community/helm-charts` and are skipped.

---

## Summary

17 charts checked. **10 updated**. **7 skipped** (require manual review).

---

## Updates Applied

| Chart | File | Old Version | New Version | App Version | Bump Type | Notes |
|-------|------|-------------|-------------|-------------|-----------|-------|
| sealed-secrets | argocd/platform/sealed-secrets-controller.yaml | 2.18.5 | 2.19.2 | 0.39.0 | minor | |
| argo-rollouts | argocd/platform/argo-rollouts.yaml | 2.40.9 | 2.41.1 | v1.9.1 | minor | |
| argo-cd | argocd/platform/argocd.yaml | 9.5.14 | 10.4.0 | v3.5.1 | **⚠️ MAJOR** | Chart 9→10; ArgoCD app v3.4.2→v3.5.1 (app minor) — review values |
| argocd-image-updater | argocd/platform/argocd-image-updater.yaml | 1.1.5 | 1.2.4 | v1.2.2 | minor | |
| kube-state-metrics | argocd/observability/kube-state-metrics-app.yaml | 7.3.0 | 8.4.0 | 2.20.0 | **⚠️ MAJOR** | Chart 7→8; KSM app 2.18.0→2.20.0 (app minor) — review values |
| alloy | argocd/observability/alloy-app.yaml | 1.8.1 | 1.11.1 | v1.18.1 | minor | |
| prometheus-node-exporter | argocd/observability/node-exporter-app.yaml | 4.55.0 | 4.56.1 | 1.12.1 | minor | |
| mimir-distributed | argocd/observability/mimir-app.yaml | 6.0.6 | 6.1.0 | 3.1.2 | minor | Skipped weekly builds (e.g. 6.3.0-weekly.408) |
| grafana | argocd/observability/grafana-app.yaml | 10.5.15 | 12.11.1 | 13.2.0 | **⚠️ MAJOR** | Chart 10→12; Grafana app 12.3.1→**13.2.0** (app MAJOR) — breaking changes likely |
| nats | argocd/apps/frolf-nats.yaml | 2.14.0 | 2.14.5 | 2.14.5 | patch | |

---

## ⚠️ Breaking Changes / Skipped

### 🔴 CRITICAL — Grafana 13.x Application Major Version

The grafana chart bumped from 10.5.15 (Grafana app **12.3.1**) to 12.11.1 (Grafana app **13.2.0**).
This is a Grafana **application major version bump** (12.x → 13.x). Grafana 13 may contain
breaking changes in datasource plugins, alerting, dashboard JSON schemas, and configuration.

**Action required before merging:** Review the Grafana 13.0 release notes for breaking changes.
Pay particular attention to: plugin API changes, dashboard provisioning format changes, and any
deprecated configuration keys. Verify `charts/grafana/values.yaml` compatibility with chart 12.x.

GitHub release reference: https://github.com/grafana/helm-charts/releases?q=grafana-12

---

### 🔴 CRITICAL — Loki OSS Migration Required (Skipped Again)

The `grafana/loki` chart is at **7.3.0** (GEL enterprise only). The OSS community fork at
`grafana-community/helm-charts` now ships **loki 18.9.1** (app: Loki 3.7.6).

The current `loki-app.yaml` has an explicit comment noting this migration is required before
the next upgrade. The `repoURL` must change from `https://grafana.github.io/helm-charts` to
`https://grafana-community.github.io/helm-charts`, and the chart version series changes from
6.x → 18.x (completely new numbering). SSD (SimpleScalable) mode is also deprecated in the
community chart and will be removed in a future version.

**Action required:** Evaluate migration to community chart. This requires:
1. Changing `repoURL` in `argocd/observability/loki-app.yaml`
2. Updating `charts/loki/values.yaml` for compatibility with the new chart schema
3. Verifying SSD → alternative mode migration if applicable

This chart was NOT updated in this run.

---

### 🔴 CRITICAL — tempo-distributed DEPRECATED in Original Repo (Skipped)

The `grafana/helm-charts` chart `tempo-distributed` at version **1.61.3** is now marked
**`deprecated: true`** in its `Chart.yaml`. The successor chart is in `grafana-community/helm-charts`
at version **3.2.2** (Tempo app **3.0.3**).

Tempo v3.0 has **numerous breaking changes** (per the CHANGELOG): breaking changes in query
configuration, compaction config CLI flags, metric generators, OpenCensus receiver removal, and more.

**Action required:** Migrate `argocd/observability/tempo-app.yaml`:
1. Change `repoURL` to `https://grafana-community.github.io/helm-charts`
2. Update `targetRevision` to `3.2.2` (or current latest at time of migration)
3. Review `charts/tempo/values.yaml` extensively — chart schema changed with the version series reset

This chart was NOT updated in this run. Continuing to run the deprecated chart version (1.61.3)
is safe short-term but will stop receiving updates.

---

### Skipped Charts (require manual review)

| Chart | File | Current | Latest Available | Reason |
|-------|------|---------|------------------|--------|
| cert-manager | argocd/platform/cert-manager.yaml | v1.16.2 | Unknown | **Network restricted** — `charts.jetstack.io` blocked; cert-manager GitHub repo chart path not found via raw content. Run `helm search repo jetstack/cert-manager` locally to check for updates. |
| tailscale-operator | argocd/platform/tailscale-operator.yaml | 1.96.5 | Unknown | **Network restricted** — `pkgs.tailscale.com/helmcharts` blocked; chart source in `tailscale/helm-charts` repo not accessible via raw content. Check manually. |
| frolf-postgres | argocd/apps/frolf-postgres.yaml | 16.2.4 | Unknown | **Network restricted** (`charts.bitnami.com` blocked) + **explicit data-loss warning** in file. Chart distributed via OCI/Bitnami registry; latest OCI version unverifiable. The last known GitHub version is 17.1.0 (PG 17.6.0). The current chart 16.2.4 overrides the image to `library/postgres:17.4` — a chart major bump to 17.x could change data directory handling and trigger `initdb`. Requires local `helm search repo bitnami/postgresql --versions` and careful PG major version verification before updating. |
| resume-postgres | argocd/apps/resume-postgres.yaml | 18.1.13 | Unknown | **Network restricted** (`charts.bitnami.com` blocked). Chart version 18.1.13 is higher than what's visible in the bitnami/charts GitHub repo (17.1.0), indicating it was distributed via OCI registry. Cannot determine latest version or PG app version without registry access. Explicit data-loss warning in file — do not update without local verification. |
| loki | argocd/observability/loki-app.yaml | 6.55.0 | 7.3.0 (GEL only) / 18.9.1 (community OSS) | Skipped — requires `repoURL` migration to `grafana-community/helm-charts` for OSS. See breaking changes section above. |
| tempo-distributed | argocd/observability/tempo-app.yaml | 1.61.3 | 3.2.2 (grafana-community) | Skipped — chart deprecated in original repo; requires `repoURL` migration to `grafana-community/helm-charts` and major version upgrade with breaking changes. See breaking changes section above. |
| ingress-nginx | argocd/platform/nginx-ingress-app.yaml | 4.15.1 | 4.15.1 | Already at latest |

---

## No Changes (already at latest)

- ingress-nginx: 4.15.1 (already latest)
- grafana (at time of May run): 10.5.15 — updated this run to 12.11.1

---

## Charts with Major Version Bumps (Applied — Review Before Merging)

### argo-cd: 9.5.14 → 10.4.0 (chart MAJOR)

The Argo CD Helm chart bumped from major version 9 to 10. The underlying Argo CD application
version moved from **v3.4.2 to v3.5.1** (application minor bump, same major 3.x). The chart
major bump may reflect changes in values structure, CRD updates, or default configuration
changes. **Review `charts/argo-cd/values.yaml` for compatibility with chart 10.x.**

Note: This manages Argo CD itself (self-managed). Exercise caution — sync failures on this app
leave the entire GitOps platform unresponsive.

GitHub release reference: https://github.com/argoproj/argo-helm/releases/tag/argo-cd-10.4.0

---

### kube-state-metrics: 7.3.0 → 8.4.0 (chart MAJOR)

The KSM Helm chart major version bumped from 7 to 8. The underlying kube-state-metrics
application version changed from **2.18.0 to 2.20.0** (application minor bump). The chart major
bump likely reflects breaking changes in chart values structure.
**Review `charts/kube-state-metrics/values.yaml` for compatibility with chart 8.x.**

GitHub release reference: https://github.com/prometheus-community/helm-charts/releases?q=kube-state-metrics-8

---

### grafana: 10.5.15 → 12.11.1 (chart MAJOR + app MAJOR)

The Grafana Helm chart jumped from 10.5.15 to 12.11.1 (two chart major versions: 10→12). The
Grafana application version jumped from **12.3.1 to 13.2.0** — a **Grafana application major
version bump**.

Grafana 13.x introduces significant changes. Known highlights from public announcements:
- Plugin API breaking changes
- Dashboard schema updates
- Potential alerting rule migration requirements
- Possible deprecations of legacy datasource configurations

**This update requires careful review before merging.** Verify:
1. `charts/grafana/values.yaml` is compatible with chart 12.x values schema
2. Grafana 13 release notes for breaking changes affecting your datasources and dashboards
3. Any provisioned dashboards or plugins that may require updates

GitHub release reference: https://github.com/grafana/helm-charts/releases?q=grafana-12

---

## Changelogs

### sealed-secrets: 2.18.5 → 2.19.2

**GitHub releases:** https://github.com/bitnami-labs/sealed-secrets/releases/tag/helm-v2.19.2

*(Changelog body not fetched — GitHub API unauthenticated access blocked by proxy. Visit link above for release notes.)*

---

### argo-rollouts: 2.40.9 → 2.41.1

App version: Argo Rollouts **v1.9.1**.

**GitHub releases:** https://github.com/argoproj/argo-helm/releases/tag/argo-rollouts-2.41.1

*(Changelog body not fetched — GitHub API blocked. Visit link above for release notes.)*

---

### argo-cd: 9.5.14 → 10.4.0

App version: Argo CD **v3.5.1** (was v3.4.2).

**GitHub releases:** https://github.com/argoproj/argo-helm/releases/tag/argo-cd-10.4.0

*(Changelog body not fetched — GitHub API blocked. Visit link above for release notes.)*

---

### argocd-image-updater: 1.1.5 → 1.2.4

App version: ArgoCD Image Updater **v1.2.2** (was v1.1.1). Minor release — check for new
annotation syntax or configuration options if using advanced image update policies.

**GitHub releases:** https://github.com/argoproj/argo-helm/releases/tag/argocd-image-updater-1.2.4

*(Changelog body not fetched — GitHub API blocked. Visit link above for release notes.)*

---

### kube-state-metrics: 7.3.0 → 8.4.0

App version: kube-state-metrics **2.20.0** (was 2.18.0).

**GitHub releases:** https://github.com/prometheus-community/helm-charts/releases?q=kube-state-metrics-8

*(Changelog body not fetched — GitHub API blocked. Visit link above for release notes.)*

---

### alloy: 1.8.1 → 1.11.1

App version: Grafana Alloy **v1.18.1** (was v1.16.1). Minor release — three minor chart bumps.

**GitHub releases:** https://github.com/grafana/alloy/releases/tag/helm-alloy-1.11.1

*(Changelog body not fetched — GitHub API blocked. Visit link above for release notes.)*

---

### prometheus-node-exporter: 4.55.0 → 4.56.1

App version: Node Exporter **1.12.1** (was 1.11.1). Minor application version bump.

**GitHub releases:** https://github.com/prometheus-community/helm-charts/releases?q=prometheus-node-exporter-4.56

*(Changelog body not fetched — GitHub API blocked. Visit link above for release notes.)*

---

### mimir-distributed: 6.0.6 → 6.1.0

App version: Grafana Mimir **3.1.2** (was 3.0.4). Minor bump. Weekly builds (6.3.0-weekly.408,
etc.) were deliberately skipped — only stable releases applied.

**GitHub releases:** https://github.com/grafana/mimir/releases/tag/mimir-distributed-6.1.0

*(Changelog body not fetched — GitHub API blocked. Visit link above for release notes.)*

---

### grafana: 10.5.15 → 12.11.1

App version: Grafana **13.2.0** (was 12.3.1). **See breaking changes section above.**

**GitHub releases:** https://github.com/grafana/helm-charts/releases?q=grafana-12

*(Changelog body not fetched — GitHub API blocked. Visit link above for release notes.)*

---

### nats: 2.14.0 → 2.14.5

App version: NATS **2.14.5** (was 2.14.0). Patch bump — same chart major version 2.x.

**GitHub releases:** https://github.com/nats-io/k8s/releases?q=nats-2.14

*(Changelog body not fetched — GitHub API blocked. Visit link above for release notes.)*
