# Helm Chart Updates — 2026-08-29

> Auto-generated by nightly-helm-version-check routine.

## Summary

17 charts checked. 11 updated. 6 skipped (require manual review).

---

## ⚠️ CRITICAL: Do Not Merge Without Reading This Section

### 1. ArgoCD 9.5.14 → 10.4.1 — MAJOR CHART + MAJOR APP VERSION BUMP

The argo-cd Helm chart v10 packages **ArgoCD v3.x** (upgrading from v2.x). This is a major ArgoCD application version jump. ArgoCD v3.0 introduced significant breaking changes (new CRD versions, changes to Application and AppProject schemas, removal of deprecated APIs). The chart manages ArgoCD itself (self-management), so a failed upgrade could break the entire GitOps control plane.

**Required actions before merging:**
- Review the [ArgoCD v3.0 migration guide](https://argo-cd.readthedocs.io/en/stable/operator-manual/upgrading/2.14-3.0/) and check for breaking changes in values.yaml
- Verify `charts/argo-cd/values.yaml` is compatible with chart 10.x schema
- Have a manual recovery plan ready in case the self-managed upgrade fails

### 2. Loki (SKIPPED) — Enterprise-Only Chart Warning

The `loki-app.yaml` file already contains an in-place note (added previously):
> **chart 7.0.0 in grafana/loki is GEL (enterprise) only. OSS users must switch repoURL to `https://grafana-community.github.io/helm-charts` before upgrading. 6.55.0 is the last OSS-compatible version in this repo.**

Loki was **not updated** and the comment was preserved. Migration to the community chart repo is a prerequisite for any future Loki update.

### 3. PostgreSQL — Both Charts Require Manual Verification

#### frolf-postgres (currently 16.2.4)
The bitnami chart tag `postgresql/16.2.4` was inspected. Its `appVersion` field shows **17.2.0** (PostgreSQL 17.2.0). The in-file comment says this pin is protecting a PG 16 database, but the chart may already be shipping PG 17 — meaning the pin is no longer preventing a PG major version bump. **This is an unexpected finding that needs immediate human verification**: check `kubectl exec -it <frolf-postgres-pod> -- psql -U postgres -c 'SELECT version();'` to confirm the actual running PostgreSQL major version before any chart changes.

#### resume-postgres (currently 18.1.13)
The bitnami/charts GitHub repository does not have a tag for `postgresql/18.1.13`. This chart version exists in the Bitnami Helm registry (`charts.bitnami.com/bitnami`) but its PostgreSQL appVersion could not be determined from the GitHub source. The latest found in the bitnami/charts main branch is `17.1.0` (PG 17.6.0). Unclear whether 18.x.y is a PG 18 series or another numbering scheme. **Do not update until PG version is verified.**

### 4. Grafana (SKIPPED) — Chart Repo Migrated

The Grafana Helm chart was migrated from `grafana/helm-charts` to `grafana-community/helm-charts` as of January 30, 2026. The last version in the original repo is 10.5.15 (currently pinned — no update available in the current repo). The community repo has grafana up to 13.0.1. Upgrading requires changing `repoURL` in `argocd/observability/grafana-app.yaml` from `https://grafana.github.io/helm-charts` to `https://grafana-community.github.io/helm-charts`, and reviewing breaking changes across 3 major versions (10→13). This requires manual migration.

---

## Updates Applied

| Chart | File | Old Version | New Version | Bump Type | Notes |
|-------|------|-------------|-------------|-----------|-------|
| sealed-secrets | argocd/platform/sealed-secrets-controller.yaml | 2.18.5 | 2.19.3 | patch | |
| argo-rollouts | argocd/platform/argo-rollouts.yaml | 2.40.9 | 2.41.1 | patch | App version 1.9.1 |
| argo-cd | argocd/platform/argocd.yaml | 9.5.14 | 10.4.1 | **⚠️ MAJOR** | Packages ArgoCD v3.5.2 (from v2.x). See breaking change note above. |
| cert-manager | argocd/platform/cert-manager.yaml | v1.16.2 | v1.21.1 | minor | v prefix preserved |
| argocd-image-updater | argocd/platform/argocd-image-updater.yaml | 1.1.5 | 1.2.4 | minor | Updater v1.2.2 |
| tailscale-operator | argocd/platform/tailscale-operator.yaml | 1.96.5 | 1.102.3 | minor | Tailscale v1.102.3 |
| kube-state-metrics | argocd/observability/kube-state-metrics-app.yaml | 7.3.0 | 8.4.1 | **⚠️ MAJOR** | ksm v2.20.0; no breaking changes noted |
| alloy | argocd/observability/alloy-app.yaml | 1.8.1 | 1.12.1 | minor | |
| prometheus-node-exporter | argocd/observability/node-exporter-app.yaml | 4.55.0 | 4.56.1 | patch | node_exporter v1.12.1 |
| mimir-distributed | argocd/observability/mimir-app.yaml | 6.0.6 | 6.2.0 | minor | |
| nats | argocd/apps/frolf-nats.yaml | 2.14.0 | 2.14.6 | patch | |

---

## ⚠️ Skipped (Require Manual Review)

| Chart | File | Current | Latest Available | Reason |
|-------|------|---------|------------------|--------|
| loki | argocd/observability/loki-app.yaml | 6.55.0 | 7.3.0 | SKIPPED — chart 7.x in `grafana/helm-charts` is GEL (enterprise) only. OSS users must migrate repoURL to `https://grafana-community.github.io/helm-charts` first. This is documented in the file's own comment. |
| grafana | argocd/observability/grafana-app.yaml | 10.5.15 | 13.0.1 (community repo) | SKIPPED — chart migrated to `grafana-community/helm-charts` after Jan 30, 2026. Current repo frozen at 10.5.15. Requires repoURL change + review of 3 major versions of breaking changes. |
| frolf-postgres | argocd/apps/frolf-postgres.yaml | 16.2.4 | 17.1.0 (PG 17.6.0) | SKIPPED — UNEXPECTED FINDING: bitnami chart 16.2.4 shows appVersion 17.2.0 (PG 17), contradicting the in-file comment that it's protecting a PG 16 database. The "pin" may no longer be protecting anything. Human must verify actual running PG version before any action. See critical section above. |
| resume-postgres | argocd/apps/resume-postgres.yaml | 18.1.13 | Unknown | SKIPPED — chart tag not found in bitnami/charts GitHub repo. Cannot verify PostgreSQL version. Latest confirmed bitnami chart is 17.1.0 (PG 17.6.0); unknown if 18.x represents PG 18 or different versioning. Manual verification required. |

---

## No Changes (Already at Latest)

- **ingress-nginx**: 4.15.1 (already latest)
- **tempo-distributed**: 1.61.3 (already latest; note: chart is deprecated per upstream)

---

## Changelogs

### sealed-secrets: 2.18.5 → 2.19.3

**Release notes** ([view on GitHub](https://github.com/bitnami-labs/sealed-secrets/releases/tag/helm-v2.19.3)):
Patch release tracking upstream sealed-secrets controller updates.

---

### argo-rollouts: 2.40.9 → 2.41.1

**Release notes** ([view on GitHub](https://github.com/argoproj/argo-helm/releases/tag/argo-rollouts-2.41.1)):
Released July 19. Updates argo-rollouts application to version 1.9.1.

---

### argo-cd: 9.5.14 → 10.4.1 ⚠️ MAJOR

**Release notes** ([view on GitHub](https://github.com/argoproj/argo-helm/releases/tag/argo-cd-10.4.1)):
Updates dependency `argoproj/argo-cd` to **v3.5.2**. Chart major version 10 corresponds to the ArgoCD v3.x application series. ArgoCD v3.0 is a major release — review all breaking changes and migration notes before merging this PR.

See: https://argo-cd.readthedocs.io/en/stable/operator-manual/upgrading/

---

### cert-manager: v1.16.2 → v1.21.1

**Release notes** ([view on GitHub](https://github.com/cert-manager/cert-manager/releases/tag/v1.21.1)):
Latest stable cert-manager release. Minor version bump (1.16 → 1.21) covering multiple patch and minor releases. Review cert-manager release notes for any CRD changes that might require migration.

---

### argocd-image-updater: 1.1.5 → 1.2.4

**Release notes** ([view on GitHub](https://github.com/argoproj/argo-helm/releases/tag/argocd-image-updater-1.2.4)):
Released June 26. Updates dependency `argoproj-labs/argocd-image-updater` to v1.2.2.

---

### tailscale-operator: 1.96.5 → 1.102.3

**Release notes**: Check https://tailscale.com/changelog for changes between Tailscale v1.96.5 and v1.102.3. Chart version tracks the Tailscale application version.

---

### kube-state-metrics: 7.3.0 → 8.4.1 ⚠️ MAJOR

**Release notes** ([view on GitHub](https://github.com/prometheus-community/helm-charts/releases?q=kube-state-metrics-8)):

Key changes in the 8.x series:
- 8.4.1: Added RBAC for admission policy collectors
- 8.4.0: Updated to kube-state-metrics v2.20.0
- 8.3.1: Fixed autosharding configuration service
- 8.3.0: Added ability to disable cluster-wide CRD RBAC
- 8.2.0: Added option for aggregationRule

No breaking changes explicitly mentioned in release notes. Verify `charts/kube-state-metrics/values.yaml` is still compatible with the 8.x values schema before merging.

---

### alloy: 1.8.1 → 1.12.1

**Release notes** ([view on GitHub](https://github.com/grafana/helm-charts/releases?q=alloy)):
Released August 27. Multiple minor updates across 1.9.x, 1.10.x, 1.11.x, 1.12.x. Check release notes for any changes to the Alloy configuration format or component APIs.

---

### prometheus-node-exporter: 4.55.0 → 4.56.1

**Release notes** ([view on GitHub](https://github.com/prometheus-community/helm-charts/releases?q=prometheus-node-exporter)):
Released July 14. Updates node_exporter to v1.12.1. Patch release.

---

### mimir-distributed: 6.0.6 → 6.2.0

**Release notes** ([view on GitHub](https://github.com/grafana/helm-charts/releases?q=mimir-distributed)):
Released August 20. Minor release; no breaking changes noted. Verify `charts/mimir/values.yaml` compatibility with 6.2.x.

---

### nats: 2.14.0 → 2.14.6

**Release notes** ([view on GitHub](https://github.com/nats-io/k8s/releases/tag/nats-2.14.6)):
Released August 28. Patch release for NATS helm chart. No breaking changes expected.

---

## Action Items for Human Review

1. **URGENT — Verify frolf-postgres PG version**: Run `kubectl exec -it <frolf-postgres-pod> -n frolf-bot -- psql -U postgres -c 'SELECT version();'`. If it shows PG 17, the current "pin" at 16.2.4 is illusory. Update the comment in `argocd/apps/frolf-postgres.yaml` and decide whether to upgrade to 17.1.0.

2. **URGENT — ArgoCD 10.x migration**: Review the ArgoCD v3.x upgrade guide before merging. The control plane manages itself — a failed upgrade is high risk.

3. **Loki migration**: Plan migration of `loki-app.yaml` from `grafana/helm-charts` to `grafana-community/helm-charts` to unlock future OSS upgrades.

4. **Grafana migration**: Plan migration of `grafana-app.yaml` from `grafana/helm-charts` to `grafana-community/helm-charts` (last version in current repo: 10.5.15; community repo at 13.0.1).

5. **resume-postgres**: Verify what PostgreSQL version chart 18.1.13 ships (check running pod or bitnami Helm registry metadata). If PG version is safe, find the latest 18.x chart and consider updating.
