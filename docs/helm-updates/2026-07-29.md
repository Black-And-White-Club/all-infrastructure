# Helm Chart Updates — 2026-07-29

> Auto-generated by nightly-helm-version-check routine.
>
> **Note:** This run could not install Helm 3 or access chart repository index files directly
> (egress policy blocked get.helm.sh and all *.github.io / chart CDN hosts). Version data was
> gathered via ArtifactHub web search results (as of 2026-07-29). Versions should be treated as
> **best-effort estimates** and verified against `helm search repo` before merging.

## Summary

17 charts checked. 8 updated. 9 skipped (5 already at latest / no stable upgrade, 4 require manual review).

## Updates Applied

| Chart | File | Old Version | New Version | Bump Type | Notes |
|-------|------|-------------|-------------|-----------|-------|
| sealed-secrets | argocd/platform/sealed-secrets-controller.yaml | 2.18.5 | 2.18.6 | patch | |
| argo-cd | argocd/platform/argocd.yaml | 9.5.14 | 9.5.22 | patch | Stayed on 9.5.x; chart 10.x has breaking changes — see breaking changes section |
| argo-rollouts | argocd/platform/argo-rollouts.yaml | 2.40.9 | 2.41.1 | minor | |
| argocd-image-updater | argocd/platform/argocd-image-updater.yaml | 1.1.5 | 1.2.4 | minor | |
| tailscale-operator | argocd/platform/tailscale-operator.yaml | 1.96.5 | 1.98.9 | minor | |
| alloy | argocd/observability/alloy-app.yaml | 1.8.1 | 1.11.0 | minor | 3 minor bumps within 1.x — review changelog |
| prometheus-node-exporter | argocd/observability/node-exporter-app.yaml | 4.55.0 | 4.56.1 | patch | |
| nats | argocd/apps/frolf-nats.yaml | 2.14.0 | 2.14.2 | patch | |

## ⚠️ Breaking Changes / Skipped (require manual review)

| Chart | File | Current | Latest Available | Reason |
|-------|------|---------|------------------|--------|
| **postgresql (frolf)** | argocd/apps/frolf-postgres.yaml | 16.2.4 | ~16.7.27 (est.) | **SKIPPED — PostgreSQL pinned. Current chart 16.x ships PG 17.x. Latest 16.x series also ships PG 17. Cannot confirm without `helm search repo --versions`. Requires explicit human verification before bumping. No automated update applied.** |
| **postgresql (resume)** | argocd/apps/resume-postgres.yaml | 18.1.13 | ~18.3.0 (est.) | **SKIPPED — PostgreSQL pinned. Chart 18.x ships PG 17.x. Latest 18.x appears to also ship PG 17. Requires explicit human verification. No automated update applied.** |
| **loki** | argocd/observability/loki-app.yaml | 6.55.0 | 7.1.0 (grafana/grafana repo) | **SKIPPED — MIGRATION REQUIRED. Chart 7.x in `grafana/grafana` is Grafana Enterprise Logs (GEL) only. OSS users must switch repoURL to `https://grafana-community.github.io/helm-charts` before upgrading. The community edition is now at ~18.6.0. Repo is currently at 6.55.0, which is the last OSS-compatible version in the old repo. SSD (SimpleScalable) mode also deprecated. This is a two-step migration: (1) switch repoURL, (2) bump chart version. Requires manual review and potentially values changes.** |
| **kube-state-metrics** | argocd/observability/kube-state-metrics-app.yaml | 7.3.0 | 8.0.0 | **SKIPPED — MAJOR chart version bump. Breaking change: CiliumNetworkPolicy support dropped. If cluster does not use Cilium CNI this may be a no-op, but confirm before upgrading. Review full 8.0.0 changelog.** |
| **argo-cd (10.x)** | argocd/platform/argocd.yaml | 9.5.22 (after today's update) | 10.2.1 | **FLAGGED — Not applied. Chart 10.0.0 breaking changes: (1) `server.additionalApplications` and `server.additionalProjects` moved to separate `argocd-apps` chart; (2) `config.params` default values removed. Current values.yaml does NOT use additionalApplications/additionalProjects — that risk is low. But a full 10.x changelog review is recommended before upgrading.** |
| **cert-manager** | argocd/platform/cert-manager.yaml | v1.16.2 | v1.21.0 | **SKIPPED — Large jump (5 minor versions). Notable breaking changes in 1.21.0: (1) `prometheus.servicemonitor.targetPort`, `prometheus.servicemonitor.path`, and `prometheus.podmonitor.path` values removed (not in current values.yaml — low risk); (2) RBAC change for serviceaccounts/token (only affects undocumented patterns); (3) cert-manager 1.20+ requires Kubernetes >= 1.31 — cluster version must be verified before upgrading.** |
| **mimir-distributed** | argocd/observability/mimir-app.yaml | 6.0.6 | 6.2.0-weekly.402 | **SKIPPED — No stable release beyond 6.0.x found. All releases in the 6.1 and 6.2 series are weekly/development builds that require `helm install --devel`. 6.0.6 appears to be the current stable. Will re-check when 6.1.0 stable releases.** |

## Already at Latest

- **ingress-nginx**: 4.15.1 (already latest)
- **tempo-distributed**: 1.61.3 (already latest; note: migration to grafana-community/helm-charts underway for this chart too — monitor for future migration requirement)
- **grafana**: 10.5.15 (already latest)
- **mimir-distributed**: 6.0.6 (latest stable — see breaking changes section)

## Changelogs

### sealed-secrets: 2.18.5 → 2.18.6

**Release notes** ([view on GitHub](https://github.com/bitnami-labs/sealed-secrets/releases/tag/helm-v2.18.6)):

Patch release. No breaking changes expected. Check the GitHub releases page for the specific changelog.

---

### argo-cd: 9.5.14 → 9.5.22

**Release notes** ([view on GitHub](https://github.com/argoproj/argo-helm/releases)):

Patch releases within the 9.5.x series. No breaking changes expected. Multiple patch releases (9.5.15 through 9.5.22) accumulated since last update.

⚠️ **Chart 10.x is available (10.2.1)** but was deliberately skipped due to breaking changes. See the breaking changes section above.

---

### argo-rollouts: 2.40.9 → 2.41.1

**Release notes** ([view on GitHub](https://github.com/argoproj/argo-helm/releases)):

Minor release. Bumps Argo Rollouts application version. Check the argoproj/argo-helm releases page for specific changelog entries tagged `argo-rollouts-2.41.x`.

---

### argocd-image-updater: 1.1.5 → 1.2.4

**Release notes** ([view on GitHub](https://github.com/argoproj/argo-helm/releases)):

Minor release. App version bumped to v1.2.2. Check the argoproj/argo-helm releases page for entries tagged `argocd-image-updater-1.2.x`.

---

### tailscale-operator: 1.96.5 → 1.98.9

**Release notes** ([view on GitHub](https://github.com/tailscale/tailscale/releases)):

Minor release tracking Tailscale 1.98.x. Bumps operator image and CRDs. Check the tailscale/tailscale releases page.

---

### alloy: 1.8.1 → 1.11.0

**Release notes** ([view on GitHub](https://github.com/grafana/helm-charts/releases)):

Three minor version bumps within the 1.x series (1.8 → 1.9 → 1.10 → 1.11). Grafana Alloy minor releases typically track new Alloy application versions. Review the grafana/helm-charts releases tagged `helm-alloy-1.9.x`, `helm-alloy-1.10.x`, and `helm-alloy-1.11.x` for accumulated changelog.

---

### prometheus-node-exporter: 4.55.0 → 4.56.1

**Release notes** ([view on GitHub](https://github.com/prometheus-community/helm-charts/releases)):

Patch release. No breaking changes expected. Check prometheus-community/helm-charts releases tagged `prometheus-node-exporter-4.56.x`.

---

### nats: 2.14.0 → 2.14.2

**Release notes** ([view on GitHub](https://github.com/nats-io/k8s/releases)):

Patch release. No breaking changes expected. Check nats-io/k8s releases tagged `nats-2.14.x`.

---

## Notes on Version Data Quality

This run was network-restricted: the egress policy blocked direct access to all Helm chart repository index files and the Helm binary download host. Versions were determined via ArtifactHub search results. This approach is less precise than `helm search repo` and may occasionally return stale data. Before merging this branch:

1. Run `helm repo update && helm search repo <repo>/<chart>` for each updated chart to confirm the version exists.
2. Pay special attention to the 4 manually-reviewed items above.
3. Confirm the PostgreSQL app_version for both postgres instances before updating those charts.
