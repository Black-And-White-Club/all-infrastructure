# Helm Chart Updates — 2026-08-02

> Auto-generated by nightly-helm-version-check routine.

> **Note on version discovery:** This run fetched chart index files directly from GitHub raw
> content (`raw.githubusercontent.com`), which is accessible from the CI environment. Helm was not
> installable (get.helm.sh blocked by network policy), and the GitHub API for external repos is
> scoped to this session's repo only. The `charts.bitnami.com`, `charts.jetstack.io`, and
> `pkgs.tailscale.com` chart repos remain unreachable — those three charts are skipped again
> this run.

---

## Summary

17 charts checked. 9 updated. 5 skipped (require manual review). 3 already at latest.

---

## Updates Applied

| Chart | File | Old Version | New Version | App Version | Bump Type | Notes |
|-------|------|-------------|-------------|-------------|-----------|-------|
| sealed-secrets | argocd/platform/sealed-secrets-controller.yaml | 2.18.5 | 2.19.1 | 0.38.4 | minor | |
| argo-rollouts | argocd/platform/argo-rollouts.yaml | 2.40.9 | 2.41.1 | v1.9.1 | minor | |
| argo-cd | argocd/platform/argocd.yaml | 9.5.14 | 10.2.2 | v3.4.6 | **⚠️ MAJOR** | Helm chart major 9→10; ArgoCD app stays v3.x — review values before merge |
| argocd-image-updater | argocd/platform/argocd-image-updater.yaml | 1.1.5 | 1.2.4 | v1.2.2 | minor | **See behavior change note below** |
| alloy | argocd/observability/alloy-app.yaml | 1.8.1 | 1.11.0 | v1.18.0 | minor | **See otelcol breaking change note below** |
| kube-state-metrics | argocd/observability/kube-state-metrics-app.yaml | 7.3.0 | 8.0.0 | 2.19.1 | **⚠️ MAJOR** | Helm chart major 7→8; contains security fixes — review values before merge |
| prometheus-node-exporter | argocd/observability/node-exporter-app.yaml | 4.55.0 | 4.56.1 | 1.12.1 | patch | |
| mimir-distributed | argocd/observability/mimir-app.yaml | 6.0.6 | 6.1.0 | 3.1.2 | minor | **See mimir change notes below** |
| nats | argocd/apps/frolf-nats.yaml | 2.14.0 | 2.14.2 | 2.14.2 | patch | |

---

## ⚠️ Breaking Changes / Skipped

### 🔴 CRITICAL — Loki: DO NOT upgrade to 7.x (enterprise-only)

**Status: SKIPPED again.** The previous run (2026-05-14) documented this. Loki chart 7.0.0+ is
exclusively for Grafana Enterprise Logs (GEL) users. The community OSS fork is at
`grafana-community/helm-charts` (pinned at 6.55.0). Current pinned version 6.55.0 is the last
OSS-compatible release.

**Action required (still outstanding from May):**  
Choose between: (1) stay on 6.55.0 pinned forever, or (2) migrate `loki-app.yaml` to point to
`https://grafana-community.github.io/helm-charts` to continue receiving OSS updates.

---

### ⚠️ Charts skipped due to network restrictions

| Chart | File | Current | Reason |
|-------|------|---------|--------|
| cert-manager | argocd/platform/cert-manager.yaml | v1.16.2 | `charts.jetstack.io` blocked by network policy; GitHub API for cert-manager/cert-manager releases also out of scope. Check `helm search repo jetstack/cert-manager` locally. A v1.17.x branch exists in the source repo, so an update is likely available. |
| tailscale-operator | argocd/platform/tailscale-operator.yaml | 1.96.5 | `pkgs.tailscale.com/helmcharts` blocked. Check manually with `helm search repo tailscale/tailscale-operator`. |
| frolf-postgres | argocd/apps/frolf-postgres.yaml | 16.2.4 | `charts.bitnami.com` blocked. GitHub source shows chart 17.1.0 / PG 17.6.0 in main (same PG major = safe from data perspective), but the actual latest published version cannot be confirmed. Explicit do-not-auto-update note is in the file. Check locally with `helm search repo bitnami/postgresql --versions`. |
| resume-postgres | argocd/apps/resume-postgres.yaml | 18.1.13 | Same bitnami block. Additionally, the deployed version (18.1.13) is numerically higher than the GitHub source main branch (17.1.0), suggesting bitnami may have released further versions not reflected in the source tree. Check locally. |

---

## ⚠️ Updates Applied — Review Before Merging

### argo-cd: 9.5.14 → 10.2.2 (Helm chart MAJOR 9→10)

The ArgoCD Helm chart jumped a major version (9→10). The underlying ArgoCD application version
moved from v3.4.2 to v3.4.6 (minor/patch, same ArgoCD 3.x major). The chart major bump likely
reflects changes to Helm values structure. **Before merging, diff your `charts/argo-cd/values.yaml`
against the upstream defaults for chart 10.x and check for renamed or removed values keys.**

Release reference: https://github.com/argoproj/argo-helm/releases?q=argo-cd-10

---

### kube-state-metrics: 7.3.0 → 8.0.0 (Helm chart MAJOR 7→8)

The KSM Helm chart major version bumped 7→8. The app version moved from 2.18.0 to 2.19.1.

**Why this update is urgent:** KSM 2.19.x includes two important security fixes:
- **GHSA-g3c8-4qh2-rhrg** (v2.19.0): Authentication bypass on `/debug/pprof/*` endpoints — these
  were not gated by `--auth-filter`. Endpoints moved to the telemetry server and are now correctly
  gated.
- **CVE-2026-42504** (v2.19.1): Go toolchain security fix.

Recommend merging this branch promptly for the security fixes. Check `charts/kube-state-metrics/values.yaml`
for compatibility with chart 8.x before syncing.

---

### argocd-image-updater: 1.1.5 → 1.2.4 (minor)

The app version moves from v1.1.1 to v1.2.2. The upstream app CHANGELOG contains a future breaking
change (`applications_api` default switching from ArgoCD API to K8s API) that is listed under
"Unreleased" — it may or may not be included in the 1.2.x release. **Before syncing, verify whether
`charts/argocd-image-updater/values.yaml` requires `config.applicationsAPIKind: argocd` to preserve
the current behavior, and confirm ArgoCD Image Updater is still running in the `argocd` namespace.**

---

### alloy: 1.8.1 → 1.11.0 (minor, but note otelcol changes)

Grafana Alloy v1.18.0 introduced default HTTP server timeouts for several otelcol receiver
components (`otelcol.receiver.otlp`, `otelcol.receiver.faro`, `otelcol.receiver.jaeger`, etc.):
- `idle_timeout` now defaults to `"1m"` (was `"0s"`)
- `read_header_timeout` now defaults to `"1m"` (was `"0s"`)
- `write_timeout` now defaults to `"30s"` (was `"0s"`)

If any Alloy pipeline config uses these receivers, review whether the new timeouts are acceptable
or need explicit overrides.

---

### mimir-distributed: 6.0.6 → 6.1.0 (minor, but with notable changes)

- **`kafka.extraEnv` removed** — replaced by `kafka.env`. If Kafka integration is used and
  `kafka.extraEnv` is set in `charts/mimir/values.yaml`, rename it to `kafka.env` before syncing.
- Minimum supported Kubernetes version bumped to **1.32**.
- Default registry changed to `docker.io` for the mimir image.
- Helm chart upgraded to Helm v4 internally.

---

## No Changes (already at latest)

- ingress-nginx: 4.15.1 (already latest)
- tempo-distributed: 1.61.3 (already latest)
- grafana: 10.5.15 (already latest)

---

## Changelogs

### sealed-secrets: 2.18.5 → 2.19.1

App version: `0.36.6` → `0.38.4`. Minor release — no breaking changes found in chart notes.

GitHub releases: https://github.com/bitnami-labs/sealed-secrets/releases?q=helm-v2.19

---

### argo-rollouts: 2.40.9 → 2.41.1

App version: `v1.9.0` → `v1.9.1`. Patch release; v1.10.0-rc1 is the next major but not yet stable.
Notable CVE addressed in argo-rollouts: CVE-2026-35469.

GitHub releases: https://github.com/argoproj/argo-rollouts/releases/tag/v1.9.1

---

### argo-cd: 9.5.14 → 10.2.2

App version: `v3.4.2` → `v3.4.6`. The latest 10.x change notes (from Chart.yaml annotations):
`Bump argo-cd to v3.4.6`. The chart major version bump is the bigger concern — review upstream
values diffs for chart 10.x.

GitHub releases: https://github.com/argoproj/argo-helm/releases?q=argo-cd-10

---

### argocd-image-updater: 1.1.5 → 1.2.4

App version: `v1.1.1` → `v1.2.2`.

GitHub releases: https://github.com/argoproj/argo-helm/releases?q=argocd-image-updater-1.2

---

### alloy: 1.8.1 → 1.11.0

App version: `v1.16.1` → `v1.18.0`.

**1.11.0 (2026-07-20):** Add `alloy.command` to override the entrypoint command. Update to Grafana Alloy v1.18.0.

**1.10.1 (2026-06-29):** Update to Grafana Alloy v1.17.1.

**1.10.0 (2026-06-12):** Allow configuring alloy service externalTrafficPolicy. Update to Grafana Alloy v1.17.0.

**1.9.0 (2026-06-08):** Add `controller.autoscaling.horizontal.externalHPA` for KEDA ScaledObject support. Update to Grafana Alloy v1.16.3. Bug fix: fix `configmap.yaml` ignoring `alloy.configMap.key`.

**1.8.2 (2026-05-25):** Update config-reloader to v0.91.0.

GitHub releases: https://github.com/grafana/alloy/releases

---

### kube-state-metrics: 7.3.0 → 8.0.0

App version: `2.18.0` → `2.19.1`.

**v2.19.1 (2026-06-10):** Go toolchain security fix (CVE-2026-42504 / GO-2026-5038). Fix watch-list bookmarks with sharding. Fix panic on CronJobs with unparseable schedules.

**v2.19.0 (2026-05-05):** Security fix GHSA-g3c8-4qh2-rhrg — `/debug/pprof/*` endpoints were not protected by `--auth-filter`; moved to telemetry server.

GitHub releases: https://github.com/kubernetes/kube-state-metrics/releases

---

### prometheus-node-exporter: 4.55.0 → 4.56.1

App version: `1.11.1` → `1.12.1`.

**1.12.1 (2026-07-14):** Bugfixes for perf profiler flag handling and EDAC optional metric collection.

**1.12.0 (2026-07-10):** New collectors: `mountstats` NFS info metrics, `nvmesubsystem`, `dmmultipath`. Infiniband device filtering. CPU frequency `cpuinfo_avg_freq` support. Various bugfixes.

GitHub releases: https://github.com/prometheus/node_exporter/releases

---

### mimir-distributed: 6.0.6 → 6.1.0

App version: `3.0.4` → `3.1.2`.

**Notable changes:**
- Upgrade Mimir to 3.1.2
- **BREAKING:** `kafka.extraEnv` removed — use `kafka.env` instead
- **BREAKING:** Minimum Kubernetes version now 1.32
- Set docker.io as default registry for mimir image
- Increase default query-frontend cache size to 25MB and memory limit to 4GiB
- Upgrade rollout-operator chart to 0.38.0 (check rollout-operator upgrade notes)
- Upgrade to Helm v4

GitHub releases: https://github.com/grafana/mimir/releases?q=mimir-distributed

---

### nats: 2.14.0 → 2.14.2

App version: `2.14.0` → `2.14.2`. Patch update — NATS server patch release.

GitHub releases: https://github.com/nats-io/nats-server/releases
