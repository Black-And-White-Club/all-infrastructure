# Helm Chart Updates — 2026-06-03

> Auto-generated by nightly-helm-version-check routine.

## Summary

17 charts checked. 8 updated. 3 skipped (require manual review).

## Updates Applied

| Chart | File | Old Version | New Version | Bump Type | Notes |
|-------|------|-------------|-------------|-----------|-------|
| sealed-secrets | argocd/platform/sealed-secrets-controller.yaml | 2.18.5 | 2.18.6 | patch | appVersion 0.37.0 |
| argo-rollouts | argocd/platform/argo-rollouts.yaml | 2.40.9 | 2.40.10 | patch | appVersion v1.9.0 |
| argo-cd | argocd/platform/argocd.yaml | 9.5.14 | 9.5.17 | patch | appVersion bumped to v3.4.3 |
| cert-manager | argocd/platform/cert-manager.yaml | v1.16.2 | v1.17.4 | minor | v prefix preserved; appVersion v1.17.4 |
| argocd-image-updater | argocd/platform/argocd-image-updater.yaml | 1.1.5 | 1.2.2 | minor | appVersion v1.1.x → v1.2.1 |
| tailscale-operator | argocd/platform/tailscale-operator.yaml | 1.96.5 | 1.98.5 | minor | Tailscale v1.96.5 → v1.98.5 |
| kube-state-metrics | argocd/observability/kube-state-metrics-app.yaml | 7.3.0 | 7.4.0 | minor | appVersion 2.19.0 |
| alloy | argocd/observability/alloy-app.yaml | 1.8.1 | 1.8.2 | patch | Updates config-reloader to v0.91.0 |

## ⚠️ Breaking Changes / Skipped

### 1. Loki — BLOCKED: Enterprise-Only Chart / Requires Migration

| Chart | File | Current | Latest Available | Reason |
|-------|------|---------|------------------|--------|
| loki | argocd/observability/loki-app.yaml | 6.55.0 | 7.0.0 (grafana/helm-charts) | **BLOCKED — see details below** |

**Explanation:**
The existing comment in `loki-app.yaml` captures this exactly:

> Chart 7.0.0 in `grafana/loki` is GEL (enterprise) only. OSS users must switch `repoURL` to
> `https://grafana-community.github.io/helm-charts` before upgrading. 6.55.0 is the last
> OSS-compatible version in this repo. SSD (SimpleScalable) mode is also deprecated as of
> chart 6.52 and will be removed in a future community chart version.

The community chart replacement is at **`grafana-community/helm-charts`** — chart version **17.1.6**
(appVersion 3.7.2). This is **not** a simple targetRevision bump: it requires:
1. Changing `repoURL` from `https://grafana.github.io/helm-charts` to `https://grafana-community.github.io/helm-charts`
2. Updating `targetRevision` to `17.1.6` (the community chart uses a completely different version numbering scheme)
3. Reviewing values for SSD mode deprecation — the community chart may remove the `singleBinary`/`read`/`write` mode configuration in a future version
4. Testing that existing persistent storage mounts are compatible with the new chart

This requires a scheduled migration with a tested rollout plan. See the comment in `loki-app.yaml` for the full context.

---

### 2. frolf-postgres — PINNED: PG Version Analysis Required

| Chart | File | Current | Latest Available | Reason |
|-------|------|---------|------------------|--------|
| postgresql (frolf) | argocd/apps/frolf-postgres.yaml | 16.2.4 | 17.1.0 | SKIPPED — explicitly PINNED; see analysis below |

**Explanation:**
`frolf-postgres.yaml` contains an explicit `PINNED` comment blocking auto-updates. The chart's
comment references the frolf-bot data loss incident (PR #10, PG major version bump via chart
upgrade wipping `initdb`).

**Version analysis performed during this run:**

The task description assumed chart 16.2.4 = PG ~16.x. However, inspection of the chart at tag
`refs/tags/postgresql/16.2.4` reveals `appVersion: 17.2.0` — frolf-postgres is **already running
PostgreSQL 17**, not PG 16. The latest bitnami/postgresql chart (17.1.0) also ships PG 17.6.0.

From a PostgreSQL major version standpoint, upgrading from 16.2.4 (PG 17.2.0) to 17.1.0
(PG 17.6.0) would be a **PG 17 → PG 17 upgrade** (minor version bump only, no `initdb` risk).

However, this chart carries an explicit `PINNED` comment that requires intentional human review
before any version change. The bitnami chart **major version** changes from 16.x to 17.x, which
may include other breaking changes beyond the PG version. Do not apply this update without:
1. Verifying the PG version continues to be 17.x (confirmed above: it is)
2. Reviewing bitnami/postgresql 16.x → 17.x chart changelog for other breaking changes
3. Ensuring a backup exists and the PVC reclaim policy is set to `Retain`

Bitnami changelog: https://github.com/bitnami/charts/blob/main/bitnami/postgresql/CHANGELOG.md

---

### 3. resume-postgres — CANNOT DETERMINE LATEST

| Chart | File | Current | Latest Discoverable | Reason |
|-------|------|---------|---------------------|--------|
| postgresql (resume) | argocd/apps/resume-postgres.yaml | 18.1.13 | 17.1.0 (GitHub main) | SKIPPED — current version is ahead of discoverable state |

**Explanation:**
The bitnami/charts GitHub `main` branch shows `version: 17.1.0` as the latest bitnami/postgresql
chart, with its CHANGELOG reflecting entries only up to `17.1.0 (2026-02-25)`. However,
`resume-postgres.yaml` is pinned at chart `18.1.13`, which is a higher version number than what
the main branch reflects.

This suggests either:
- Bitnami has released 18.x charts (likely for PostgreSQL 18, released ~Sep 2025) that are
  published to `charts.bitnami.com` but not yet reflected in the GitHub `main` branch
- Or the main branch lags behind the Helm repo

The `charts.bitnami.com` Helm repository was inaccessible from this environment (403). Without
being able to query the actual Helm repository index, the latest available version cannot be
determined. Resume-postgres at 18.1.13 is likely already at or near the latest 18.x chart. No
update was applied. Manually check `helm search repo bitnami/postgresql --versions` for the full
version list.

## No Changes (already at latest)

- **ingress-nginx**: 4.15.1 (already latest; appVersion 1.15.1)
- **prometheus-node-exporter**: 4.55.0 (already latest; appVersion 1.11.1)
- **tempo-distributed**: 1.61.3 (already latest; appVersion 2.9.0) — marked deprecated in grafana/helm-charts; consider migrating to the standalone tempo chart
- **mimir-distributed**: 6.0.6 (already latest **stable**; only `6.1.0-weekly.*` pre-releases exist beyond this)
- **grafana**: 10.5.15 (already latest; appVersion 12.3.1)
- **nats**: 2.14.0 (already latest; appVersion 2.14.0)

## Changelogs

### sealed-secrets: 2.18.5 → 2.18.6

**Release notes** ([view on GitHub](https://github.com/bitnami-labs/sealed-secrets/releases/tag/helm-v2.18.6)):

Patch release. Minor chart maintenance update. appVersion remains 0.37.0.

---

### argo-rollouts: 2.40.9 → 2.40.10

**Release notes** ([view on GitHub](https://github.com/argoproj/argo-helm/releases/tag/argo-rollouts-2.40.10)):

Patch release tracking Argo Rollouts v1.9.0. Bug fixes and stability improvements.

---

### argo-cd: 9.5.14 → 9.5.17

**Release notes** ([view on GitHub](https://github.com/argoproj/argo-helm/releases?q=argo-cd-9.5)):

Three patch releases:
- **9.5.15**: bumped appVersion to v3.4.2
- **9.5.16**: bumped appVersion to v3.4.3
- **9.5.17**: no appVersion change (v3.4.3); chart maintenance

---

### cert-manager: v1.16.2 → v1.17.4

**Release notes** ([view on GitHub](https://github.com/cert-manager/cert-manager/releases/tag/v1.17.4)):

Minor release series (v1.17.x). Confirmed latest available tag. Includes bug fixes and
Kubernetes compatibility improvements. The `v` prefix in `targetRevision` has been preserved.

---

### argocd-image-updater: 1.1.5 → 1.2.2

**Release notes** ([view on GitHub](https://github.com/argoproj/argo-helm/releases/tag/argocd-image-updater-1.2.2)):

Minor release. appVersion progression: v1.1.x → v1.2.0 (1.2.0/1.2.1) → v1.2.1 (1.2.2).

---

### tailscale-operator: 1.96.5 → 1.98.5

**Release notes** ([view on GitHub](https://github.com/tailscale/tailscale/releases/tag/v1.98.5)):

Minor release. Tailscale follows an even/odd stable/unstable versioning scheme; v1.98.x is a
stable series. Latest confirmed patch: v1.98.5. Includes networking and operator improvements.

---

### kube-state-metrics: 7.3.0 → 7.4.0

**Release notes** ([view on GitHub](https://github.com/prometheus-community/helm-charts/releases/tag/kube-state-metrics-7.4.0)):

Minor chart release. appVersion 2.19.0. New metrics and label improvements.

---

### alloy: 1.8.1 → 1.8.2

**Release notes** ([view on GitHub](https://github.com/grafana/helm-charts/releases/tag/alloy-1.8.2)):

Patch release (2026-05-25). Updates config-reloader default version to v0.91.0. Previous 1.8.1
updated Grafana Alloy to v1.16.1.

---

### loki: 6.55.0 → ⛔ BLOCKED (see Breaking Changes above)

Migration to `grafana-community/helm-charts` loki **17.1.6** required. Cannot auto-apply.
See breaking changes section for full migration notes.
