# Helm Chart Updates — 2026-06-15

> Auto-generated by nightly-helm-version-check routine.

## Summary

17 charts checked. 7 updated. 3 already at latest. 7 skipped (require manual review).

> **Note on version discovery:** The Helm chart repositories (charts.bitnami.com,
> grafana.github.io, etc.) were network-blocked in this environment. Chart versions
> were sourced from `Chart.yaml` files on the `main` branch of each upstream GitHub
> repository via `raw.githubusercontent.com`. For charts whose repos use release-time
> version injection (cert-manager, tailscale-operator) or pre-release main branches
> (mimir, bitnami/postgresql), the latest stable version could not be determined —
> those charts are skipped and flagged below.

---

## Updates Applied

| Chart | File | Old Version | New Version | App Version | Bump Type | Notes |
|-------|------|-------------|-------------|-------------|-----------|-------|
| sealed-secrets | argocd/platform/sealed-secrets-controller.yaml | 2.18.5 | 2.18.6 | 0.37.0 | patch | |
| argo-rollouts | argocd/platform/argo-rollouts.yaml | 2.40.9 | 2.41.0 | v1.9.0 | minor | |
| argo-cd | argocd/platform/argocd.yaml | 9.5.14 | 9.5.21 | v3.4.3 | patch | |
| argocd-image-updater | argocd/platform/argocd-image-updater.yaml | 1.1.5 | 1.2.2 | v1.2.1 | minor | |
| alloy | argocd/observability/alloy-app.yaml | 1.8.1 | 1.10.0 | v1.17.0 | minor | |
| kube-state-metrics | argocd/observability/kube-state-metrics-app.yaml | 7.3.0 | 7.5.1 | 2.19.1 | minor | |
| nats | argocd/apps/frolf-nats.yaml | 2.14.0 | 2.14.2 | 2.14.2 | patch | |

---

## ⚠️ Breaking Changes / Skipped

### 🔴 LOKI — Migration Required Before Any Upgrade

| Chart | File | Current | Latest Available | Reason |
|-------|------|---------|------------------|--------|
| loki | argocd/observability/loki-app.yaml | 6.55.0 | 7.1.0 (GEL only) | See details below |

**DO NOT apply this upgrade without completing the migration steps documented in the file.**

The loki-app.yaml already contains this warning comment:

> `chart 7.0.0 in grafana/loki is GEL (enterprise) only. OSS users must switch repoURL to`
> `https://grafana-community.github.io/helm-charts before upgrading. 6.55.0 is the last`
> `OSS-compatible version in this repo. SSD (SimpleScalable) mode is also deprecated as of`
> `chart 6.52 and will be removed in a future community chart version.`

**Required migration steps:**
1. Switch `repoURL` from `https://grafana.github.io/helm-charts` to `https://grafana-community.github.io/helm-charts`
2. Review and update `values.yaml` — SSD mode is deprecated and will be removed in a future community chart release
3. Verify OSS vs. Enterprise feature parity for any Loki features in use
4. This is a major chart version bump (6.x → 7.x) and requires a planned maintenance window

---

### 🟡 PostgreSQL — Safety Hold (Data Loss Risk)

| Chart | File | Current | Bitnami Main Branch | Reason |
|-------|------|---------|---------------------|--------|
| postgresql (frolf) | argocd/apps/frolf-postgres.yaml | 16.2.4 | 17.1.0 / PG 17.6.0 | **SKIP — PG major version change from 16 → 17. Applying WILL wipe the frolf database.** |
| postgresql (resume) | argocd/apps/resume-postgres.yaml | 18.1.13 | Unknown* | **SKIP — Cannot verify latest chart version reliably (bitnami main branch shows 17.1.0, lower than current 18.1.13)** |

> *The bitnami/charts `main` branch shows `postgresql` at chart version 17.1.0 (appVersion 17.6.0),
> which is **lower** than the currently pinned resume-postgres version of 18.1.13. This indicates
> that bitnami's main branch does not track the latest released chart version — likely because they
> maintain release branches per major chart version. Without direct Helm repo index access, the true
> latest version cannot be determined.

**For frolf-postgres specifically:** The file already has a `# PINNED` comment explaining this exact
risk. The last data-loss incident happened when the chart version was bumped and PG 16→17 triggered
`initdb`. Do not update without a migration plan and maintenance window.

**Action needed:** A human with Helm repo access should run:
```bash
helm repo update
helm search repo bitnami/postgresql --versions | head -10
```
And verify PG major version before any update.

---

### 🟡 Charts with Unverifiable Latest Version

The following charts could not be version-checked because:
- Their Helm chart source repos inject the version at release time (not stored in `main` branch `Chart.yaml`)
- Or a stable release could not be confirmed from available data

| Chart | File | Current | Status |
|-------|------|---------|--------|
| cert-manager | argocd/platform/cert-manager.yaml | v1.16.2 | cert-manager repo uses `v0.0.0` placeholder in master; actual release version set at build time. Check https://github.com/cert-manager/cert-manager/releases for latest. |
| tailscale-operator | argocd/platform/tailscale-operator.yaml | 1.96.5 | tailscale/helm-charts repo uses `v0.0.0` placeholder in main. Check https://github.com/tailscale/helm-charts/releases for latest. |
| mimir-distributed | argocd/observability/mimir-app.yaml | 6.0.6 | Only pre-release found in grafana/mimir main: `6.1.0-weekly.398`. Cannot confirm latest stable. Check https://github.com/grafana/mimir/releases for stable releases. |
| grafana | argocd/observability/grafana-app.yaml | 10.5.15 | Chart.yaml not found in any accessible path in grafana/grafana or grafana/helm-charts repos. Check https://github.com/grafana/grafana/releases for latest. |

---

## No Changes (already at latest)

- **ingress-nginx**: 4.15.1 (already latest, appVersion 1.15.1)
- **node-exporter**: 4.55.0 (already latest, appVersion 1.11.1)
- **tempo-distributed**: 1.61.3 (already latest, appVersion 2.9.0)

---

## Changelogs

### sealed-secrets: 2.18.5 → 2.18.6

**App version:** 0.37.0

**Release notes:** https://github.com/bitnami-labs/sealed-secrets/releases/tag/helm-v2.18.6

From RELEASE-NOTES.md (app v0.37.0 — ships with this chart release):
- Expose plaintext `template.data` values in template rendering context ([#1940](https://github.com/bitnami-labs/sealed-secrets/pull/1940))
- Bump Go version to 1.26.3 ([#1966](https://github.com/bitnami-labs/sealed-secrets/pull/1966))
- Updated k8s API/client dependencies and golang.org/x/crypto
- CI: Bumped K8s integration matrix to latest 1.33/1.34/1.35 patches

---

### argo-rollouts: 2.40.9 → 2.41.0

**App version:** v1.9.0

**Release notes:** https://github.com/argoproj/argo-helm/releases/tag/argo-rollouts-2.41.0

_Full changelog available on ArtifactHub. This is a minor chart version bump shipping argo-rollouts v1.9.0._

---

### argo-cd: 9.5.14 → 9.5.21

**App version:** v3.4.3

**Release notes:** https://github.com/argoproj/argo-helm/releases/tag/argo-cd-9.5.21

_Patch release of the argo-cd Helm chart (chart version 9.5.21) shipping ArgoCD v3.4.3. Full changelog on ArtifactHub._

---

### argocd-image-updater: 1.1.5 → 1.2.2

**App version:** v1.2.1

**Release notes:** https://github.com/argoproj/argo-helm/releases/tag/argocd-image-updater-1.2.2

This is a **minor** version bump (1.1.x → 1.2.x). Review the chart changes before deploying; minor bumps may introduce new values or deprecate old ones.

---

### alloy: 1.8.1 → 1.10.0

**App version:** v1.17.0

**Release notes:** https://github.com/grafana/alloy/releases/tag/helm-alloy-1.10.0

Highlights from Alloy v1.17.0 application changelog (shipped by this chart):
- New `gql` subcommand (GraphQL server support)
- `database_observability` improvements: MySQL/Postgres wait event tracking, traceparent extraction, query sample timestamp accuracy
- `otelcol.exporter.prometheus`: Classic histogram → NHCB conversion, Nginx receiver added
- `loki.rules.kubernetes`: External labels support, configurable namespace separator
- HPA support via `controller.autoscaling.horizontal.externalHPA`
- Bug fixes: Critical CVE address in deps, cluster TLS join fix, faro receiver improvements

_This is a **minor** Helm chart version bump (1.8 → 1.10) shipping Alloy v1.17.0. Review values.yaml for any new/changed configuration options._

---

### kube-state-metrics: 7.3.0 → 7.5.1

**App version:** 2.19.1

**Release notes:** https://github.com/prometheus-community/helm-charts/releases/tag/kube-state-metrics-7.5.1

This is a **minor** chart version bump shipping kube-state-metrics 2.19.1. Review ArtifactHub for full changelog.

---

### nats: 2.14.0 → 2.14.2

**App version:** 2.14.2

**Release notes:** https://github.com/nats-io/k8s/releases/tag/nats-2.14.2

Patch release — no breaking changes expected. Full changelog at the release link above.

---

## Methodology Note

Version discovery in this run used `raw.githubusercontent.com` to fetch `Chart.yaml` from each
chart's upstream `main` branch, since the Helm repository index files were network-blocked and
the GitHub API core rate limit (60 req/hr) was exhausted early in the run. The `main` branch
`Chart.yaml` version is typically kept in sync with the latest release for most charts, but may
lag or lead for repos that inject version at build time. A follow-up run with Helm CLI access
or authenticated GitHub API calls would resolve the 4 unverifiable charts.
