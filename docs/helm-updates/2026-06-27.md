# Helm Chart Updates — 2026-06-27

> Auto-generated by nightly-helm-version-check routine.

## Summary

17 charts checked. 10 updated. 2 skipped (require manual review). 5 already at latest.

## Updates Applied

| Chart | File | Old Version | New Version | Bump Type | Notes |
|-------|------|-------------|-------------|-----------|-------|
| sealed-secrets | argocd/platform/sealed-secrets-controller.yaml | 2.18.5 | 2.19.0 | patch | |
| argo-rollouts | argocd/platform/argo-rollouts.yaml | 2.40.9 | 2.41.0 | minor | |
| argo-cd | argocd/platform/argocd.yaml | 9.5.14 | 9.7.1 | minor | |
| argocd-image-updater | argocd/platform/argocd-image-updater.yaml | 1.1.5 | 1.2.4 | minor | |
| cert-manager | argocd/platform/cert-manager.yaml | v1.16.2 | v1.20.3 | minor | ⚠️ Security fix — HIGH CVE `GHSA-8rvj-mm4h-c258`; all users should upgrade |
| tailscale-operator | argocd/platform/tailscale-operator.yaml | 1.96.5 | 1.98.5 | minor | |
| kube-state-metrics | argocd/observability/kube-state-metrics-app.yaml | 7.3.0 | 7.5.1 | minor | |
| alloy | argocd/observability/alloy-app.yaml | 1.8.1 | 1.10.0 | minor | |
| postgresql (frolf) | argocd/apps/frolf-postgres.yaml | 16.2.4 | 17.1.0 | minor | PG major verified: 17.2.0 → 17.6.0 (PG17 → PG17, safe) |
| nats | argocd/apps/frolf-nats.yaml | 2.14.0 | 2.14.2 | patch | |

## ⚠️ Breaking Changes / Skipped

| Chart | File | Current | Latest Available | Reason |
|-------|------|---------|------------------|--------|
| loki | argocd/observability/loki-app.yaml | 6.55.0 | 7.0.0 | **SKIPPED — MIGRATION REQUIRED.** The `7.0.0` chart at `https://grafana.github.io/helm-charts` is GEL (Grafana Enterprise Logs) only. OSS users must switch `repoURL` to `https://grafana-community.github.io/helm-charts` before upgrading. Additionally, SSD (SimpleScalable) mode is deprecated as of chart 6.52 and will be removed in a future community chart version. A separate migration PR is required. This is already documented in a comment inside `loki-app.yaml`. |
| postgresql (resume) | argocd/apps/resume-postgres.yaml | 18.1.13 | Unknown | **SKIPPED — Cannot verify.** The current pin `18.1.13` is ahead of the latest version tracked in the `bitnami/charts` GitHub repo (`17.1.0`, last committed 2026-02-25). The `https://charts.bitnami.com/bitnami` Helm repo is not accessible through the egress proxy. Cannot determine whether `18.1.13` is the latest available or if newer versions exist. Manual verification required. If updating, verify the PostgreSQL major version stays at 17.x to avoid wiping the database. |

## No Changes (already at latest)

- ingress-nginx: 4.15.1 (already latest)
- prometheus-node-exporter: 4.55.0 (already latest)
- tempo-distributed: 1.61.3 (already latest)
- mimir-distributed: 6.0.6 (already latest stable — all newer versions in the `grafana/helm-charts` index are `6.1.0-weekly.xxx` pre-release builds)
- grafana: 10.5.15 (already latest)

## Changelogs

### sealed-secrets: 2.18.5 → 2.19.0

**Release notes** ([view on GitHub](https://github.com/bitnami-labs/sealed-secrets/releases/tag/helm-v2.19.0)):

No release notes body provided. See GitHub release for details.

---

### argo-rollouts: 2.40.9 → 2.41.0

**Release notes** ([view on GitHub](https://github.com/argoproj/argo-helm/releases/tag/argo-rollouts-2.41.0)):

```
## What's Changed
* feat(argo-rollouts): Add Gateway API HTTPRoute support for the dashboard by @somaz94
```

---

### argo-cd: 9.5.14 → 9.7.1

**Release notes** ([view on GitHub](https://github.com/argoproj/argo-helm/releases/tag/argo-cd-9.7.1)):

```
## What's Changed
* fix(argo-cd): gate redis metrics NetworkPolicy rule behind exporter.enabled by @yugstar
```

---

### argocd-image-updater: 1.1.5 → 1.2.4

**Release notes** ([view on GitHub](https://github.com/argoproj/argo-helm/releases/tag/argocd-image-updater-1.2.4)):

```
## What's Changed
* chore(argocd-image-updater): Update dependency argoproj-labs/argocd-image-updater to v1.2.2
```

---

### cert-manager: v1.16.2 → v1.20.3

**Release notes** ([view on GitHub](https://github.com/cert-manager/cert-manager/releases/tag/v1.20.3)):

> ⚠️ **Security advisory**: This patch release fixes a HIGH severity security issue
> ([`GHSA-8rvj-mm4h-c258`](https://github.com/cert-manager/cert-manager/security/advisories/GHSA-8rvj-mm4h-c258))
> where the default `cert-manager-edit` aggregate ClusterRole granted namespace users permission
> to create ACME `Challenge` and `Order` resources directly. A user who could create a `Challenge`
> referencing a `ClusterIssuer` could supply attacker-controlled solver configuration while
> cert-manager loaded credentials from the `ClusterIssuer`'s namespace, bypassing Issuer solver
> selectors. With the acme-dns provider specifically, this could disclose DNS credentials to an
> attacker-controlled endpoint.
>
> This release also removes the issuer owner reference from Challenges which was blocking Challenge
> garbage collection, and updates Go to fix reported CVEs.
>
> **All users should upgrade.**

Note: This is a jump of 4 minor versions (1.16 → 1.20). Intermediate changelogs at:
- [v1.17.x](https://github.com/cert-manager/cert-manager/releases?q=v1.17&expanded=true)
- [v1.18.x](https://github.com/cert-manager/cert-manager/releases?q=v1.18&expanded=true)
- [v1.19.x](https://github.com/cert-manager/cert-manager/releases?q=v1.19&expanded=true)

---

### tailscale-operator: 1.96.5 → 1.98.5

**Release notes** ([view on GitHub](https://github.com/tailscale/tailscale/releases/tag/v1.98.5)):

> Please refer to the changelog available at https://tailscale.com/changelog

The chart version tracks the Tailscale binary release version. `v1.98.5` was published 2026-06-02.

---

### kube-state-metrics: 7.3.0 → 7.5.1

**Release notes** ([view on GitHub](https://github.com/prometheus-community/helm-charts/releases/tag/kube-state-metrics-7.5.1)):

```
## What's Changed
* [kube-state-metrics] Allow setting hostUsers on deployment by @jnoordsij
```

---

### alloy: 1.8.1 → 1.10.0

**Release notes** ([view on GitHub](https://github.com/grafana/alloy/releases/tag/v1.17.0)):

Chart version `1.10.0` corresponds to Grafana Alloy application release `v1.17.0` (2026-06-11).

```
### Features
* Add gql subcommand
* database_observability.mysql: Add database_observability_wait_event_seconds_total counter
* database_observability.mysql: Refactor bulk table metadata collection
```

Note: Intermediate chart version `1.9.0` (app `v1.16.3`) was skipped — updating directly to `1.10.0`.

---

### postgresql (frolf): 16.2.4 → 17.1.0

**PostgreSQL version check** ([bitnami/charts](https://github.com/bitnami/charts)):

| Chart version | PostgreSQL appVersion |
|---|---|
| 16.2.4 (current) | 17.2.0 |
| 17.1.0 (new) | 17.6.0 |

PostgreSQL **major version stays at 17** — this upgrade is safe. This is a PostgreSQL minor version upgrade (17.2 → 17.6). The chart major version bump (16.x → 17.x) does **not** correspond to a PostgreSQL major version change.

Latest commit to bitnami/charts postgresql: 2026-02-25.

---

### nats: 2.14.0 → 2.14.2

**Release notes** ([view on GitHub](https://github.com/nats-io/k8s/releases/tag/nats-2.14.2)):

No release notes body provided. Published 2026-06-11. See GitHub release for details.

---

## Methodology Notes

- Helm chart endpoints (`charts.bitnami.com`, `grafana.github.io/helm-charts`, etc.) are not accessible through the session egress proxy. Chart versions were sourced from:
  - GitHub Releases API (`api.github.com/repos/*/releases`)
  - GitHub raw `Chart.yaml` files at release tags
  - `grafana/helm-charts` `gh-pages` branch `index.yaml` (the official Grafana Helm chart index)
  - `prometheus-community/helm-charts` `gh-pages` branch `index.yaml`
- The tailscale helm chart version (`pkgs.tailscale.com/helmcharts`) is inferred from the Tailscale binary release version, as the `tailscale/tailscale` repo `Chart.yaml` always contains `version: 0.1.0` (version is injected at publish time).
