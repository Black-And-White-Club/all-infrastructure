# Helm Chart Updates — 2026-05-19

> Auto-generated by nightly-helm-version-check routine.
> Data sourced from Chart.yaml on each chart's upstream GitHub main branch and GitHub release tags.
> Note: Helm CDN and chart repo index URLs were unreachable from the runner; versions were resolved via raw.githubusercontent.com and GitHub release HTML.

## Summary

17 charts checked. 5 updated. 2 skipped (require manual review). 10 already at latest.

## Updates Applied

| Chart | File | Old Version | New Version | Bump Type | Notes |
|-------|------|-------------|-------------|-----------|-------|
| cert-manager | argocd/platform/cert-manager.yaml | v1.16.2 | v1.21.0 | minor | v prefix preserved; 5 minor versions; chart version = app version |
| argocd-image-updater | argocd/platform/argocd-image-updater.yaml | 1.1.5 | 1.2.1 | minor | Fixes namespaced Role permissions |
| tailscale-operator | argocd/platform/tailscale-operator.yaml | 1.96.5 | 1.98.2 | minor | Chart version tracks Tailscale release version |
| mimir-distributed | argocd/observability/mimir-app.yaml | 6.0.6 | 6.1.0 | minor | |
| postgresql (frolf) | argocd/apps/frolf-postgres.yaml | 16.2.4 | 17.1.0 | minor | ⚠️ SEE POSTGRES NOTE BELOW — PG major version verified safe |

## ⚠️ Breaking Changes / Skipped

### SKIPPED — loki: 6.55.0 (no safe update path without repo migration)

**File:** argocd/observability/loki-app.yaml

The loki chart at version 7.0.0 in the `grafana.github.io/helm-charts` repo is **Grafana Enterprise Loki (GEL) only**. The `loki-app.yaml` file itself documents this:

> _MIGRATION REQUIRED before next Loki bump: chart 7.0.0 in grafana/loki is GEL (enterprise) only. OSS users must switch repoURL to https://grafana-community.github.io/helm-charts before upgrading. 6.55.0 is the last OSS-compatible version in this repo. SSD (SimpleScalable) mode is also deprecated as of chart 6.52 and will be removed in a future community chart version._

**Action required:** Before upgrading loki beyond 6.55.0, change `repoURL` in loki-app.yaml from `https://grafana.github.io/helm-charts` to `https://grafana-community.github.io/helm-charts`, validate the community chart supports the current `values.yaml` configuration (especially SSD/SimpleScalable mode which is deprecated), and test in a non-production environment first.

---

### SKIPPED — resume-postgres: 18.1.13 (version anomaly — cannot verify)

**File:** argocd/apps/resume-postgres.yaml

| Current | Latest Verified | Reason |
|---------|-----------------|--------|
| 18.1.13 | 17.1.0 (PG17.6.0) | The pinned version 18.1.13 is **higher** than the highest bitnami/postgresql chart version found in the upstream git repository (17.1.0 as of 2026-02-25). Chart 18.x was not found in the bitnami/charts GitHub repo or CHANGELOG. This may mean: (a) bitnami published 18.x to their Helm repo without updating the GitHub main branch yet, or (b) the version is incorrect. **Do NOT downgrade this without verifying what PG version 18.x ships.** Manual investigation required. |

---

### PostgreSQL (frolf) — PG major version verification note

The `frolf-postgres.yaml` file carries a strong PINNED warning because a prior chart upgrade caused data loss (PG16→PG17 transition). Before applying this PR, verify the following, which was confirmed by this routine:

| Chart Version | PostgreSQL appVersion |
|--------------|----------------------|
| 16.2.4 (current) | **17.2.0** (PG17) |
| 17.1.0 (new) | **17.6.0** (PG17) |

**Both versions ship PostgreSQL 17.** There is NO PostgreSQL major version boundary crossing in this update. The prior data loss occurred when the chart was upgraded from the 15.x series (PG16) to the 16.x series (PG17) — Bitnami's chart major version does NOT map 1:1 to the PostgreSQL major version.

This update (chart 16.2.4 → 17.1.0) is a minor version bump within PG17 and is safe to apply.

## No Changes (already at latest)

- sealed-secrets: 2.18.5 (already latest)
- ingress-nginx: 4.15.1 (already latest)
- argo-rollouts: 2.40.9 (already latest)
- argo-cd: 9.5.14 (already latest)
- kube-state-metrics: 7.3.0 (already latest)
- alloy: 1.8.1 (already latest)
- prometheus-node-exporter: 4.55.0 (already latest)
- tempo-distributed: 1.61.3 (already latest; marked deprecated upstream)
- grafana: 10.5.15 (already latest; appVersion 12.3.1)
- nats: 2.14.0 (already latest)

## Changelogs

### cert-manager: v1.16.2 → v1.21.0

**Release notes** ([view on GitHub](https://github.com/cert-manager/cert-manager/releases/tag/v1.21.0)):

Five minor releases since last pin (v1.17, v1.18, v1.19, v1.20, v1.21). Changelog content unavailable from runner (GitHub Releases API blocked); review full release notes at the link above before merging. Check for any CRD schema changes that may require running `kubectl apply --server-side` with the updated CRDs.

---

### argocd-image-updater: 1.1.5 → 1.2.1

**Release notes** ([view on GitHub](https://github.com/argoproj/argo-helm/releases/tag/argocd-image-updater-1.2.1)):

- Fixed: Update permissions for namespaced Role (from `artifacthub.io/changes` in Chart.yaml)
- App version: v1.2.0

---

### tailscale-operator: 1.96.5 → 1.98.2

**Release notes** ([view on GitHub](https://github.com/tailscale/tailscale/releases/tag/v1.98.2)):

Minor bump within the 1.9x stable series (Tailscale uses even minor versions for stable: 1.94, 1.96, 1.98, …). Changelog content unavailable from runner; review release notes at the link above. Chart version tracks the Tailscale application release version.

---

### mimir-distributed: 6.0.6 → 6.1.0

**Release notes** ([view on GitHub](https://github.com/grafana/helm-charts/releases/tag/mimir-distributed-6.1.0)):

From the upstream CHANGELOG (changes in main since 6.0.6 that are included in 6.1.0):

- [CHANGE] Upgrade minimum supported Kubernetes version to 1.32
- [ENHANCEMENT] Add support to customize gossip ring k8s service annotations
- [ENHANCEMENT] Ruler querier and query-frontend: Add support for newly-introduced querier ring for query planning
- [ENHANCEMENT] Upgrade rollout-operator chart to 0.48.0
- [ENHANCEMENT] Add support for KEDA ScaledObject fallback configuration
- [ENHANCEMENT] Kafka: Made log retention period configurable via `kafka.logRetentionHours`
- [ENHANCEMENT] Allow overwriting zone downscale annotation for ingester and store-gateway
- [BUGFIX] Fix missing newline for custom pod labels
- [BUGFIX] Fix Kafka image reference to include registry in StatefulSet template
- [BUGFIX] Remove helm's empty selector for smoke-test-job (was throwing errors in ArgoCD)

> ⚠️ Note: minimum Kubernetes version is now **1.32** if this change landed in 6.1.0.

---

### postgresql (frolf): 16.2.4 → 17.1.0

**Release notes** ([view on GitHub](https://github.com/bitnami/charts/blob/main/bitnami/postgresql/CHANGELOG.md)):

- **17.1.0 (2026-02-25):** Configurable data source credentials for postgresql exporter
- **17.0.2 (2026-02-09):** Modify charts' READMEs title; Add `primary.service.enabled` parameter; wording updates
- **17.0.1 (2025-10-14):** Minor: collect metrics as postgres user; update kubectl command for creating secrets
- **17.0.0 → 16.7.27 range:** Various dependency reference updates (patch releases)

PostgreSQL appVersion: 17.2.0 (current) → 17.6.0 (new). Same major version.
