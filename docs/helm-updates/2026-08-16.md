# Helm Chart Updates — 2026-08-16

> Auto-generated by nightly-helm-version-check routine.

## ⚠️ TASK INCOMPLETE — Proxy Policy Blocked All Helm Chart Repositories

The nightly Helm version check **could not complete** because the egress proxy policy
in this remote execution environment denies connections to all Helm chart repositories
and the GitHub API. No versions were fetched, no files were updated.

### Blocked hosts

| Host | Error |
|------|-------|
| `get.helm.sh:443` | 403 — Policy denial (Helm install script) |
| `api.github.com:443` | 403 — Policy denial (GitHub API) |
| `argoproj.github.io:443` | Connection refused / timeout |
| `kubernetes.github.io:443` | Connection refused / timeout |
| `grafana.github.io:443` | Connection refused / timeout |
| `prometheus-community.github.io:443` | Connection refused / timeout |
| `charts.bitnami.com:443` | 403 — Policy denial |
| `charts.jetstack.io:443` | Connection refused / timeout |
| `nats-io.github.io:443` | Connection refused / timeout |
| `pkgs.tailscale.com:443` | Connection refused / timeout |

**To resolve:** The scheduled task requires outbound HTTPS to the above hosts.
Either allow them in the session's egress policy, or run the version check from
an environment without these restrictions.

---

## ⚠️ Stale Baseline in Scheduled Task Config

The scheduled task was configured with outdated "current pinned version" baselines.
The actual versions in the repository are **already newer** than the task's baseline,
suggesting a previous run or manual update already bumped many charts.

The task config baseline vs. what is actually pinned in the repo today:

| Chart | Task Baseline | Actual in Repo (2026-08-16) | Difference |
|-------|--------------|----------------------------|------------|
| sealed-secrets | 2.17.3 | **2.18.5** | Task is stale |
| ingress-nginx | 4.14.0 | **4.15.1** | Task is stale |
| argo-rollouts | 2.39.0 | **2.40.9** | Task is stale |
| argo-cd | 9.4.5 | **9.5.14** | Task is stale |
| cert-manager | v1.16.2 | v1.16.2 | Match |
| argocd-image-updater | 1.0.5 | **1.1.5** | Task is stale |
| tailscale-operator | 1.96.5 | 1.96.5 | Match |
| loki | 6.46.0 | **6.55.0** | Task is stale |
| kube-state-metrics | 6.4.1 | **7.3.0** | Task is stale (minor bump) |
| alloy | 1.5.0 | **1.8.1** | Task is stale |
| prometheus-node-exporter | 4.49.2 | **4.55.0** | Task is stale |
| tempo-distributed | 1.56.2 | **1.61.3** | Task is stale |
| mimir-distributed | 6.0.3 | **6.0.6** | Task is stale |
| grafana | 10.2.0 | **10.5.15** | Task is stale |
| frolf-postgres (bitnami) | 16.2.4 | 16.2.4 | Match |
| resume-postgres (bitnami) | 18.1.13 | 18.1.13 | Match |
| nats | 1.2.5 | **2.14.0** | Task is stale (major bump!) |

**Recommendation:** Update the scheduled task's baseline versions to match the actual
repo state. The next successful run should use the values in the "Actual in Repo" column
as the comparison baseline.

---

## Action Required

1. **Allow egress** to the Helm chart repos and GitHub API, OR run this check from an
   unrestricted environment.
2. **Update the scheduled task** baseline config to reflect the actual pinned versions
   listed above, so future runs correctly identify only new updates.
