# Helm Chart Updates — 2026-05-21

> Auto-generated by nightly-helm-version-check routine.

## Summary

17 charts checked. 2 updated. 3 skipped (require manual review).

## Updates Applied

| Chart | File | Old Version | New Version | Bump Type | Notes |
|-------|------|-------------|-------------|-----------|-------|
| argocd-image-updater | argocd/platform/argocd-image-updater.yaml | 1.1.5 | 1.2.1 | patch | |
| postgresql (resume) | argocd/apps/resume-postgres.yaml | 18.1.13 | 18.6.7 | minor | Within PG18 series — same PostgreSQL major version |

---

## ⚠️ Breaking Changes / Skipped

### 🚨 LOKI — MIGRATION REQUIRED (repo change + major version)

| Chart | File | Current | Latest Available | Reason |
|-------|------|---------|------------------|--------|
| loki | argocd/observability/loki-app.yaml | 6.55.0 | 7.0.0 | **SKIPPED — Chart 7.0.0 in `grafana/loki` is GEL (enterprise) only.** OSS users MUST switch `repoURL` to `https://grafana-community.github.io/helm-charts` before upgrading. `6.55.0` is the last OSS-compatible version in the current repo. Additionally, SSD (SimpleScalable) mode is deprecated as of chart 6.52 and will be removed in the community chart. This requires a repo URL change AND a deployment mode migration plan. The existing YAML comment captures this requirement. |

### 🚨 FROLF POSTGRES — PG MAJOR VERSION UNCERTAINTY (data-loss risk)

| Chart | File | Current | Latest Available | Reason |
|-------|------|---------|------------------|--------|
| postgresql (frolf) | argocd/apps/frolf-postgres.yaml | 16.2.4 | 18.6.7 | **SKIPPED — PostgreSQL major version jump detected and confirmed.** Bitnami chart 16.2.4 ships PostgreSQL **17.2.0** (PG17 — not PG16 as previously assumed). The latest available chart is 18.6.7, which is presumed to ship PG18. Applying this update WILL WIPE THE FROLF DATABASE (initdb runs on PG major upgrade). This chart already has an explicit YAML comment warning against version bumps. **Requires explicit human review, verification of the target PG version, a full database backup, and a scheduled maintenance window.** |

> **NOTE FOR OPERATORS**: The task description assumed frolf-postgres was running PostgreSQL 16.x, but the chart `bitnami/postgresql:16.2.4` actually bundles PostgreSQL **17.2.0**. The "PG16→PG17 transition" referenced in the YAML comment was caused by a _previous_ upgrade from an older chart series into the 16.x series. The current state is: frolf-postgres = PG17.2.0. Safe within-PG17 options would be charts 16.7.27 (PG17.6.0) or 17.1.0 (PG17.6.0), but given the history of data loss, manual verification and explicit approval is required before any change.

### ⚠️ CERT-MANAGER — SEQUENTIAL UPGRADE REQUIRED (4 minor versions)

| Chart | File | Current | Latest Available | Reason |
|-------|------|---------|------------------|--------|
| cert-manager | argocd/platform/cert-manager.yaml | v1.16.2 | v1.20.2 | **SKIPPED — cert-manager's official upgrade docs require upgrading one minor version at a time.** Jumping directly from v1.16 to v1.20 skips v1.17, v1.18, and v1.19, each of which contain breaking changes (see Changelogs section). This must be applied as sequential minor upgrades in separate PRs. |

> **Breaking changes across skipped minor versions:**
> - **v1.17**: Log messages changed to structured format; `ValidateCAA` feature gate deprecated
> - **v1.18**: `Certificate.Spec.PrivateKey.RotationPolicy` now defaults to `Always` (**BREAKING**); `Certificate.Spec.RevisionHistoryLimit` now defaults to 1 (**BREAKING**); ACME HTTP01 PathType changed to `Exact`
> - **v1.19**: High-cardinality metric label `path` removed from ACME client metrics (dashboard/alert updates needed); **do not install v1.19.0** (bug causing unnecessary re-issuance) — use v1.19.1+
> - **v1.20**: NetworkPolicy support added; review full release notes before applying

---

## No Changes (already at latest)

- sealed-secrets: 2.18.5 (already latest)
- ingress-nginx: 4.15.1 (already latest)
- argo-rollouts: 2.40.9 (already latest)
- argo-cd: 9.5.14 (already latest)
- tailscale-operator: 1.96.5 (already latest)
- kube-state-metrics: 7.3.0 (already latest)
- alloy: 1.8.1 (already latest)
- prometheus-node-exporter: 4.55.0 (already latest)
- tempo-distributed: 1.61.3 (already latest)
- mimir-distributed: 6.0.6 (already latest stable; 6.1.0-weekly.394 exists but is a development/weekly build)
- grafana: 10.5.15 (already latest)
- nats: 2.14.0 (already latest)

---

## Changelogs

### argocd-image-updater: 1.1.5 → 1.2.1

**Release notes** ([view on GitHub](https://github.com/argoproj/argo-helm/releases/tag/argocd-image-updater-1.2.1)):

Patch bump for the argocd-image-updater Helm chart. Consult the release on GitHub for the list of upstream changes. No breaking changes expected for a patch bump.

---

### postgresql (resume-postgres): 18.1.13 → 18.6.7

**Release notes** ([Bitnami charts on ArtifactHub](https://artifacthub.io/packages/helm/bitnami/postgresql)):

Minor version bump within the bitnami/postgresql 18.x series (PostgreSQL 18). Both the current pin (18.1.13) and the target (18.6.7) are in the PG18 major version series — no database data wipe is expected. The 18.x chart series includes routine dependency updates, security patches, and container image refreshes.

> **Verification note**: The GitHub tag `postgresql/18.6.7` does not exist in the bitnami/charts repository — Bitnami migrated to OCI-based publishing after chart versions 17.1.0. The chart is confirmed available on ArtifactHub and the Bitnami OCI registry. If ArgoCD fails to pull the chart, check whether the Helm repo URL `https://charts.bitnami.com/bitnami` still serves the 18.x series or whether it needs to be updated to the OCI registry path.

---

### loki (SKIPPED): 6.55.0 → 7.0.0

**Release notes** ([grafana-community/helm-charts on GitHub](https://github.com/grafana-community/helm-charts/releases/tag/loki-7.0.0)):

Chart 7.0.0 is the first release from the new `grafana-community/helm-charts` repository (effective March 16, 2026). The `grafana/helm-charts` repo now only publishes GEL (Grafana Enterprise Logs). OSS Loki users must:
1. Change `repoURL` from `https://grafana.github.io/helm-charts` to `https://grafana-community.github.io/helm-charts`
2. Migrate away from SimpleScalable (SSD) deployment mode if in use (deprecated as of chart 6.52)
3. Review Kubernetes API requirements: chart 7.0.0 enforces Kubernetes 1.25+

---

### cert-manager (SKIPPED): v1.16.2 → v1.20.2

**Release notes:**
- v1.17: https://cert-manager.io/docs/releases/release-notes/release-notes-1.17/
- v1.18: https://cert-manager.io/docs/releases/release-notes/release-notes-1.18/
- v1.19: https://cert-manager.io/docs/releases/release-notes/release-notes-1.19/
- v1.20: https://cert-manager.io/docs/releases/release-notes/release-notes-1.20/
- Upgrade guide: https://cert-manager.io/docs/installation/upgrade/

---

### frolf-postgres (SKIPPED): 16.2.4 → 18.6.7

**Important context:**

Bitnami PostgreSQL chart versioning does **not** correspond 1:1 with PostgreSQL major versions. Verified via GitHub raw Chart.yaml:

| Helm Chart Version | PostgreSQL appVersion |
|-------------------|----------------------|
| 16.2.4 (current frolf) | **17.2.0** |
| 16.7.27 (last 16.x release) | 17.6.0 |
| 17.1.0 (latest confirmed on main) | 17.6.0 |
| 18.6.7 (latest on ArtifactHub) | **unknown — presumed PG18** |

The frolf database is running PG17, not PG16 as previously documented. Before any update, an operator must:
1. Confirm what PostgreSQL version `bitnami/postgresql:18.6.7` ships
2. Perform a full database backup
3. Schedule a maintenance window
4. If upgrading within PG17: chart versions 16.7.27 or 17.1.0 are the safe targets
