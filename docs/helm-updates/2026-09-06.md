# Helm Chart Updates — 2026-09-06

> Auto-generated by nightly-helm-version-check routine.

## ⛔ Run Status: BLOCKED — Network Policy Prevents Version Checking

**The routine could not determine latest chart versions.** All Helm chart repository hosts are blocked by the session's egress policy. The routine attempted to:

1. Install Helm 3 via `get.helm.sh` → **403 policy denial**
2. Helm was built from source via `go install helm.sh/helm/v3/cmd/helm@v3.17.3` → **succeeded**
3. Add all chart repos (`helm repo add ...`) → **all returned "Forbidden"** (egress policy blocks GitHub Pages and other chart hosts)
4. Direct `curl` to chart index files → **connection refused (000)** at the proxy

### Blocked Hosts (all required for version checking)

| Helm Chart Host | Purpose |
|----------------|---------|
| `bitnami-labs.github.io` | sealed-secrets index |
| `kubernetes.github.io` | ingress-nginx index |
| `argoproj.github.io` | argo-cd, argo-rollouts, argocd-image-updater index |
| `charts.jetstack.io` | cert-manager index |
| `pkgs.tailscale.com` | tailscale-operator index |
| `grafana.github.io` | loki, alloy, tempo, mimir, grafana index |
| `prometheus-community.github.io` | kube-state-metrics, node-exporter index |
| `charts.bitnami.com` | postgresql index |
| `nats-io.github.io` | nats index |
| `get.helm.sh` | Helm binary download |
| `api.github.com` (other repos) | GitHub release notes (token scoped to this repo only) |

### Action Required

To make this routine functional, one of the following changes is needed:

1. **Update egress policy** to allow the Helm chart repository hosts listed above.
   - Most are GitHub Pages (`*.github.io`) or well-known Helm CDNs.
   - This is the recommended fix for full routine functionality.

2. **Run manually** on a machine with unrestricted internet access:
   ```bash
   helm repo add bitnami-labs https://bitnami-labs.github.io/sealed-secrets
   helm repo add ingress-nginx https://kubernetes.github.io/ingress-nginx
   helm repo add argoproj https://argoproj.github.io/argo-helm
   helm repo add jetstack https://charts.jetstack.io
   helm repo add tailscale https://pkgs.tailscale.com/helmcharts
   helm repo add grafana https://grafana.github.io/helm-charts
   helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
   helm repo add bitnami https://charts.bitnami.com/bitnami
   helm repo add nats https://nats-io.github.io/k8s/helm/charts/
   helm repo update
   # Then run helm search repo <chart> for each chart
   ```

3. **Alternative: ArtifactHub API** — ArtifactHub (artifacthub.io) aggregates Helm chart versions and exposes an API. If that domain is allowed, the routine could be updated to use it instead of direct Helm repo queries.

---

## Current Pinned Versions (as of 2026-09-06)

> These were read directly from the YAML files. Note: these differ from the scheduled task's reference table — the repo has been updated since the task prompt was authored.

| Chart | File | Pinned Version |
|-------|------|---------------|
| sealed-secrets | argocd/platform/sealed-secrets-controller.yaml | 2.18.5 |
| ingress-nginx | argocd/platform/nginx-ingress-app.yaml | 4.15.1 |
| argo-rollouts | argocd/platform/argo-rollouts.yaml | 2.40.9 |
| argo-cd | argocd/platform/argocd.yaml | 9.5.14 |
| cert-manager | argocd/platform/cert-manager.yaml | v1.16.2 |
| argocd-image-updater | argocd/platform/argocd-image-updater.yaml | 1.1.5 |
| tailscale-operator | argocd/platform/tailscale-operator.yaml | 1.96.5 |
| loki | argocd/observability/loki-app.yaml | 6.55.0 |
| kube-state-metrics | argocd/observability/kube-state-metrics-app.yaml | 7.3.0 |
| alloy | argocd/observability/alloy-app.yaml | 1.8.1 |
| prometheus-node-exporter | argocd/observability/node-exporter-app.yaml | 4.55.0 |
| tempo-distributed | argocd/observability/tempo-app.yaml | 1.61.3 |
| mimir-distributed | argocd/observability/mimir-app.yaml | 6.0.6 |
| grafana | argocd/observability/grafana-app.yaml | 10.5.15 |
| postgresql (frolf) | argocd/apps/frolf-postgres.yaml | 16.2.4 |
| postgresql (resume) | argocd/apps/resume-postgres.yaml | 18.1.13 |
| nats | argocd/apps/frolf-nats.yaml | 2.14.0 |

**17 charts checked. 0 updated (version checking blocked). 0 skipped (couldn't reach repos).**

### Notable Version Drift vs. Task Prompt Reference

The scheduled task's reference table shows older versions than what is currently in the repo. The following charts have already been updated since the prompt was written:

| Chart | Prompt Reference | Actual Current |
|-------|-----------------|----------------|
| sealed-secrets | 2.17.3 | **2.18.5** |
| ingress-nginx | 4.14.0 | **4.15.1** |
| argo-rollouts | 2.39.0 | **2.40.9** |
| argo-cd | 9.4.5 | **9.5.14** |
| argocd-image-updater | 1.0.5 | **1.1.5** |
| loki | 6.46.0 | **6.55.0** |
| kube-state-metrics | 6.4.1 | **7.3.0** (minor bump) |
| alloy | 1.5.0 | **1.8.1** |
| prometheus-node-exporter | 4.49.2 | **4.55.0** |
| tempo-distributed | 1.56.2 | **1.61.3** |
| mimir-distributed | 6.0.3 | **6.0.6** |
| grafana | 10.2.0 | **10.5.15** |
| nats | 1.2.5 | **2.14.0** (major bump — already in repo) |

The task prompt's reference table should be updated to reflect current pinned versions for future runs.

---

## ⚠️ PostgreSQL Safety Reminder

Even when this routine can run successfully in future, the PostgreSQL charts require extra caution:

- **frolf-postgres** (bitnami/postgresql 16.2.4): Do NOT upgrade if the chart's `appVersion` changes from PostgreSQL 16.x to any other major version. A major PG version bump causes `initdb` which **wipes the database**. frolf-bot lost data once due to this. See the comment in `argocd/apps/frolf-postgres.yaml`.
- **resume-postgres** (bitnami/postgresql 18.1.13): Same risk — verify `appVersion` major version is unchanged before updating.

---

## Next Steps

1. Update egress policy to allow Helm chart repository hosts (or use ArtifactHub API).
2. Update the scheduled task's reference table to current versions listed above.
3. Re-run the routine after egress policy is updated.
