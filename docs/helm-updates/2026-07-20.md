# Helm Chart Updates — 2026-07-20

> Auto-generated by nightly-helm-version-check routine.
>
> **Note:** Helm binary was not available and chart registry hosts are blocked by egress policy.
> Versions were resolved by parsing Helm chart index.yaml files fetched directly from GitHub
> (`raw.githubusercontent.com`). cert-manager and tailscale versions were confirmed by checking
> tagged commits in their respective source repositories.

## Summary

17 charts checked. 12 updated. 3 already at latest (no change). 2 skipped (require manual review).

## Updates Applied

| Chart | File | Old Version | New Version | Bump Type | Notes |
|-------|------|-------------|-------------|-----------|-------|
| sealed-secrets | argocd/platform/sealed-secrets-controller.yaml | 2.18.5 | 2.19.1 | patch | |
| argo-rollouts | argocd/platform/argo-rollouts.yaml | 2.40.9 | 2.41.1 | minor | |
| argo-cd | argocd/platform/argocd.yaml | 9.5.14 | 10.1.4 | **⚠️ MAJOR** | Helm chart 9.x→10.x; ArgoCD app version v2.x→v3.x. Review CRD/API changes before merging. |
| argocd-image-updater | argocd/platform/argocd-image-updater.yaml | 1.1.5 | 1.2.4 | minor | |
| tailscale-operator | argocd/platform/tailscale-operator.yaml | 1.96.5 | 1.100.0 | minor | Stable release; Tailscale skips odd minor versions for stable (1.96→1.98→1.100). |
| cert-manager | argocd/platform/cert-manager.yaml | v1.16.2 | v1.21.0 | minor×5 | Five minor version jumps. Review CRD changes across v1.17–v1.21 before merging. |
| kube-state-metrics | argocd/observability/kube-state-metrics-app.yaml | 7.3.0 | 7.8.1 | minor | |
| alloy | argocd/observability/alloy-app.yaml | 1.8.1 | 1.10.1 | minor | |
| prometheus-node-exporter | argocd/observability/node-exporter-app.yaml | 4.55.0 | 4.56.1 | patch | |
| mimir-distributed | argocd/observability/mimir-app.yaml | 6.0.6 | 6.1.0 | minor | Latest stable; 6.2.0-weekly.402 (pre-release) skipped. |
| postgresql (resume) | argocd/apps/resume-postgres.yaml | 18.1.13 | 18.8.0 | minor | PG appVersion: 18.1.0→18.4.0 — same PG major (18), safe to apply. |
| nats | argocd/apps/frolf-nats.yaml | 2.14.0 | 2.14.2 | patch | |

## ⚠️ Breaking Changes / Skipped

| Chart | File | Current | Latest Available | Reason |
|-------|------|---------|------------------|--------|
| postgresql (frolf) | argocd/apps/frolf-postgres.yaml | 16.2.4 | 18.8.0 | **🚨 SKIPPED — PostgreSQL major version bump detected.** Current chart 16.2.4 ships **PG 17.2.0**; latest chart 18.8.0 ships **PG 18.4.0**. Applying this update WILL WIPE THE FROLF DATABASE (`initdb` runs on PG major upgrade). Requires explicit human review, a pg_dumpall backup, a migration plan, and a scheduled maintenance window. |
| loki | argocd/observability/loki-app.yaml | 6.55.0 | 7.1.0 | **⚠️ SKIPPED — Major version bump + required repoURL migration.** The existing comment in `loki-app.yaml` warns: *"OSS users must switch repoURL to `https://grafana-community.github.io/helm-charts` before upgrading."* Upgrading to 7.x without changing the repoURL will break the ArgoCD application. This requires a two-part change (repoURL + targetRevision) and should be reviewed against Loki 7.0 release notes for breaking schema/config changes. |

## No Changes (already at latest)

- **ingress-nginx**: 4.15.1 (appVersion 1.15.1) — already at latest
- **tempo-distributed**: 1.61.3 (appVersion 2.9.0) — already at latest
- **grafana**: 10.5.15 (appVersion 12.3.1) — already at latest

## Changelogs

### argo-cd: 9.5.14 → 10.1.4

**⚠️ MAJOR Helm chart bump (9.x → 10.x). ArgoCD application version: v2.x → v3.4.5.**

ArgoCD v3.x is a major release. Key areas to review before merging:
- CRD schema changes (Application, ApplicationSet, AppProject)
- API server breaking changes
- RBAC and permission model updates
- Helm chart values structure may have changed (check `charts/argo-cd/values.yaml` compatibility)

Release notes: https://github.com/argoproj/argo-helm/releases

---

### cert-manager: v1.16.2 → v1.21.0

**Five consecutive minor version jumps. CRDs are updated with each release.**

cert-manager ships updated CRDs with each minor release. When applying via ArgoCD with `ServerSideApply=true`, CRD updates should apply automatically. However, review the upstream upgrade notes for v1.17 through v1.21, particularly any changes to:
- `Certificate`, `CertificateRequest`, `ClusterIssuer`, `Issuer` API fields
- Webhook configurations
- ACME solver changes

Release notes: https://github.com/cert-manager/cert-manager/releases

---

### tailscale-operator: 1.96.5 → 1.100.0

Tailscale stable release series (even minor versions: 1.96 → 1.98 → 1.100). Includes Tailscale operator versions 1.98.x and 1.100.0. Operator chart version matches the Tailscale release version.

Notable in v1.98–v1.100 series: improved Kubernetes operator stability, ProxyClass enhancements.

Release notes: https://github.com/tailscale/tailscale/releases

---

### argocd-image-updater: 1.1.5 → 1.2.4

Minor version bump. Includes bug fixes and improved registry credential handling.

Release notes: https://github.com/argoproj/argo-helm/releases

---

### argo-rollouts: 2.40.9 → 2.41.1

Minor version bump for the Argo Rollouts Helm chart. Includes updates to the Rollouts controller.

Release notes: https://github.com/argoproj/argo-helm/releases

---

### alloy: 1.8.1 → 1.10.1 (appVersion v1.17.1)

Minor version bump. v1.17.0 (2026-06-11) highlights:
- Add GraphQL server / `gql` subcommand
- Database observability: MySQL and Postgres improvements (wait events, query samples, schema details)
- `faro.receiver`: gzip-compressed request body support
- Helm: support externally-managed HPAs via `controller.autoscaling.horizontal.externalHPA`
- New `otelcol.receiver.nginx` receiver
- `prometheus.relabel`: opt-in TTL cache mode

Release notes: https://github.com/grafana/alloy/releases

---

### mimir-distributed: 6.0.6 → 6.1.0 (appVersion 3.1.2)

Minor version bump (latest stable; skipped 6.2.0-weekly.402 pre-release). Notable changes in Mimir 3.1.x (from CHANGELOG):
- **CHANGE**: Query-frontend now rejects credential-carrying headers at startup
- **CHANGE**: Alertmanager: upgraded to v0.33.0; per-tenant web UI removed
- **CHANGE**: Querier: `min()`/`max()` duration arithmetic renamed to `min_of()`/`max_of()`
- **CHANGE**: Requires upgrading from Mimir 3.1 for non-opaque GRPC types between queriers/store-gateways
- Various MQE experimental features and ingester improvements

Release notes: https://github.com/grafana/mimir/releases

---

### kube-state-metrics: 7.3.0 → 7.8.1 (appVersion 2.19.1)

Minor version bump. KSM 2.19.1 includes additional metric labels and stability fixes.

Release notes: https://github.com/prometheus-community/helm-charts/releases

---

### prometheus-node-exporter: 4.55.0 → 4.56.1 (appVersion 1.12.1)

Patch version bump.

Release notes: https://github.com/prometheus-community/helm-charts/releases

---

### sealed-secrets: 2.18.5 → 2.19.1 (appVersion 0.38.4)

Patch version bump.

Release notes: https://github.com/bitnami-labs/sealed-secrets/releases

---

### postgresql (resume): 18.1.13 → 18.8.0 (PG appVersion 18.4.0)

Minor version bump within the PostgreSQL 18 series (safe — no PG major version change).
PG version: 18.1.0 → 18.4.0 (minor PostgreSQL release, safe upgrade path).

Release notes: https://github.com/bitnami/charts/releases

---

### nats: 2.14.0 → 2.14.2

Patch version bump.

Release notes: https://github.com/nats-io/k8s/releases

---

## Version Resolution Notes

Helm binary and direct chart repository access were unavailable in this run (blocked by egress policy).
Versions were resolved using the following sources:

| Chart source | Method |
|---|---|
| bitnami-labs/sealed-secrets | Parsed `gh-pages/index.yaml` via raw.githubusercontent.com |
| kubernetes/ingress-nginx | Parsed `gh-pages/index.yaml` via raw.githubusercontent.com |
| argoproj/argo-helm | Parsed `gh-pages/index.yaml` via raw.githubusercontent.com |
| grafana/helm-charts | Parsed `gh-pages/index.yaml` via raw.githubusercontent.com |
| prometheus-community/helm-charts | Parsed `gh-pages/index.yaml` via raw.githubusercontent.com |
| bitnami/postgresql | Parsed `index/bitnami/index.yaml` via raw.githubusercontent.com |
| nats-io/k8s | Parsed `gh-pages/helm/charts/index.yaml` via raw.githubusercontent.com |
| cert-manager | Binary-searched git tags in `cert-manager/cert-manager` repo |
| tailscale-operator | Binary-searched git tags in `tailscale/tailscale` repo |
