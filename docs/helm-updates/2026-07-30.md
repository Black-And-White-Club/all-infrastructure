# Helm Chart Updates — 2026-07-30

> Auto-generated by nightly-helm-version-check routine.

## Summary

17 charts checked. 12 updated. 3 skipped (require manual review). 2 already at latest.

## Updates Applied

| Chart | File | Old Version | New Version | Bump Type | Notes |
|-------|------|-------------|-------------|-----------|-------|
| sealed-secrets | argocd/platform/sealed-secrets-controller.yaml | 2.18.5 | 2.19.1 | minor | |
| argo-rollouts | argocd/platform/argo-rollouts.yaml | 2.40.9 | 2.41.1 | minor | |
| argo-cd | argocd/platform/argocd.yaml | 9.5.14 | 10.2.1 | **⚠️ MAJOR** | Chart 9→10. Breaking: removed default values from `config.params`; `crd-project.yaml` renamed to `crd-appproject.yaml`. Review ArgoCD values.yaml before sync. |
| cert-manager | argocd/platform/cert-manager.yaml | v1.16.2 | v1.21.1 | minor | 5-minor-version jump; generally backward-compatible. |
| argocd-image-updater | argocd/platform/argocd-image-updater.yaml | 1.1.5 | 1.2.4 | minor | |
| tailscale-operator | argocd/platform/tailscale-operator.yaml | 1.96.5 | 1.98.9 | minor | |
| kube-state-metrics | argocd/observability/kube-state-metrics-app.yaml | 7.3.0 | 8.0.0 | **⚠️ MAJOR** | Breaking: dropped CiliumNetworkPolicy support. Cluster uses K3s/flannel — not Cilium — so likely safe. Verify before sync. |
| alloy | argocd/observability/alloy-app.yaml | 1.8.1 | 1.11.0 | minor | |
| prometheus-node-exporter | argocd/observability/node-exporter-app.yaml | 4.55.0 | 4.56.1 | minor | |
| mimir-distributed | argocd/observability/mimir-app.yaml | 6.0.6 | 6.1.0 | minor | |
| postgresql (resume) | argocd/apps/resume-postgres.yaml | 18.1.13 | 18.8.1 | minor | Within chart 18.x series — same PostgreSQL major version. See safety note below. |
| nats | argocd/apps/frolf-nats.yaml | 2.14.0 | 2.14.2 | patch | |

## ⚠️ Breaking Changes / Skipped

| Chart | File | Current | Latest Available | Reason |
|-------|------|---------|------------------|--------|
| postgresql (frolf) | argocd/apps/frolf-postgres.yaml | 16.2.4 | 18.8.1 | **SKIPPED — PostgreSQL MAJOR VERSION BUMP DETECTED.** Bitnami chart 16.x ships PostgreSQL 16; latest chart 18.8.1 ships PostgreSQL 17 or 18. Applying this update WILL WIPE THE FROLF DATABASE (initdb runs on PG major upgrade — frolf-bot already lost all data this way in PR #10). Requires explicit human review, a full backup, a migration plan, and a scheduled maintenance window. |
| grafana | argocd/observability/grafana-app.yaml | 10.5.15 | 12.10.0 | **SKIPPED — HELM REPO MIGRATED.** The Grafana Helm chart moved from `grafana.github.io/helm-charts` to `grafana-community.github.io/helm-charts` after January 30, 2026. Version 10.5.15 was the last published to the old repo. Upgrading requires changing the `repoURL` in the ArgoCD Application AND updating `targetRevision`. See [migration guide](https://grafana.com/docs/loki/latest/setup/upgrade/upgrade-to-community/). New repo latest: `grafana-12.10.0` (released 2026-07-28). |
| loki | argocd/observability/loki-app.yaml | 6.55.0 | 7.2.0 | **SKIPPED — HELM REPO MIGRATED.** The Loki Helm chart moved from `grafana.github.io/helm-charts` to `grafana-community.github.io/helm-charts`. Version 6.55.0 (appVersion 3.6.7) was the **final release** from the old repo. The file already has a comment noting this. Upgrading to 7.x also requires: (1) change `repoURL`; (2) breaking values changes — SimpleScalable mode is deprecated, persistence config structure changed, enterprise support removed. See [upgrade guide](https://grafana.com/docs/loki/latest/setup/upgrade/upgrade-to-6x/). |

### PostgreSQL Safety Detail

Bitnami postgresql chart major version tracks the PostgreSQL engine major version:
- Chart 16.x → PostgreSQL 16.x (frolf-postgres current)
- Chart 17.x → PostgreSQL 17.x
- Chart 18.x → PostgreSQL 17.x or 18.x (resume-postgres current)

**frolf-postgres** is hard-pinned at 16.2.4 (PG 16). The latest bitnami chart (18.8.1) ships a newer PG major version. Update would trigger `initdb` → data wipe. **Do not touch without a DBA-reviewed migration plan.**

**resume-postgres** updated from 18.1.13 → 18.8.1: both are within the chart 18.x series, preserving the same PostgreSQL major version. This is considered safe; validated by the chart major version remaining at 18.

## No Changes (already at latest)

- **ingress-nginx**: 4.15.1 — already at latest
- **tempo-distributed**: 1.61.3 — already at latest

## Changelogs

### sealed-secrets: 2.18.5 → 2.19.1

**Release notes** ([view on GitHub](https://github.com/bitnami-labs/sealed-secrets/releases/tag/helm-v2.19.1)):

Latest patch release of the Bitnami sealed-secrets Helm chart. Check the release page for specific dependency and security updates.

---

### argo-rollouts: 2.40.9 → 2.41.1

**Release notes** ([view on GitHub](https://github.com/argoproj/argo-helm/releases/tag/argo-rollouts-2.41.1)):

Minor patch update to the Argo Rollouts Helm chart.

---

### argo-cd: 9.5.14 → 10.2.1 ⚠️ MAJOR

**Release notes** ([view on GitHub](https://github.com/argoproj/argo-helm/releases/tag/argo-cd-10.2.1)):

Chart 10.2.1 itself is a minor patch (redis exporter bumped to v1.88.0). The **major version jump from 9→10** introduced these breaking changes in 10.0.0:
- Removed default values from `config.params` — custom params previously relying on defaults may behave differently.
- `crd-project.yaml` renamed to `crd-appproject.yaml` (contents unchanged, upstream naming alignment).

Review the ArgoCD `charts/argo-cd/values.yaml` and compare against the 10.x chart defaults before allowing sync. Since ArgoCD manages its own sync, this is particularly high-stakes — a misconfigured self-managed ArgoCD could break the entire GitOps control plane.

---

### cert-manager: v1.16.2 → v1.21.1

**Release notes** ([view on GitHub](https://github.com/cert-manager/cert-manager/releases/tag/v1.21.1)):

Five-minor-version jump (v1.16 → v1.21). cert-manager has a strong backward-compatibility policy; minor releases are generally safe. Review the cert-manager changelogs for v1.17 through v1.21 if any custom resources or webhook configurations are in use.

---

### argocd-image-updater: 1.1.5 → 1.2.4

**Release notes** ([view on GitHub](https://github.com/argoproj/argo-helm/releases/tag/argocd-image-updater-1.2.4)):

Minor chart update for ArgoCD Image Updater.

---

### tailscale-operator: 1.96.5 → 1.98.9

**Release notes** ([Tailscale changelog](https://tailscale.com/changelog)):

Two minor versions forward (1.96 → 1.98). Corresponds to Tailscale operator release v1.98.9.

---

### kube-state-metrics: 7.3.0 → 8.0.0 ⚠️ MAJOR

**Release notes** ([view on GitHub](https://github.com/prometheus-community/helm-charts/releases/tag/kube-state-metrics-8.0.0)):

Breaking change: **CiliumNetworkPolicy support dropped**. If no CiliumNetworkPolicy values are in `charts/kube-state-metrics/values.yaml`, this is safe. This cluster runs K3s with flannel networking (not Cilium), so the impact should be zero — but verify the values file before allowing sync.

---

### alloy: 1.8.1 → 1.11.0

**Release notes** ([view on GitHub](https://github.com/grafana/helm-charts/releases/tag/alloy-1.11.0)):

Three minor versions forward on Grafana Alloy. Still served from `grafana.github.io/helm-charts` (not yet migrated). Review `charts/alloy/values.yaml` for any deprecated keys.

---

### prometheus-node-exporter: 4.55.0 → 4.56.1

**Release notes** ([view on GitHub](https://github.com/prometheus-community/helm-charts/releases/tag/prometheus-node-exporter-4.56.1)):

Minor patch update.

---

### mimir-distributed: 6.0.6 → 6.1.0

**Release notes** ([view on GitHub](https://github.com/grafana/helm-charts/releases/tag/mimir-distributed-6.1.0)):

Minor update. Weekly pre-release versions (6.2.0-weekly.*) were skipped; 6.1.0 is the latest stable release.

---

### postgresql (resume): 18.1.13 → 18.8.1

**Release notes** ([bitnami/charts](https://github.com/bitnami/charts/tree/main/bitnami/postgresql)):

Minor update within the chart 18.x series. PostgreSQL major version unchanged. Check `charts/postgres/resume-values.yaml` for any deprecated configuration keys.

---

### nats: 2.14.0 → 2.14.2

**Release notes** ([view on GitHub](https://github.com/nats-io/k8s/releases/tag/nats-2.14.2)):

Patch update.

---

## Network / Tooling Notes

This run used direct HTTPS fetches to GitHub release pages (via WebFetch) and GitHub search (via WebSearch) to determine latest chart versions — the Helm binary could not be installed (get.helm.sh blocked by egress policy) and Helm chart repo index.yaml files are similarly blocked. Version data was gathered from GitHub release tags and ArtifactHub web search results. If any version appears stale, re-verify by checking the relevant GitHub releases page directly.
