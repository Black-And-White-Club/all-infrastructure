# Helm Chart Updates — 2026-09-10

> Auto-generated by nightly-helm-version-check routine.
> Note: Helm binary and direct chart-repo access were unavailable in this run (egress policy blocks chart repo CDNs and get.helm.sh). Version data was gathered via WebSearch against ArtifactHub, GitHub release pages, and official docs. Reviewers should independently verify latest versions before merging.

## Summary

17 charts checked. 10 updated. 3 already at latest. 4 skipped (require manual review — see ⚠️ Breaking Changes section).

## Updates Applied

| Chart | File | Old Version | New Version | Bump Type | Notes |
|-------|------|-------------|-------------|-----------|-------|
| sealed-secrets | argocd/platform/sealed-secrets-controller.yaml | 2.18.5 | 2.18.6 | patch | |
| argo-rollouts | argocd/platform/argo-rollouts.yaml | 2.40.9 | 2.43.1 | minor | |
| cert-manager | argocd/platform/cert-manager.yaml | v1.16.2 | v1.21.1 | minor | 5 minor-version jump; review each release's upgrade notes before merging |
| argocd-image-updater | argocd/platform/argocd-image-updater.yaml | 1.1.5 | 1.3.1 | minor | |
| tailscale-operator | argocd/platform/tailscale-operator.yaml | 1.96.5 | 1.102.3 | minor | |
| kube-state-metrics | argocd/observability/kube-state-metrics-app.yaml | 7.3.0 | 7.5.2 | minor | |
| alloy | argocd/observability/alloy-app.yaml | 1.8.1 | 1.9.2 | minor | |
| prometheus-node-exporter | argocd/observability/node-exporter-app.yaml | 4.55.0 | 4.56.3 | patch | |
| mimir-distributed | argocd/observability/mimir-app.yaml | 6.0.6 | 6.2.0 | minor | |
| nats | argocd/apps/frolf-nats.yaml | 2.14.0 | 2.14.6 | patch | |

## ⚠️ Breaking Changes / Skipped (Require Human Review)

### 1. ArgoCD — Major Chart Version Bump (9.x → 10.x)

| Chart | File | Current | Latest Available | Reason |
|-------|------|---------|------------------|--------|
| argo-cd | argocd/platform/argocd.yaml | 9.5.14 | 10.8.2 | **SKIPPED — Major chart version bump.** Chart 10.x ships ArgoCD 2.13+ and introduces breaking changes: `managedNamespaceMetadata` behavior changed with server-side apply (existing client-side-applied namespace labels/annotations may be dropped), bundled Helm upgraded (3.13.2 → 3.14.3), kubectl upgraded (1.24 → 1.26). Upgrading ArgoCD (the GitOps controller managing the whole platform) must be done deliberately with a verified rollback plan. |

**Action required:** Review the [argo-helm releases](https://github.com/argoproj/argo-helm/releases) changelog for every 9.x → 10.x entry. Test in a non-prod cluster first if possible. Confirm managedNamespaceMetadata usage is not broken.

---

### 2. Loki — Chart Migrated to grafana-community

| Chart | File | Current | Status | Reason |
|-------|------|---------|--------|--------|
| loki | argocd/observability/loki-app.yaml | 6.55.0 | **AT FORK POINT** | **SKIPPED — Migration required.** Effective March 16, 2026, the Grafana Loki OSS Helm chart was forked to `grafana-community/helm-charts`. The original `grafana/loki` chart at version 6.55.0 is the fork point; the `grafana/loki` chart in the original repo is now maintenance-only for GEL (Grafana Enterprise Logs) users. The community chart is at 7.x and continuing active development. For OSS deployments, migrate to: `helm repo add grafana-community https://grafana-community.github.io/helm-charts` and chart `grafana-community/loki`. |

**Action required:** Evaluate migrating `argocd/observability/loki-app.yaml` to use `repoURL: https://grafana-community.github.io/helm-charts` with chart `loki`. Check the community chart's changelog for any breaking changes between 6.55.0 and the latest 7.x. Note: the built-in MinIO subchart is deprecated and will be removed 2026-10-31.

---

### 3. Tempo Distributed — Chart Migrated + Kafka Architecture (Major Breaking Changes)

| Chart | File | Current | Latest (Community) | Reason |
|-------|------|---------|---------------------|--------|
| tempo-distributed | argocd/observability/tempo-app.yaml | 1.61.3 | ~3.0.5 (community) | **SKIPPED — Major architectural breaking changes.** The `grafana/tempo-distributed` chart has migrated to `grafana-community/helm-charts`. Chart 3.0.0 introduced a Kafka-based architecture (distributors write to Kafka, block-builders consume it), **removed the MinIO subchart entirely**, and requires significant values restructuring. This is not a drop-in upgrade. |

**Action required:** Evaluate Tempo 3.x architecture requirements (Kafka broker dependency). Review [CHANGELOG](https://github.com/grafana-community/helm-charts/tree/main/charts/tempo-distributed) before attempting migration. This likely requires a maintenance window and may require standing up a Kafka-compatible broker.

---

### 4. frolf-postgres — PostgreSQL Major Version Risk Across Patch Versions

| Chart | File | Current | Latest in 16.x | Reason |
|-------|------|---------|-----------------|--------|
| postgresql (frolf) | argocd/apps/frolf-postgres.yaml | 16.2.4 | ~16.6.7 | **SKIPPED — Bitnami chart 16.x ships DIFFERENT PostgreSQL major versions across patches.** Investigation found that bitnami postgresql chart 16.6.2 ships PostgreSQL **17.4.0** (NOT 16.x). This means a within-series bump from 16.2.4 → 16.6.x would trigger a PG major version upgrade (16 → 17), which causes `initdb` to run on startup and **WIPES THE ENTIRE DATABASE**. This already happened once (frolf-bot PR #10). Do not update until the exact appVersion for each intermediate chart version is verified. |

**Action required:** Use `helm show chart bitnami/postgresql --version <target>` to check `appVersion` for the target chart version before updating. Only proceed if appVersion stays within PG 16 (i.e., `16.x.y`). If the latest within the 16.x chart series has already jumped to PG 17, plan a proper migration with pg_upgrade, a backup, and a maintenance window. **Never merge this automatically.**

---

### 5. ingress-nginx — Project Archived (EOL)

| Chart | File | Current | Latest | Status |
|-------|------|---------|--------|--------|
| ingress-nginx | argocd/platform/nginx-ingress-app.yaml | 4.15.1 | 4.15.1 | **ALREADY AT FINAL VERSION — PROJECT EOL** |

The `kubernetes/ingress-nginx` project was archived by its owners on **March 24, 2026**. Version 4.15.1 is the final chart release. No further updates, bug fixes, or security patches will be released. Existing deployments continue to function, but this is a **critical gap** in the security posture.

**Action required:** Plan migration to a maintained Ingress/Gateway implementation. The upstream recommendation is to adopt a [Gateway API](https://gateway-api.sigs.k8s.io/) implementation. Evaluate options such as Envoy Gateway, Istio, or Cilium Gateway. This migration can be phased but should be tracked as a P1 infrastructure task.

---

## No Changes (already at latest)

- **grafana**: 10.5.15 (already latest)
- **resume-postgres**: 18.1.13 (confirmed as latest on ArtifactHub — bitnami/postgresql chart 18.1.13 ships PostgreSQL 17.6.0)
- **ingress-nginx**: 4.15.1 (final version — see EOL warning above)

## Changelogs

### sealed-secrets: 2.18.5 → 2.18.6

**Release notes** ([view on GitHub](https://github.com/bitnami-labs/sealed-secrets/releases/tag/helm-v2.18.6)):
Patch release. Check the GitHub releases page for details. Likely bumps controller image and dependency updates.

---

### argo-rollouts: 2.40.9 → 2.43.1

**Release notes** ([view on GitHub](https://github.com/argoproj/argo-helm/releases?q=argo-rollouts)):
Minor release series (2.41.x, 2.42.x, 2.43.x). Includes Argo Rollouts controller updates, CRD improvements, and bug fixes. Review changelog entries for any values schema changes between 2.40.x and 2.43.x.

---

### cert-manager: v1.16.2 → v1.21.1

**Release notes** ([view on GitHub](https://github.com/cert-manager/cert-manager/releases)):
Five minor versions (1.17, 1.18, 1.19, 1.20, 1.21). cert-manager maintains strong backward compatibility within 1.x, but reviewers should scan each release's upgrade notes at https://cert-manager.io/docs/releases/. Key recent changes include improvements to ACME solver reliability, gateway API support enhancements, and CRD refinements.

---

### argocd-image-updater: 1.1.5 → 1.3.1

**Release notes** ([view on GitHub](https://github.com/argoproj/argo-helm/releases?q=argocd-image-updater)):
Minor release. Image Updater improvements, bug fixes. Check for any changes to annotation schema or write-back behavior that might affect existing image update configurations.

---

### tailscale-operator: 1.96.5 → 1.102.3

**Release notes** ([view on GitHub](https://github.com/tailscale/helm-charts/releases)):
Tracks Tailscale stable releases (version numbers align with Tailscale client version). Review https://tailscale.com/changelog for operator-relevant changes between 1.96 and 1.102.

---

### kube-state-metrics: 7.3.0 → 7.5.2

**Release notes** ([view on GitHub](https://github.com/prometheus-community/helm-charts/releases?q=kube-state-metrics)):
Minor release. kube-state-metrics updates and metric additions. Review for any metric renames or new label changes that might affect existing Grafana dashboards or alerting rules.

---

### alloy: 1.8.1 → 1.9.2

**Release notes** ([view on GitHub](https://github.com/grafana/alloy/releases)):
Minor release. Grafana Alloy v1.9.x brings pipeline and component improvements. Review https://grafana.com/docs/alloy/latest/release-notes/ for any breaking changes to component APIs used in the alloy configuration.

---

### prometheus-node-exporter: 4.55.0 → 4.56.3

**Release notes** ([view on GitHub](https://github.com/prometheus-community/helm-charts/releases?q=prometheus-node-exporter)):
Patch release. Node exporter image update and chart improvements.

---

### mimir-distributed: 6.0.6 → 6.2.0

**Release notes** ([view on Grafana docs](https://grafana.com/docs/helm-charts/mimir-distributed/latest/release-notes/)):
Minor release. Mimir performance and stability improvements. Review release notes at the Grafana docs link for any values schema changes, particularly around component-specific configs.

---

### nats: 2.14.0 → 2.14.6

**Release notes** ([view on GitHub](https://github.com/nats-io/k8s/releases)):
Patch release. NATS server version bump and chart stability fixes. The nats chart major.minor tracks the NATS server version (2.14.x → NATS Server 2.14.x), so this stays on the same NATS minor release series.
