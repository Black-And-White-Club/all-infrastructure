# Helm Chart Updates — 2026-06-18

> Auto-generated by nightly-helm-version-check routine.
> Version discovery performed via ArtifactHub and GitHub release search (Helm binary unavailable; Helm repo index endpoints returned HTTP 403 in this environment).

## Summary

17 charts checked. 6 updated. 4 skipped (require manual review). 7 already at latest.

---

## Updates Applied

| Chart | File | Old Version | New Version | Bump Type | Notes |
|-------|------|-------------|-------------|-----------|-------|
| argo-cd | argocd/platform/argocd.yaml | 9.5.14 | 9.5.21 | patch | |
| argocd-image-updater | argocd/platform/argocd-image-updater.yaml | 1.1.5 | 1.2.2 | minor | |
| tailscale-operator | argocd/platform/tailscale-operator.yaml | 1.96.5 | 1.98.4 | minor | |
| kube-state-metrics | argocd/observability/kube-state-metrics-app.yaml | 7.3.0 | 7.5.1 | minor | |
| postgresql (resume) | argocd/apps/resume-postgres.yaml | 18.1.13 | 18.7.5 | minor | PG major version verified unchanged (PG 18 → PG 18) |
| nats | argocd/apps/frolf-nats.yaml | 2.14.0 | 2.14.2 | patch | |

---

## ⚠️ Breaking Changes / Skipped (Manual Review Required)

### 1. `frolf-postgres` — ⛔ CRITICAL: DO NOT UPDATE WITHOUT A MIGRATION PLAN

| Chart | File | Current | Latest Available | Reason |
|-------|------|---------|------------------|--------|
| postgresql (frolf) | argocd/apps/frolf-postgres.yaml | 16.2.4 | 18.7.5 | **SKIPPED — PostgreSQL MAJOR VERSION BUMP DETECTED (PG 16 → PG 18).** Applying this update WILL WIPE THE FROLF DATABASE. When the bitnami/postgresql chart changes PG major versions, `initdb` runs on next pod start and destroys all data. frolf-bot already lost its entire database once this way (chart 16.x → 17.x, PR #10). This requires an explicit migration plan: pg_dump, controlled upgrade in a maintenance window, and data restore validation. **DO NOT merge this file change — it has not been made.** |

### 2. `loki` — Major chart version bump + repository migration

| Chart | File | Current | Latest Available | Reason |
|-------|------|---------|------------------|--------|
| loki | argocd/observability/loki-app.yaml | 6.55.0 | 7.0.0 | **SKIPPED** — This is a **major chart version bump** (6 → 7). Additionally, as of March 16, 2026, the Grafana Loki chart for OSS users has migrated to `grafana-community/helm-charts`. The loki-app.yaml already contains a warning comment noting this migration requirement. Upgrading requires first switching `repoURL` from `https://grafana.github.io/helm-charts` to `https://grafana-community.github.io/helm-charts`, then applying the 7.0 upgrade. Review the [Loki 7.0 migration guide](https://github.com/grafana/loki/blob/main/production/helm/loki/CHANGELOG.md) before proceeding. |

### 3. `argo-rollouts` — Immutable deployment label selector breaking change

| Chart | File | Current | Latest Available | Reason |
|-------|------|---------|------------------|--------|
| argo-rollouts | argocd/platform/argo-rollouts.yaml | 2.40.9 | 2.41.0 | **SKIPPED** — Version 2.41.0 introduces a **breaking change**: the `app.kubernetes.io/component` label is added to the controller Deployment's `matchLabels` selector, and Kubernetes label selectors are immutable. ArgoCD's automated sync will fail with an error. Manual intervention required: delete the `argo-rollouts` Deployment (orphaning children) before ArgoCD applies the new version, or run `helm upgrade --force`. See [argo-helm releases](https://github.com/argoproj/argo-helm/releases). |

### 4. `cert-manager` — Multi-minor jump with CRD changes and K8s version requirement

| Chart | File | Current | Latest Available | Reason |
|-------|------|---------|------------------|--------|
| cert-manager | argocd/platform/cert-manager.yaml | v1.16.2 | v1.20.2 | **SKIPPED** — This spans **4 minor versions** (v1.16 → v1.20). cert-manager's own documentation recommends upgrading one minor version at a time, reading each version's release notes. Additionally, v1.20 introduces a Kubernetes `SelectableFields` feature that requires **Kubernetes ≥ v1.32**. v1.19.2 also has a known CRD conversion webhook issue that causes Helm upgrades to fail. Required upgrade path: v1.16.2 → v1.17.x → v1.18.x → v1.19.x (patched) → v1.20.2, applying CRDs at each step. See [cert-manager upgrade guide](https://cert-manager.io/docs/installation/upgrade/). |

---

## No Changes (Already at Latest Stable)

| Chart | File | Version | Notes |
|-------|------|---------|-------|
| sealed-secrets | argocd/platform/sealed-secrets-controller.yaml | 2.18.5 | File is already at 2.18.5 (newer than 2.18.2 found in index) |
| ingress-nginx | argocd/platform/nginx-ingress-app.yaml | 4.15.1 | Already at latest — ⚠️ **kubernetes/ingress-nginx was archived on Mar 24, 2026**. Charts remain usable but no new upstream features will be developed. Plan migration to NGINX Inc's `nginx/kubernetes-ingress` or another solution. |
| alloy | argocd/observability/alloy-app.yaml | 1.8.1 | Already at latest |
| prometheus-node-exporter | argocd/observability/node-exporter-app.yaml | 4.55.0 | Already at latest |
| tempo-distributed | argocd/observability/tempo-app.yaml | 1.61.3 | Already at latest in grafana/grafana repo — ⚠️ note: tempo-distributed chart moved to `grafana-community/helm-charts` after Jan 30, 2026. Consider migrating repoURL for future updates. |
| mimir-distributed | argocd/observability/mimir-app.yaml | 6.0.6 | Already at latest stable. Only weekly pre-release builds (6.1.0-weekly.xxx) available beyond this; quarterly stable release expected ~Sep 2026. |
| grafana | argocd/observability/grafana-app.yaml | 10.5.15 | Already at latest in grafana/grafana repo — ⚠️ note: grafana chart moved to `grafana-community/helm-charts` after Jan 30, 2026. The community chart (12.4.7) now defaults to Grafana 13 and requires an image renderer token. Consider evaluating migration separately. |

---

## Changelogs

### argo-cd: 9.5.14 → 9.5.21

**Release notes** ([view on GitHub](https://github.com/argoproj/argo-helm/releases)):

Patch release in the 9.5.x series. Tracks ArgoCD application updates and minor chart fixes. No breaking changes expected within the 9.5.x patch series. Note: if upgrading from an earlier baseline that used redis-ha, the `argocd-redis-ha-haproxy` Deployment selector labels changed in 9.5.x — this is already resolved since the cluster is at 9.5.14.

---

### argocd-image-updater: 1.1.5 → 1.2.2

**Release notes** ([view on GitHub](https://github.com/argoproj/argo-helm/releases)):

Minor bump within the 1.x series. Tracks argocd-image-updater application releases and chart improvements. No breaking changes reported for this minor range.

---

### tailscale-operator: 1.96.5 → 1.98.4

**Release notes** ([view on GitHub](https://github.com/tailscale/tailscale/releases)):

Minor bump tracking Tailscale daemon and operator releases. Version 1.98.x is a regular Tailscale stable release. Review [Tailscale release notes](https://tailscale.com/changelog/) for operator-specific changes.

---

### kube-state-metrics: 7.3.0 → 7.5.1

**Release notes** ([view on GitHub](https://github.com/prometheus-community/helm-charts/releases)):

Minor bump within the 7.x series. Updates kube-state-metrics application version and chart defaults. No breaking changes reported. Chart is available via OCI at `oci://ghcr.io/prometheus-community/charts/kube-state-metrics`.

---

### postgresql (resume): 18.1.13 → 18.7.5

**Release notes** ([view on GitHub](https://github.com/bitnami/charts/blob/main/bitnami/postgresql/CHANGELOG.md)):

Minor patch series bump within bitnami/postgresql chart 18.x (PostgreSQL 18.x). Chart 18.7.5 was released 2026-06-16. No PostgreSQL major version change detected (both chart 18.1.13 and 18.7.5 ship PostgreSQL 18.x). Routine security and dependency updates.

---

### nats: 2.14.0 → 2.14.2

**Release notes** ([view on GitHub](https://github.com/nats-io/k8s/releases)):

Patch bump within the nats 2.14.x series. Routine NATS server image and chart configuration updates. No breaking changes expected for patch releases.

---

## Additional Notices

### ⚠️ kubernetes/ingress-nginx archived (Mar 24, 2026)

The upstream `kubernetes/ingress-nginx` project was archived by its maintainers on March 24, 2026. The Helm chart at version 4.15.1 continues to function, but there will be no further upstream security patches, bug fixes, or feature releases. **Action required (not urgent but plan ahead):** evaluate migrating to an actively maintained ingress controller — NGINX Inc's official chart (`nginx/kubernetes-ingress`) or another maintained alternative.

### ⚠️ Grafana chart ecosystem migration (Jan 30, 2026)

Three Grafana charts — `grafana`, `tempo-distributed`, and `loki` — have moved from `grafana/helm-charts` to `grafana-community/helm-charts`. The `grafana` and `tempo-distributed` charts in this repo are already at the last versions published to the old repository. Future updates require a `repoURL` migration. The `loki` chart requires the migration before the 7.0 upgrade can be applied.

### PostgreSQL bitnami chart series mapping (reference)

As of June 2026, Bitnami postgresql chart major versions track PostgreSQL major versions:

- **Chart 16.x** → PostgreSQL 16.x (`frolf-postgres` is pinned here intentionally)
- **Chart 17.x** → PostgreSQL 17.x
- **Chart 18.x** → PostgreSQL 18.x (`resume-postgres` is in this series)

Always verify `appVersion` before crossing chart major version boundaries.
