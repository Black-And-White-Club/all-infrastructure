# Helm Chart Updates — 2026-05-18

> Auto-generated by nightly-helm-version-check routine.

## Summary

17 charts checked. 2 updated. 3 skipped (require manual review). 12 already at latest.

## Updates Applied

| Chart | File | Old Version | New Version | Bump Type | Notes |
|-------|------|-------------|-------------|-----------|-------|
| cert-manager | argocd/platform/cert-manager.yaml | v1.16.2 | v1.20.2 | minor | ⚠️ Container UID/GID changed (1000→65532) in v1.20.0. Cert-manager recommends incremental upgrades; verify volume permissions post-apply. |
| argocd-image-updater | argocd/platform/argocd-image-updater.yaml | 1.1.5 | 1.2.1 | minor | Bug fix: missing permissions on namespaced Role. |

## ⚠️ Breaking Changes / Skipped

| Chart | File | Current | Latest Available | Reason |
|-------|------|---------|------------------|--------|
| postgresql (frolf) | argocd/apps/frolf-postgres.yaml | 16.2.4 | 17.1.0 | **SKIPPED — PostgreSQL major version bump: chart 16.x ships PG 16, chart 17.x ships PG 17. Applying this upgrade WILL WIPE THE FROLF DATABASE** (initdb runs on PG major upgrade). The frolf-postgres.yaml has a PINNED comment confirming this. Requires explicit human review, a full backup, a migration plan, and a scheduled maintenance window. |
| postgresql (resume) | argocd/apps/resume-postgres.yaml | 18.1.13 | unknown | **SKIPPED — The current version 18.1.13 is not present in the bitnami/charts GitHub CHANGELOG** (latest tracked there is 17.1.0). This suggests the chart is being pulled from a separate distribution channel (possibly Bitnami Secure Images OCI registry). The PostgreSQL major version shipped by 18.1.13 could not be verified. Manual review required to determine: (1) what PG version 18.1.13 ships, (2) what the latest available version is, and (3) whether any upgrade is safe. |
| loki (OSS) | argocd/observability/loki-app.yaml | 6.55.0 | 7.1.0 | **SKIPPED — The grafana/helm-charts loki chart is now GEL (Grafana Enterprise Logs) only as of chart 7.0.0 (March 16, 2026). Version 6.55.0 is the final OSS-compatible release in this repo.** OSS users must migrate to `https://grafana-community.github.io/helm-charts` before upgrading. Additionally, SimpleScalable (SSD) mode is deprecated as of chart 6.52 and will be removed in a future community chart version. This requires a repoURL change in the ArgoCD Application, a mode/values migration, and human review. See migration notes in loki-app.yaml. |

## No Changes (already at latest)

- sealed-secrets: 2.18.5 (already latest, appVersion: 0.36.6)
- ingress-nginx: 4.15.1 (already latest, appVersion: 1.15.1)
- argo-rollouts: 2.40.9 (already latest, appVersion: v1.9.0)
- argo-cd: 9.5.14 (already latest, appVersion: v3.4.2)
- tailscale-operator: 1.96.5 (already at or beyond latest stable Tailscale release v1.96.4)
- loki: 6.55.0 (last OSS-compatible version; upgrade requires repo migration — see Breaking Changes)
- kube-state-metrics: 7.3.0 (already latest, appVersion: 2.18.0)
- alloy: 1.8.1 (already latest, appVersion: v1.16.1)
- prometheus-node-exporter: 4.55.0 (already latest, appVersion: 1.11.1)
- tempo-distributed: 1.61.3 (already latest, appVersion: 2.9.0)
- mimir-distributed: 6.0.6 (latest stable; 6.1.0 is weekly pre-releases only)
- grafana: 10.5.15 (already latest in grafana/helm-charts; grafana-community/grafana is a separate repo at 12.3.3)
- frolf-nats: 2.14.0 (already latest)

## Changelogs

### cert-manager: v1.16.2 → v1.20.2

**Release notes** ([view v1.20.2 on GitHub](https://github.com/cert-manager/cert-manager/releases/tag/v1.20.2)):

**v1.20.2 (2025-04-11):**
- Helm chart fix: resolved invalid YAML generation when both `webhook.config` and `webhook.volumes` are defined simultaneously.
- Security: bumped Go to 1.26.2 and updated dependencies to address reported vulnerabilities.

**v1.20.0 breaking changes (full release notes: [v1.20.0](https://github.com/cert-manager/cert-manager/releases/tag/v1.20.0)):**
- **Container UID/GID changed from 1000/0 to 65532/65532.** This may require adjusting volume permissions (`fsGroup`, `runAsUser`) in values overrides.
- `DefaultPrivateKeyRotationPolicyAlways` feature gate promoted to GA (cannot be disabled).
- Azure Private DNS support added for DNS-01 ACME challenges.
- OtherNames feature promoted to Beta and enabled by default.
- Venafi issuer references rebranded to CyberArk.

> **Upgrade note:** cert-manager recommends upgrading one minor version at a time. Jumping from v1.16.2 to v1.20.2 skips v1.17, v1.18, and v1.19. Review the individual release notes for each minor version before applying:
> - [v1.17.x](https://github.com/cert-manager/cert-manager/releases?q=v1.17&expanded=true)
> - [v1.18.x](https://github.com/cert-manager/cert-manager/releases?q=v1.18&expanded=true)
> - [v1.19.x](https://github.com/cert-manager/cert-manager/releases?q=v1.19&expanded=true)

---

### argocd-image-updater: 1.1.5 → 1.2.1

**Release notes** ([view argocd-image-updater-1.2.1 on GitHub](https://github.com/argoproj/argo-helm/releases/tag/argocd-image-updater-1.2.1)):

**1.2.1 (2026-05-16):**
- Bug fix: added missing permissions to namespaced Role (PR #3886 by ystkfujii).

**1.2.0 (2026-05-15):**
- Minor version release with improvements preceding the 1.2.1 patch.

No breaking changes in either release.

---

## Loki Migration Notes (Action Required)

The Grafana Loki Helm chart in `grafana/helm-charts` is **now GEL (enterprise) only** as of chart 7.0.0 (released March 16, 2026). Version **6.55.0 is the final OSS-compatible release** in that repository.

To upgrade Loki for OSS users:
1. Change `repoURL` in `argocd/observability/loki-app.yaml` from `https://grafana.github.io/helm-charts` to `https://grafana-community.github.io/helm-charts`
2. Determine the appropriate version in the community chart (starting from ~7.0.0)
3. Review deprecation of SimpleScalable (SSD) mode — removed in a future community chart version
4. Update values.yaml accordingly

See the existing migration comment in `argocd/observability/loki-app.yaml` for additional context.
