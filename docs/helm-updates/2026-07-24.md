# Helm Chart Updates — 2026-07-24

> Auto-generated by nightly-helm-version-check routine.

## ⚠️ Run Status: BLOCKED — External Network Access Denied

This nightly run was **unable to check for chart updates**. All external chart
repository hosts and the GitHub API were blocked by the session's egress proxy
policy. No chart versions were changed in this run.

### What was blocked

Every upstream source required for version checking was denied with `403
Forbidden` by the proxy:

| Source | URL | Result |
|--------|-----|--------|
| Helm binary download | `get.helm.sh` | 403 Policy Denial |
| Bitnami chart index | `charts.bitnami.com` | 403 Policy Denial |
| Grafana chart index | `grafana.github.io` | 403 Policy Denial |
| Prometheus Community index | `prometheus-community.github.io` | 403 Policy Denial |
| ingress-nginx chart index | `kubernetes.github.io` | 403 Policy Denial |
| ArgoProj chart index | `argoproj.github.io` | 403 Policy Denial |
| Jetstack chart index | `charts.jetstack.io` | 403 Policy Denial |
| Tailscale chart index | `pkgs.tailscale.com` | 403 Policy Denial |
| NATS chart index | `nats-io.github.io` | 403 Policy Denial |
| GitHub API (releases) | `api.github.com` | 403 – session not scoped to external repos |

The GitHub API 403 body: *"GitHub access to this repository is not enabled for
this session. Use add_repo to request access."*

This means the nightly job **cannot** install Helm, **cannot** fetch chart
`index.yaml` files, and **cannot** query GitHub releases for any upstream
chart repository. No version checking was possible.

### What is needed to fix this

For this scheduled routine to function, the session's network egress policy
must allow at least one of:

1. **Direct chart repo access** — allow `charts.bitnami.com`,
   `grafana.github.io`, `prometheus-community.github.io`,
   `kubernetes.github.io`, `argoproj.github.io`, `charts.jetstack.io`,
   `pkgs.tailscale.com`, and `nats-io.github.io`; OR

2. **Helm binary download** — allow `get.helm.sh` so Helm can be installed and
   repos can be managed normally; OR

3. **GitHub API access to public repos** — allow `api.github.com` without repo
   restriction (only public read) so release tags can be queried.

---

## Current Pinned Versions (as-found in repo on 2026-07-24)

These are the actual values currently in the files — note they differ from the
original task inventory, suggesting previous successful runs updated many charts.

| Chart | File | Pinned Version |
|-------|------|----------------|
| sealed-secrets | argocd/platform/sealed-secrets-controller.yaml | 2.18.5 |
| ingress-nginx | argocd/platform/nginx-ingress-app.yaml | 4.15.1 |
| argo-rollouts | argocd/platform/argo-rollouts.yaml | 2.40.9 |
| argo-cd | argocd/platform/argocd.yaml | 9.5.14 |
| cert-manager | argocd/platform/cert-manager.yaml | v1.16.2 |
| argocd-image-updater | argocd/platform/argocd-image-updater.yaml | 1.1.5 |
| tailscale-operator | argocd/platform/tailscale-operator.yaml | 1.96.5 |
| loki | argocd/observability/loki-app.yaml | 6.55.0 |
| kube-state-metrics | argocd/observability/kube-state-metrics-app.yaml | 7.3.0 |
| alloy | argocd/observability/alloy-app.yaml | 1.8.1 |
| prometheus-node-exporter | argocd/observability/node-exporter-app.yaml | 4.55.0 |
| tempo-distributed | argocd/observability/tempo-app.yaml | 1.61.3 |
| mimir-distributed | argocd/observability/mimir-app.yaml | 6.0.6 |
| grafana | argocd/observability/grafana-app.yaml | 10.5.15 |
| postgresql (frolf) | argocd/apps/frolf-postgres.yaml | 16.2.4 |
| postgresql (resume) | argocd/apps/resume-postgres.yaml | 18.1.13 |
| nats | argocd/apps/frolf-nats.yaml | 2.14.0 |

### Notable observations

- **frolf-postgres** remains at chart `16.2.4`. Before any future update to this
  chart, the PostgreSQL major version in the new chart's `appVersion` MUST be
  verified. A PG major version bump (e.g. 16→17) will cause `initdb` to run on
  startup and **wipe the frolf database**. See the warning comment in
  `argocd/apps/frolf-postgres.yaml`.

- **resume-postgres** is at chart `18.1.13`. Same major-version check applies.

- **cert-manager** uses a `v` prefix on `targetRevision` (currently `v1.16.2`).
  This prefix must be preserved when updating.

---

## Summary

- **17** charts checked (version read from files only — no upstream query possible)
- **0** charts updated
- **17** charts unverified (cannot determine if newer versions exist)
- **Action required**: Fix egress policy so the routine can reach Helm chart
  repositories or the GitHub API. See "What is needed to fix this" above.
