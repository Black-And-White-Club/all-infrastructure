# Helm Chart Updates — 2026-08-25

> Auto-generated by nightly-helm-version-check routine.
> Versions sourced from GitHub chart source repos via `git ls-remote` and `raw.githubusercontent.com` Chart.yaml
> (Helm CLI and Artifact Hub were unavailable due to network policy; bitnami Helm OCI registry likewise blocked).

## Summary

17 charts checked. 9 updated. 5 skipped (require manual review). 3 already at latest.

## Updates Applied

| Chart | File | Old Version | New Version | Bump Type | Notes |
|-------|------|-------------|-------------|-----------|-------|
| sealed-secrets | argocd/platform/sealed-secrets-controller.yaml | 2.18.5 | 2.19.3 | minor | |
| argo-rollouts | argocd/platform/argo-rollouts.yaml | 2.40.9 | 2.41.1 | minor | |
| cert-manager | argocd/platform/cert-manager.yaml | v1.16.2 | v1.19.6 | minor | 3 minor versions; review cert-manager changelog for deprecations |
| argocd-image-updater | argocd/platform/argocd-image-updater.yaml | 1.1.5 | 1.2.4 | minor | |
| alloy | argocd/observability/alloy-app.yaml | 1.8.1 | 1.12.0 | minor | |
| prometheus-node-exporter | argocd/observability/node-exporter-app.yaml | 4.55.0 | 4.56.1 | patch | |
| mimir-distributed | argocd/observability/mimir-app.yaml | 6.0.6 | 6.2.0 | minor | Stable release confirmed (no -weekly/-rc suffix) |
| postgresql (frolf) | argocd/apps/frolf-postgres.yaml | 16.2.4 | 16.7.27 | minor | **PostgreSQL major version UNCHANGED** — both chart 16.2.4 and 16.7.27 ship PostgreSQL 17.x (verified via Chart.yaml). The PINNED comment refers to the old bitnami versioning where chart 16.x = PG16. After bitnami's repo restructure, chart 16.x now tracks PG17. This update moves from PG 17.2.0 → PG 17.6.0. |
| nats | argocd/apps/frolf-nats.yaml | 2.14.0 | 2.14.5 | patch | |

## ⚠️ Breaking Changes / Skipped

| Chart | File | Current | Latest Available | Reason |
|-------|------|---------|------------------|--------|
| argo-cd | argocd/platform/argocd.yaml | 9.5.14 | 10.4.0 | ⚠️ **MAJOR chart version bump (9→10).** Likely contains breaking changes, CRD migrations, or API removals. Requires explicit human review before upgrade. |
| loki | argocd/observability/loki-app.yaml | 6.55.0 | 7.3.0 | ⚠️ **Requires repo migration.** A comment in loki-app.yaml says: *"OSS users must switch repoURL to https://grafana-community.github.io/helm-charts before upgrading. 6.55.0 is the last OSS-compatible version in this repo."* This is not a simple version bump — the repoURL must be changed AND the SSD mode is deprecated. Requires manual migration planning. |
| kube-state-metrics | argocd/observability/kube-state-metrics-app.yaml | 7.3.0 | 8.4.0 | ⚠️ **MAJOR chart version bump (7→8).** May contain breaking changes in metrics naming or label selectors. Requires explicit human review before upgrade. |
| tailscale-operator | argocd/platform/tailscale-operator.yaml | 1.96.5 | unknown | ❓ **Cannot determine latest version.** The tailscale Helm registry (pkgs.tailscale.com) is not reachable via network policy, and the GitHub tailscale/helm-charts repo uses a different version numbering (helm-chart/1.x.x) that does not map to the targetRevision (1.96.5 follows Tailscale release numbering). Manual check required: visit https://pkgs.tailscale.com/helmcharts/. |
| postgresql (resume) | argocd/apps/resume-postgres.yaml | 18.1.13 | unknown | ❓ **Cannot determine latest version.** Chart version 18.1.13 is not present in the bitnami/charts GitHub repo tags (latest git tag is postgresql/17.1.0). This chart appears to be served from the bitnami OCI registry which is not reachable via network policy. Cannot perform PG major version safety check without access. Manual check required before any update. |

## No Changes (already at latest)

- ingress-nginx: 4.15.1 (already latest — confirmed via git tag `helm-chart-4.15.1`)
- tempo-distributed: 1.61.3 (already latest — confirmed via git tag `tempo-distributed-1.61.3`)
- grafana: 10.5.15 (already latest — confirmed via git tag `grafana-10.5.15`)

## PostgreSQL Version Safety Notes

**frolf-postgres investigation**: The bitnami/charts GitHub repo was restructured. Contrary to what the PINNED comment's historical context implies, the current chart 16.2.4 (and all 16.x chart versions in the current repo) ships **PostgreSQL 17.x**, NOT PostgreSQL 16.x. Specifically:
- Chart 16.2.4 → PostgreSQL 17.2.0 (verified from Chart.yaml at `refs/tags/postgresql/16.2.4`)
- Chart 16.7.27 → PostgreSQL 17.6.0 (verified from Chart.yaml at `refs/tags/postgresql/16.7.27`)

The database is already running PostgreSQL 17. Updating to 16.7.27 is a PostgreSQL minor version bump (17.2 → 17.6) and is safe. The "16.x→17.x transition" mentioned in the PINNED comment refers to the **old** bitnami chart versioning scheme (where chart 16 = PG16), not the current one.

**resume-postgres**: At chart 18.1.13 with PostgreSQL version unknown (OCI registry not accessible). No update applied.

## Changelogs

### sealed-secrets: 2.18.5 → 2.19.3

**GitHub releases**: https://github.com/bitnami-labs/sealed-secrets/releases/tag/helm-v2.19.3
*(Release notes not fetched — GitHub API access is scoped to black-and-white-club/all-infrastructure only)*

---

### argo-rollouts: 2.40.9 → 2.41.1

**GitHub releases**: https://github.com/argoproj/argo-helm/releases/tag/argo-rollouts-2.41.1
*(Release notes not fetched — GitHub API access is scoped to black-and-white-club/all-infrastructure only)*

---

### cert-manager: v1.16.2 → v1.19.6

**GitHub releases**: https://github.com/cert-manager/cert-manager/releases/tag/v1.19.6
*(Release notes not fetched — GitHub API access is scoped to black-and-white-club/all-infrastructure only)*

> **Note**: This spans 3 minor versions (v1.16 → v1.17 → v1.18 → v1.19). Recommended to review the release notes for each version before merging, especially for CRD changes or webhook configuration changes.

---

### argocd-image-updater: 1.1.5 → 1.2.4

**GitHub releases**: https://github.com/argoproj/argo-helm/releases/tag/argocd-image-updater-1.2.4
*(Release notes not fetched — GitHub API access is scoped to black-and-white-club/all-infrastructure only)*

---

### alloy: 1.8.1 → 1.12.0

**GitHub releases**: https://github.com/grafana/helm-charts/releases/tag/alloy-1.12.0
*(Release notes not fetched — GitHub API access is scoped to black-and-white-club/all-infrastructure only)*

---

### prometheus-node-exporter: 4.55.0 → 4.56.1

**GitHub releases**: https://github.com/prometheus-community/helm-charts/releases/tag/prometheus-node-exporter-4.56.1
*(Release notes not fetched — GitHub API access is scoped to black-and-white-club/all-infrastructure only)*

---

### mimir-distributed: 6.0.6 → 6.2.0

**GitHub releases**: https://github.com/grafana/helm-charts/releases/tag/mimir-distributed-6.2.0
*(Release notes not fetched — GitHub API access is scoped to black-and-white-club/all-infrastructure only)*

---

### postgresql (frolf): 16.2.4 → 16.7.27

**GitHub tag**: https://github.com/bitnami/charts/releases/tag/postgresql%2F16.7.27
*(Release notes not fetched — GitHub API access is scoped to black-and-white-club/all-infrastructure only)*

**PostgreSQL version**: 17.2.0 → 17.6.0 (same major version, safe minor update)

---

### nats: 2.14.0 → 2.14.5

**GitHub releases**: https://github.com/nats-io/k8s/releases/tag/nats-2.14.5
*(Release notes not fetched — GitHub API access is scoped to black-and-white-club/all-infrastructure only)*

---

## Environment Note

This run was executed with network egress policy that blocked:
- `get.helm.sh` — Helm CLI binary download
- `*.github.io` — Helm chart repository indexes (bitnami-labs, argoproj, grafana, prometheus-community, etc.)
- `artifacthub.io` — Artifact Hub API
- `charts.bitnami.com` — Bitnami Helm registry
- `pkgs.tailscale.com` — Tailscale Helm registry
- GitHub API for non-allowlisted repositories

Version discovery used `git ls-remote` (tag-based) and `raw.githubusercontent.com` Chart.yaml fetches, which were accessible. For all confirmed updates, versions were cross-validated via both git tags and Chart.yaml content.
