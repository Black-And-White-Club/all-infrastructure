# Helm Chart Updates — 2026-06-08

> Auto-generated by nightly-helm-version-check routine.
> Version discovery used GitHub release pages and upstream Chart.yaml files (chart repos returned 403
> in this environment; Helm binary CDN also blocked). All versions were cross-referenced against
> ArtifactHub and GitHub release tags where possible.

## Summary

17 charts checked. 8 updated. 9 require no action or manual review.

- **Updated (8):** sealed-secrets, argo-rollouts, argo-cd, cert-manager, argocd-image-updater, tailscale-operator, kube-state-metrics, alloy
- **Already at latest (6):** ingress-nginx, prometheus-node-exporter, tempo-distributed, mimir-distributed, grafana, nats
- **Skipped — manual review required (3):** frolf-postgres, loki, resume-postgres

---

## Updates Applied

| Chart | File | Old Version | New Version | Bump Type | Notes |
|-------|------|-------------|-------------|-----------|-------|
| sealed-secrets | argocd/platform/sealed-secrets-controller.yaml | 2.18.5 | 2.18.6 | patch | |
| argo-rollouts | argocd/platform/argo-rollouts.yaml | 2.40.9 | 2.41.0 | minor | New: Gateway API HTTPRoute dashboard support |
| argo-cd | argocd/platform/argocd.yaml | 9.5.14 | 9.5.20 | patch | Redis exporter dep bump |
| cert-manager | argocd/platform/cert-manager.yaml | v1.16.2 | v1.20.2 | minor | Spans 4 minor versions; see changelog below |
| argocd-image-updater | argocd/platform/argocd-image-updater.yaml | 1.1.5 | 1.2.2 | minor | App updated to v1.2.1 |
| tailscale-operator | argocd/platform/tailscale-operator.yaml | 1.96.5 | 1.98.4 | minor | MagicDNS fix, DNSConfig node affinity |
| kube-state-metrics | argocd/observability/kube-state-metrics-app.yaml | 7.3.0 | 7.4.0 | patch | |
| alloy | argocd/observability/alloy-app.yaml | 1.8.1 | 1.8.2 | patch | |

---

## ⚠️ Breaking Changes / Skipped — Requires Human Review

### 🔴 CRITICAL: frolf-postgres — PostgreSQL Major Version Bump

| Field | Value |
|-------|-------|
| File | argocd/apps/frolf-postgres.yaml |
| Current chart | 16.2.4 (PostgreSQL 16.x) |
| Latest chart | 17.1.0 (PostgreSQL 17.x) |
| Action | **DO NOT UPDATE** |

**Why skipped:** Bitnami postgresql chart series 17.x ships PostgreSQL 17. The currently pinned chart (16.2.4) ships PostgreSQL 16. A major PostgreSQL version upgrade causes `initdb` to run on startup, which **WIPES THE ENTIRE DATABASE**. frolf-bot already lost all its data once this way (PR #10, chart 16.x→17.x transition). The file has an explicit `PINNED:` comment describing exactly this risk.

**To upgrade:** Requires a full pg_dump backup, migration plan, and scheduled maintenance window. This is not a routine version bump.

---

### 🔴 CRITICAL: loki — Requires Repo Migration Before Any Upgrade

| Field | Value |
|-------|-------|
| File | argocd/observability/loki-app.yaml |
| Current chart | 6.55.0 (grafana/helm-charts repo) |
| Latest OSS chart | 7.x+ (grafana-community/helm-charts repo — different repoURL) |
| Action | **DO NOT UPDATE** (migration required first) |

**Why skipped:** The file already has an explicit `MIGRATION REQUIRED` comment:

> chart 7.0.0 in `grafana/loki` is GEL (enterprise) only. OSS users must switch repoURL to
> `https://grafana-community.github.io/helm-charts` before upgrading. 6.55.0 is the last
> OSS-compatible version in this repo. SSD (SimpleScalable) mode is also deprecated as of
> chart 6.52 and will be removed in a future community chart version.

**To upgrade:** Change `repoURL` from `https://grafana.github.io/helm-charts` to
`https://grafana-community.github.io/helm-charts`, then update `targetRevision` to the
latest community chart version (currently 13.x as of mid-2026). Review breaking changes
for each major version between 6.55.0 and the target (each major has documented breaking changes
including Kubernetes version requirements and default deploymentMode changes).

---

### 🟡 NEEDS VERIFICATION: resume-postgres — Cannot Confirm PG Major Version

| Field | Value |
|-------|-------|
| File | argocd/apps/resume-postgres.yaml |
| Current chart | 18.1.13 |
| Likely latest chart | 18.7.2 (ArtifactHub reference; not fully confirmed) |
| Action | **Verify before updating** |

**Why skipped:** The bitnami/postgresql chart CHANGELOG accessible from GitHub only documents
through version 17.1.0 (released 2026-02-25). Chart 18.x.x versions exist on ArtifactHub
(confirmed 18.1.1, referenced 18.7.2) but the chart repo index was inaccessible in this
environment (403). Following the bitnami versioning pattern (chart 16.x = PG16, 17.x = PG17),
chart 18.x likely ships PostgreSQL 18 — meaning 18.1.13→18.7.2 would be a safe same-major
update. However, this cannot be confirmed without verifying `appVersion` in the 18.7.2 Chart.yaml.

**Before updating:** Run `helm show chart bitnami/postgresql --version 18.7.2 | grep appVersion`
to confirm the PostgreSQL major version hasn't changed between 18.1.13 and the target version.
If both are PG18, the update is safe.

---

## No Changes Required (Already at Latest)

| Chart | File | Version |
|-------|------|---------|
| ingress-nginx | argocd/platform/nginx-ingress-app.yaml | 4.15.1 |
| prometheus-node-exporter | argocd/observability/node-exporter-app.yaml | 4.55.0 |
| tempo-distributed | argocd/observability/tempo-app.yaml | 1.61.3 |
| mimir-distributed | argocd/observability/mimir-app.yaml | 6.0.6 (6.1.0 is weekly/pre-release only) |
| grafana | argocd/observability/grafana-app.yaml | 10.5.15 |
| nats | argocd/apps/frolf-nats.yaml | 2.14.0 |

**Note on ingress-nginx:** The kubernetes/ingress-nginx repository was archived on March 24, 2026
and is now read-only. No further releases, bugfixes, or security updates will be made. The
cluster should plan migration to a maintained ingress controller (e.g., the Chainguard fork or
a Gateway API implementation).

**Note on mimir-distributed:** The 6.1.0 series is currently weekly/development-only builds.
Latest stable is 6.0.6. No update applied.

---

## Changelogs

### sealed-secrets: 2.18.5 → 2.18.6

**Release notes** ([view on GitHub](https://github.com/bitnami-labs/sealed-secrets/releases/tag/helm-v2.18.6)):

Released May 21, 2026. Routine chart maintenance release for the sealed-secrets controller.
Full release notes did not load; see link above.

---

### argo-rollouts: 2.40.9 → 2.41.0

**Release notes** ([view on GitHub](https://github.com/argoproj/argo-helm/releases/tag/argo-rollouts-2.41.0)):

Released June 5, 2026.

**What's Changed:**
- Added Gateway API HTTPRoute support for the Argo Rollouts dashboard (contributor: @somaz94)

---

### argo-cd: 9.5.14 → 9.5.20

**Release notes** ([view on GitHub](https://github.com/argoproj/argo-helm/releases/tag/argo-cd-9.5.20)):

Released June 7, 2026.

**What's Changed:**
- Redis exporter dependency bump
- Multiple patch releases in the 9.5.x series with minor dependency updates

---

### cert-manager: v1.16.2 → v1.20.2

**Release notes** ([view on GitHub](https://github.com/cert-manager/cert-manager/releases/tag/v1.20.2)):

Released April 11, 2025 (chart spans v1.16→v1.20, approximately 4 minor versions).

**Notable changes in v1.20.2:**
- Fixes invalid YAML generated in the Helm chart when both `webhook.config` and `webhook.volumes` are defined simultaneously
- Go version updates addressing security vulnerabilities (CVE-2026-24051, CVE-2025-68121)

**⚠️ Reviewer note:** This spans 4 minor versions (v1.16→v1.20). Review the cert-manager release notes for v1.17, v1.18, v1.19, and v1.20 for any API or configuration breaking changes before merging.

---

### argocd-image-updater: 1.1.5 → 1.2.2

**Release notes** ([view on GitHub](https://github.com/argoproj/argo-helm/releases/tag/argocd-image-updater-1.2.2)):

Released May 22, 2026.

**What's Changed:**
- Updated argocd-image-updater application dependency to v1.2.1
- Dependency maintenance updates

---

### tailscale-operator: 1.96.5 → 1.98.4

**Release notes** ([view on GitHub](https://github.com/tailscale/tailscale/releases/tag/v1.98.4)):

**What's Changed (1.98.x series):**
- Fixed MagicDNS resolution of tailnet hostnames after a network change (regression from 1.98.0 patched in 1.98.4)
- Added DNSConfig node affinity and node selector support
- Helm chart priority class names support
- Fixed handling of long service and ingress names

---

### kube-state-metrics: 7.3.0 → 7.4.0

**Release notes** ([view on GitHub](https://github.com/prometheus-community/helm-charts/releases/tag/kube-state-metrics-7.4.0)):

Patch release within the 7.x chart series. No breaking changes expected.
See link above for full diff.

---

### alloy: 1.8.1 → 1.8.2

**Release notes** ([view on GitHub](https://github.com/grafana/alloy/releases/tag/helm-alloy-1.8.2)):

Patch release within the 1.8.x chart series (Alloy app version v1.16.1). No breaking changes expected.
See link above for full diff.

---

## Environment Notes

The following chart repository indexes returned HTTP 403 in this execution environment and
could not be queried directly with `helm search repo`:

- bitnami-labs.github.io, kubernetes.github.io, argoproj.github.io, charts.jetstack.io,
  pkgs.tailscale.com, grafana.github.io, prometheus-community.github.io, charts.bitnami.com,
  nats-io.github.io

Version discovery was performed via GitHub raw Chart.yaml files, GitHub release pages,
ArtifactHub search results, and WebSearch. Versions should be validated with
`helm search repo <chart> --output json` from a network-unrestricted environment before
merging this branch.
