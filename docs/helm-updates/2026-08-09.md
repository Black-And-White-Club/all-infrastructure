# Helm Chart Updates — 2026-08-09

> Auto-generated by nightly-helm-version-check routine.

## ⚠️ Routine Could Not Fetch Latest Versions

**The nightly check could not compare against upstream Helm repos.**

The execution environment's network egress policy blocks outbound HTTPS to all
Helm chart repository endpoints (`bitnami-labs.github.io`,
`kubernetes.github.io`, `argoproj.github.io`, `charts.jetstack.io`,
`pkgs.tailscale.com`, `grafana.github.io`, `prometheus-community.github.io`,
`charts.bitnami.com`, `nats-io.github.io`) as well as GitHub API calls to
external repositories. Helm itself could not be installed for the same reason
(`get.helm.sh` blocked).

**No chart versions were changed this run.** The routine was blocked before it
could determine whether updates exist.

---

## Inventory Audit: Task Config vs. Actual File Versions

The scheduled task's internal inventory (stored in the prompt) no longer
matches what is actually committed in the repository. Someone or something has
been applying Helm chart updates outside this routine's tracking. The
discrepancies are listed below — review them to reconcile the task's version
inventory and to ensure the major bumps below were intentional.

### Charts that diverged from the routine's inventory

| Chart | File | Task's "Current" | Actual in Repo | Drift |
|-------|------|-----------------|----------------|-------|
| sealed-secrets | argocd/platform/sealed-secrets-controller.yaml | 2.17.3 | **2.18.5** | +minor |
| ingress-nginx | argocd/platform/nginx-ingress-app.yaml | 4.14.0 | **4.15.1** | +minor |
| argo-rollouts | argocd/platform/argo-rollouts.yaml | 2.39.0 | **2.40.9** | +minor |
| argo-cd | argocd/platform/argocd.yaml | 9.4.5 | **9.5.14** | +minor |
| argocd-image-updater | argocd/platform/argocd-image-updater.yaml | 1.0.5 | **1.1.5** | +minor |
| loki | argocd/observability/loki-app.yaml | 6.46.0 | **6.55.0** | +minor |
| **kube-state-metrics** | argocd/observability/kube-state-metrics-app.yaml | 6.4.1 | **7.3.0** | ⚠️ MAJOR BUMP (6→7) |
| alloy | argocd/observability/alloy-app.yaml | 1.5.0 | **1.8.1** | +minor |
| prometheus-node-exporter | argocd/observability/node-exporter-app.yaml | 4.49.2 | **4.55.0** | +minor |
| tempo-distributed | argocd/observability/tempo-app.yaml | 1.56.2 | **1.61.3** | +minor |
| mimir-distributed | argocd/observability/mimir-app.yaml | 6.0.3 | **6.0.6** | +patch |
| grafana | argocd/observability/grafana-app.yaml | 10.2.0 | **10.5.15** | +minor |
| **nats** | argocd/apps/frolf-nats.yaml | 1.2.5 | **2.14.0** | ⚠️ MAJOR BUMP (1→2, +12 minor) |

### Charts matching the routine's inventory (no drift)

| Chart | File | Version |
|-------|------|---------|
| cert-manager | argocd/platform/cert-manager.yaml | v1.16.2 |
| tailscale-operator | argocd/platform/tailscale-operator.yaml | 1.96.5 |
| frolf-postgres | argocd/apps/frolf-postgres.yaml | 16.2.4 |
| resume-postgres | argocd/apps/resume-postgres.yaml | 18.1.13 |

---

## ⚠️ Major Version Bumps Already Applied — Review Recommended

### kube-state-metrics: 6.4.1 → 7.3.0 (already in repo)

This is a major chart version bump (6 → 7). Chart major bumps for
`kube-state-metrics` often change default metric labels, expose or retire
metrics, or change RBAC structure. If any Grafana dashboards or alert rules
reference KSM metrics by label, verify they still match. Check the
[kube-state-metrics changelog](https://github.com/prometheus-community/helm-charts/releases?q=kube-state-metrics)
for breaking changes between 6.x and 7.x.

### nats: 1.2.5 → 2.14.0 (already in repo)

This is a major chart version bump (1 → 2) spanning **12 minor releases**.
The NATS Helm chart v2 likely introduced structural changes (JetStream
configuration, account/auth layout, config-reloader behavior). The CLAUDE.md
already documents that a failing sidecar readiness probe took NATS offline
cluster-wide (commit `9d1a4aaf`). Confirm the 2.x chart's probe configuration
and any new sidecar ports or Services are consistent with the fixes in
`charts/nats/values.yaml`. Review the
[nats-io/k8s changelog](https://github.com/nats-io/k8s/releases?q=nats-)
for v2 breaking changes.

---

## PostgreSQL Status (Safety Check)

The PostgreSQL major-version check was not run this cycle (network blocked).
Current pinned versions remain unchanged:

| Chart | File | Chart Version | Notes |
|-------|------|--------------|-------|
| frolf-postgres | argocd/apps/frolf-postgres.yaml | 16.2.4 | Pinned; do NOT upgrade across PG major versions |
| resume-postgres | argocd/apps/resume-postgres.yaml | 18.1.13 | Pinned; check PG major on any chart upgrade |

---

## Action Items for Next Human Review

1. **Update the task's version inventory** — the stored prompt lists stale
   "current" versions. Update the chart inventory table in the scheduled task
   to reflect the actual committed versions listed in the audit table above.

2. **Verify the kube-state-metrics 6→7 upgrade** was intentional and that
   Grafana/alert dashboards still work correctly.

3. **Verify the NATS 1→2 upgrade** was intentional and review v2 probe/sidecar
   configuration against the CLAUDE.md `9d1a4aaf` fix.

4. **Unblock upstream access** (or run this routine from an environment with
   open egress) so that nightly version checks can actually compare against
   latest upstream releases. Until then, this routine can audit drift but
   cannot discover new upstream versions.
